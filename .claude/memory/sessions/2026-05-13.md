### 18:50 - [lead] /bot 거래내역 대금 분리 + 오늘 기준 KST + order_created 제외
- **수행**: /bot trade-history 가 "거래대금"을 모호하게 보여주던 것을 주문금액/체결금액으로 분리. UTC → KST 오늘 기준 정정. 감사용 order_created 이벤트는 응답에서 제외하고 order_submitted/failed/cancelled 중심으로 노출.
- **파일**:
  - `src/api/v1/bot.py` — TradeHistoryResponse 에 order_amount/filled_price/filled_quantity/filled_amount 추가. `today_kst()` 로 필터. Order LEFT JOIN 으로 체결가/수량 동봉. event_type != "order_created" 필터.
  - `frontend/src/types/api.ts` — TradeHistoryItem 에 4개 필드 추가.
  - `frontend/src/components/bot/trade-history-table.tsx` — 컬럼: 주문가/주문수량/주문금액/체결수량/체결금액. `side.toLowerCase() === "buy"` 로 대소문자 무관 비교. 미체결은 "-" 표기.
  - `tests/api/test_trade_history.py` — fixture 를 Order + TradeLog 조합으로 재구성. order_created 제외 + 금액 계산 + 미체결 케이스 검증.
- **결정**:
  - 체결 정보는 TradeLog 가 아니라 Order(filled_price/filled_quantity) 가 단일 진실원. trade_log 에 별도 filled 이벤트가 없으므로 LEFT JOIN 으로 동봉.
  - "거래대금" 같은 통칭 금지. 주문금액 = price*quantity, 체결금액 = filled_price*filled_quantity 로 분리.
- **검증**: `uv run pytest tests/api -q` 188 PASS / `uv run ruff check` clean / `cd frontend && npm run build` OK / `npm test -- --run` 9 PASS.
- **TODO**: 사용자 브라우저 확인은 docker compose 재기동 이후. (구빌드 상태라 미반영)

### 18:55 - [lead] 전체 QA 이슈 팀 분해 시작
- **수행**: 전체 QA 7건(거래내역 외) 일괄 수정 위해 팀 `qa-fixes-batch` 생성. 작업 2건 분해, worktree 격리로 backend/frontend 병렬 투입.
- **파일**: 없음 (팀/태스크/에이전트 메타).
- **결정**:
  - Task #1 (backend): drawdown_guard SELL side-aware + decisions approve 의미 정정(applied_at 분리) + mark_decisions_applied 헬퍼.
  - Task #2 (frontend): /strategy 하드코딩 → 실 API, middleware matcher 루트 명시 + force-dynamic, sidebar.tsx Math.random 결정적 너비, /decisions UI 문구.
  - apply_llm_param_hints 의 DB 우선 정책 자체는 유지. approve != applied 의미 분리만 본 PR 범위로 한정 (LLM 우선 정책 변경은 별건).
- **TODO**: 두 팀원 보고 도착 시 통합 검증, docker compose 재빌드, PR 생성. 본 sessions 파일에 두 팀원 결과 append.

### 18:59 - [frontend] /strategy 실데이터화 + middleware 보호 + sidebar lint + decisions UI 문구
- **수행**: 5건 일괄 수정. (1) /strategy 페이지의 하드코딩 mockTrades/파라미터를 useStrategyConfig·useStrategies 훅 기반 실 API 데이터로 교체. TradeHistoryTable 컴포넌트 재사용. 평균회귀 탭은 "현재 활성 전략에서 미사용" placeholder. (2) middleware.ts matcher에 /dashboard, /strategy, /strategy-config, /decisions bare 경로 추가. (3) strategy-config·decisions 폴더에 layout.tsx 신설, `export const dynamic = "force-dynamic"` 으로 prerender 차단. (4) sidebar.tsx Math.random → 고정 "75%". (5) /decisions StatusBadge를 pending=검토 필요(회색), approved+!applied_at=승인됨—다음 실행 시 후보(노랑), applied/approved+applied_at=적용 완료(녹색), rejected=거부됨(회색/blue-50)으로 변경. approve 토스트를 "다음 봇 실행 시 후보로 등록되었습니다"로 변경. applied_at 표시는 applied 상태에서만.
- **파일**:
  - `frontend/src/app/(authenticated)/strategy/page.tsx` — 전면 재작성 (mock → useStrategyConfig + useStrategies + TradeHistoryTable)
  - `frontend/src/middleware.ts` — matcher에 /dashboard, /strategy, /strategy-config, /decisions bare 경로 추가
  - `frontend/src/app/(authenticated)/strategy-config/layout.tsx` — 신규 (force-dynamic)
  - `frontend/src/app/(authenticated)/decisions/layout.tsx` — 신규 (force-dynamic)
  - `frontend/src/components/ui/sidebar.tsx` — Math.random → "75%"
  - `frontend/src/app/(authenticated)/decisions/page.tsx` — StatusBadge 리팩터 (decision 전체 prop, 상태별 문구/색상)
  - `frontend/src/hooks/mutations/use-review-decision.ts` — approve toast 문구 변경
  - `frontend/__tests__/smoke/pages.test.tsx` — useStrategyConfig/useTradeHistory mock 추가, 검증 문구 갱신
- **결정**: sidebar.tsx는 shadcn/ui 원본이지만 lint 통과를 위해 최소 패치(1줄 결정적 너비). force-dynamic은 layout.tsx에 적용("use client" page에서는 무의미하므로).
- **검증**: `npm run lint` → 0 errors, 3 warnings(기존). `npm run build` → 성공. /decisions·/strategy-config가 ƒ(Dynamic)으로 변경 확인. `npm test -- --run` → 9/9 PASS.

### 18:59 - [backend] drawdown_guard SELL side-aware + decisions approve 의미 정리
- **수행**: SELL 주문이 max_investment 한도에 막히는 버그 수정. approve 엔드포인트에서 applied_at 분리. mark_decisions_applied 헬퍼 신규 추가.
- **파일**:
  - `src/trading/drawdown_guard.py` — check_level2 에 `side: str` 파라미터 추가, SELL 시 max_investment 검사 skip. run_all_checks → check_level2 에 side 전달.
  - `src/trading/order_service.py` — SELL 일 때 current_invested 합산 skip (0 고정).
  - `src/api/v1/decisions.py` — approve 에서 `applied_at = now_kst()` 제거. unused import 정리.
  - `src/trading/llm_decision_loader.py` — `mark_decisions_applied()` 함수 추가 (status=applied + applied_at 일괄 갱신).
  - `tests/trading/test_drawdown_guard.py` — SELL skip / BUY 차단 / SELL 손실률 유지 테스트 3건 추가.
  - `tests/trading/test_order_service.py` — 전략 SELL 한도 초과 통과 + 수동 SELL 회귀 테스트 2건 추가.
  - `tests/api/test_decisions.py` — approve 후 applied_at=None 검증 수정. mark_decisions_applied 단위 테스트 2건 추가.
- **결정**: approve ≠ applied 의미 분리. approve 는 사용자 승인만, applied 는 전략이 실제 적용한 시점에 mark_decisions_applied 로 기록.
- **검증**: `uv run ruff check` clean / `uv run pytest -q` 1913 passed, 2 xpassed.

### 19:05 - [lead] 통합 검증 + docker compose 재빌드
- **수행**: backend/frontend 두 팀원 작업 합쳐서 메인 리포에서 회귀 검증. docker compose up -d --build 로 frontend/backend 컨테이너 재빌드 시작 (백그라운드).
- **검증**:
  - `uv run ruff check src tests` → clean
  - `uv run pytest -q` → **1913 passed, 2 xpassed** (3:01)
  - `cd frontend && npm run lint` → 0 errors, 3 warnings (모두 기존 watch warning)
  - `cd frontend && npm run build` → 성공. `/decisions`, `/strategy-config` 가 ƒ(Dynamic) 으로 표시 — force-dynamic 적용 확인
  - `cd frontend && npm test -- --run` → 9/9 PASS
- **TODO**: docker rebuild 완료 대기 → 무쿠키 curl(`/strategy-config`, `/decisions` 307) 확인 → 사용자 브라우저 검증(/trade SELL 1주, /strategy 실데이터, /bot 거래내역 분리, /decisions 문구) → PR 생성 → TeamDelete.

### 19:08 - [lead] docker compose 재빌드 + 무쿠키 redirect 검증
- **수행**: `docker compose up -d --build frontend backend` 완료 후 컨테이너가 swap 안 돼 `--force-recreate --no-build` 추가 실행. backend/frontend 새 컨테이너 17초만에 healthy.
- **검증**:
  - `curl -sI http://localhost:3000/strategy-config` → **307 Temporary Redirect, location: /login** ✅
  - `curl -sI http://localhost:3000/decisions` → **307 → /login** ✅
  - `curl -sI http://localhost:3000/dashboard` → 307 → /login (회귀 확인) ✅
- **결정**: `docker compose up -d --build` 단독으로는 새 image 가 swap 안 되는 케이스가 있다. 빌드 후 컨테이너 swap 강제 필요시 `--force-recreate --no-build` 한 번 더.
- **TODO**: 사용자 브라우저 검증 → PR 생성 → 본 sessions 에 PR entry append → TeamDelete.

### 19:25 - [lead] 사용자 QA 피드백 3건 후속 수정
- **수행**: 사용자 브라우저 검증 결과 (#1 SELL 통과, #5 보호 OK / #2 #3 #4 불합격) 받은 뒤 3건 후속 수정.
  1. **#2 거래내역 비어있음** — broker 호출 실패 시 Order 가 `created` 상태로 멈춰서 trade_log 에 `order_created` 만 남았었다. orders.py 의 try/except 를 재구성: `place_order` 가 예외 발생해도 `BrokerOrderResponse(status="failed", message=...)` 로 합성해 `submit_order` 호출 → `order_failed` 이벤트 + Order.status=FAILED 로 마킹.
  2. **#3 전략 흐름도 하드코딩** — `MomentumStrategyFlow` 에 `params: MomentumFlowParams` prop 추가 (screenThreshold/volumeRatio/stopLossPct/takeProfitPct/entryEndTime). /strategy 페이지에서 strategy_config DB 값 그대로 전달.
  3. **#4 approved + applied_at row** — `StatusBadge` 에서 approved 분기의 `applied_at` 검사 제거. status==="approved" 면 항상 "승인됨 — 다음 실행 시 후보" 노랑. 카드 상단의 "적용:" 텍스트도 `status === "applied"` 일 때만 표시.
- **파일**:
  - `src/api/v1/orders.py` — BrokerOrderResponse import 추가, broker 예외 시 failed 응답 합성 + submit_order 호출.
  - `frontend/src/components/strategy/strategy-flow.tsx` — MomentumStrategyFlow props 화 (MomentumFlowParams interface 추가).
  - `frontend/src/app/(authenticated)/strategy/page.tsx` — MomentumStrategyFlow 에 실 config prop 전달.
  - `frontend/src/app/(authenticated)/decisions/page.tsx` — StatusBadge.approved 분기 단순화, "적용:" 텍스트는 applied 한정.
- **결정**:
  - mock broker 통신 자체를 우회하지는 않음. 실 흐름(`KiwoomClient.place_order`) 유지, 실패 시에만 합성 응답으로 status 전이 보장. 실거래 전환 시에도 동일 안전망 적용.
  - approved 상태에서 applied_at 가진 기존 row 는 마이그레이션하지 않고 UI 가 status 만 신뢰. DB 쪽 정리는 별건.
- **검증**: `uv run ruff check src tests` clean / `uv run pytest tests/api/test_orders.py tests/api/test_trade_history.py tests/api/test_decisions.py tests/trading -q` 477 PASS / `npm run lint` 0 errors / `npm run build` 성공 / `npm test -- --run` 9/9 PASS.
- **TODO**: docker rebuild 완료 대기 → 사용자 재검증 (#2 #3 #4) → PR 생성 → TeamDelete.
