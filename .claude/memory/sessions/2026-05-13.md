### 18:33 - [backend-api] GET /api/v1/strategy/current 신규 엔드포인트
- **수행**: 활성 전략 현황 + cross_momentum 상세 반환 API 구현. 응답 스키마(StrategyCurrentResponse, CrossMomentumDetail, ExpectedOrdersPreview). next_rebalance_kst KRX 캘린더 기반 계산. balance 조회 실패 시 0 fallback (200 유지). 테스트 7건 전체 통과, 회귀 1943건 통과.
- **파일**:
  - `src/api/v1/strategy.py` — 신규. 라우터 + 스키마 + 헬퍼(_today, _find_last_business_day_of_month, _compute_next_rebalance_kst, _fetch_available_cash, _build_cross_momentum_detail)
  - `src/api/v1/router.py` — strategy_router 등록
  - `tests/api/test_strategy_current.py` — 신규. 7개 테스트(401, none, all_fields, max_order_amount, next_rebalance_kst, universe_size, balance_fallback)

### 18:22 - [backend-core] RebalanceParams strategy_config 화 + 단계 분리 + equal-weight 완전화
- **수행**: cross_momentum 하드코딩 파라미터 8개를 strategy_config DB 외부화. RebalanceParams에 rebalance_freq/min_order_amount/max_order_amount_pct/cash_buffer_pct 추가. compute_rebalance_orders를 target weight 기반 equal-weight로 재작성 (보유 종목 비중 조정 포함). execute_monthly_rebalance를 4-phase(sell→refresh balance→buy→reconcile)로 분리. Alembic 015 마이그레이션 신설.
- **파일**:
  - `src/trading/cross_momentum_rebalance.py` — RebalanceParams 확장, load_rebalance_params 신설, RebalanceOrders에 adjust_sells/adjust_buys/sell_amounts/buy_amounts 추가, _execute_sells/_execute_buys/_compute_reconcile 헬퍼 신설, execute_monthly_rebalance 4-phase 재구성
  - `alembic/versions/015_cross_momentum_config_seed.py` — cross_momentum.* 8개 키 seed
  - `tests/trading/test_cross_momentum_rebalance.py` — 53 테스트 (load_rebalance_params, equal-weight 비중 조정, min/max cap, 4-phase 순서, reconcile structlog 등)
  - `tests/trading/test_cross_momentum_rebalance_t2.py` — T2 테스트 cash_buffer_pct 호환 수정
- **결정**: n_positions=5 기본값 (사용자 요구, 계좌 ~1,152만 기준), top_pct=None이면 n_positions 우선, cash_buffer_pct=0.10 기본
- **검증**: ruff check/format clean, pytest tests/trading 476 passed, pytest 전체 1934 passed

### 18:50 - [lead] /bot 거래내역 대금 분리 + 오늘 기준 KST + order_created 제외
- **수행**: /bot trade-history 가 "거래대금"을 모호하게 보여주던 것을 주문금액/체결금액으로 분리. UTC → KST 오늘 기준 정정. 감사용 order_created 이벤트는 응답에서 제외하고 order_submitted/failed/cancelled 중심으로 노출.
- **파일**:
  - `src/api/v1/bot.py` — TradeHistoryResponse 에 order_amount/filled_price/filled_quantity/filled_amount 추가. `today_kst()` 로 필터. Order LEFT JOIN 으로 체결가/수량 동봉. event_type != "order_created" 필터.
  - `frontend/src/types/api.ts` — TradeHistoryItem 에 4개 필드 추가.
  - `frontend/src/components/bot/trade-history-table.tsx` — 컬럼: 주문가/주문수량/주문금액/체결수량/체결금액. `side.toLowerCase() === "buy"` 로 대소문자 무관 비교. 미체결은 "-" 표기.
  - `tests/api/test_trade_history.py` — fixture 를 Order + TradeLog 조합으로 재구성. order_created 제외 + 금액 계산 + 미체결 케이스 검증.
- **결정**:
  - 체결 정보는 TradeLog 가 아니라 Order(filled_price/filled_quantity) 가 단일 진실원. trade_log 에 별도 filled 이벤트가 없으므로 LEFT JOIN 으로 동봉.
  - "거래대금" 같은 통칭 금지. 주문금액 = price*quantity, 체결금액 = filled_price*filled_quantity 로 분리.
- **검증**: `uv run pytest tests/api -q` 188 PASS / `uv run ruff check` clean / `cd frontend && npm run build` OK / `npm test -- --run` 9 PASS.
- **TODO**: 사용자 브라우저 확인은 docker compose 재기동 이후. (구빌드 상태라 미반영)

### 18:55 - [lead] 전체 QA 이슈 팀 분해 시작
- **수행**: 전체 QA 7건(거래내역 외) 일괄 수정 위해 팀 `qa-fixes-batch` 생성. 작업 2건 분해, worktree 격리로 backend/frontend 병렬 투입.
- **파일**: 없음 (팀/태스크/에이전트 메타).
- **결정**:
  - Task #1 (backend): drawdown_guard SELL side-aware + decisions approve 의미 정정(applied_at 분리) + mark_decisions_applied 헬퍼.
  - Task #2 (frontend): /strategy 하드코딩 → 실 API, middleware matcher 루트 명시 + force-dynamic, sidebar.tsx Math.random 결정적 너비, /decisions UI 문구.
  - apply_llm_param_hints 의 DB 우선 정책 자체는 유지. approve != applied 의미 분리만 본 PR 범위로 한정 (LLM 우선 정책 변경은 별건).
- **TODO**: 두 팀원 보고 도착 시 통합 검증, docker compose 재빌드, PR 생성. 본 sessions 파일에 두 팀원 결과 append.

### 18:59 - [frontend] /strategy 실데이터화 + middleware 보호 + sidebar lint + decisions UI 문구
- **수행**: 5건 일괄 수정. (1) /strategy 페이지의 하드코딩 mockTrades/파라미터를 useStrategyConfig·useStrategies 훅 기반 실 API 데이터로 교체. TradeHistoryTable 컴포넌트 재사용. 평균회귀 탭은 "현재 활성 전략에서 미사용" placeholder. (2) middleware.ts matcher에 /dashboard, /strategy, /strategy-config, /decisions bare 경로 추가. (3) strategy-config·decisions 폴더에 layout.tsx 신설, `export const dynamic = "force-dynamic"` 으로 prerender 차단. (4) sidebar.tsx Math.random → 고정 "75%". (5) /decisions StatusBadge를 pending=검토 필요(회색), approved+!applied_at=승인됨—다음 실행 시 후보(노랑), applied/approved+applied_at=적용 완료(녹색), rejected=거부됨(회색/blue-50)으로 변경. approve 토스트를 "다음 봇 실행 시 후보로 등록되었습니다"로 변경. applied_at 표시는 applied 상태에서만.
- **파일**:
  - `frontend/src/app/(authenticated)/strategy/page.tsx` — 전면 재작성 (mock → useStrategyConfig + useStrategies + TradeHistoryTable)
  - `frontend/src/middleware.ts` — matcher에 /dashboard, /strategy, /strategy-config, /decisions bare 경로 추가
  - `frontend/src/app/(authenticated)/strategy-config/layout.tsx` — 신규 (force-dynamic)
  - `frontend/src/app/(authenticated)/decisions/layout.tsx` — 신규 (force-dynamic)
  - `frontend/src/components/ui/sidebar.tsx` — Math.random → "75%"
  - `frontend/src/app/(authenticated)/decisions/page.tsx` — StatusBadge 리팩터 (decision 전체 prop, 상태별 문구/색상)
  - `frontend/src/hooks/mutations/use-review-decision.ts` — approve toast 문구 변경
  - `frontend/__tests__/smoke/pages.test.tsx` — useStrategyConfig/useTradeHistory mock 추가, 검증 문구 갱신
- **결정**: sidebar.tsx는 shadcn/ui 원본이지만 lint 통과를 위해 최소 패치(1줄 결정적 너비). force-dynamic은 layout.tsx에 적용("use client" page에서는 무의미하므로).
- **검증**: `npm run lint` → 0 errors, 3 warnings(기존). `npm run build` → 성공. /decisions·/strategy-config가 ƒ(Dynamic)으로 변경 확인. `npm test -- --run` → 9/9 PASS.

### 18:59 - [backend] drawdown_guard SELL side-aware + decisions approve 의미 정리
- **수행**: SELL 주문이 max_investment 한도에 막히는 버그 수정. approve 엔드포인트에서 applied_at 분리. mark_decisions_applied 헬퍼 신규 추가.
- **파일**:
  - `src/trading/drawdown_guard.py` — check_level2 에 `side: str` 파라미터 추가, SELL 시 max_investment 검사 skip. run_all_checks → check_level2 에 side 전달.
  - `src/trading/order_service.py` — SELL 일 때 current_invested 합산 skip (0 고정).
  - `src/api/v1/decisions.py` — approve 에서 `applied_at = now_kst()` 제거. unused import 정리.
  - `src/trading/llm_decision_loader.py` — `mark_decisions_applied()` 함수 추가 (status=applied + applied_at 일괄 갱신).
  - `tests/trading/test_drawdown_guard.py` — SELL skip / BUY 차단 / SELL 손실률 유지 테스트 3건 추가.
  - `tests/trading/test_order_service.py` — 전략 SELL 한도 초과 통과 + 수동 SELL 회귀 테스트 2건 추가.
  - `tests/api/test_decisions.py` — approve 후 applied_at=None 검증 수정. mark_decisions_applied 단위 테스트 2건 추가.
- **결정**: approve ≠ applied 의미 분리. approve 는 사용자 승인만, applied 는 전략이 실제 적용한 시점에 mark_decisions_applied 로 기록.
- **검증**: `uv run ruff check` clean / `uv run pytest -q` 1913 passed, 2 xpassed.

### 19:05 - [lead] 통합 검증 + docker compose 재빌드
- **수행**: backend/frontend 두 팀원 작업 합쳐서 메인 리포에서 회귀 검증. docker compose up -d --build 로 frontend/backend 컨테이너 재빌드 시작 (백그라운드).
- **검증**:
  - `uv run ruff check src tests` → clean
  - `uv run pytest -q` → **1913 passed, 2 xpassed** (3:01)
  - `cd frontend && npm run lint` → 0 errors, 3 warnings (모두 기존 watch warning)
  - `cd frontend && npm run build` → 성공. `/decisions`, `/strategy-config` 가 ƒ(Dynamic) 으로 표시 — force-dynamic 적용 확인
  - `cd frontend && npm test -- --run` → 9/9 PASS
- **TODO**: docker rebuild 완료 대기 → 무쿠키 curl(`/strategy-config`, `/decisions` 307) 확인 → 사용자 브라우저 검증(/trade SELL 1주, /strategy 실데이터, /bot 거래내역 분리, /decisions 문구) → PR 생성 → TeamDelete.

### 19:08 - [lead] docker compose 재빌드 + 무쿠키 redirect 검증
- **수행**: `docker compose up -d --build frontend backend` 완료 후 컨테이너가 swap 안 돼 `--force-recreate --no-build` 추가 실행. backend/frontend 새 컨테이너 17초만에 healthy.
- **검증**:
  - `curl -sI http://localhost:3000/strategy-config` → **307 Temporary Redirect, location: /login** ✅
  - `curl -sI http://localhost:3000/decisions` → **307 → /login** ✅
  - `curl -sI http://localhost:3000/dashboard` → 307 → /login (회귀 확인) ✅
- **결정**: `docker compose up -d --build` 단독으로는 새 image 가 swap 안 되는 케이스가 있다. 빌드 후 컨테이너 swap 강제 필요시 `--force-recreate --no-build` 한 번 더.
- **TODO**: 사용자 브라우저 검증 → PR 생성 → 본 sessions 에 PR entry append → TeamDelete.

### 19:25 - [lead] 사용자 QA 피드백 3건 후속 수정
- **수행**: 사용자 브라우저 검증 결과 (#1 SELL 통과, #5 보호 OK / #2 #3 #4 불합격) 받은 뒤 3건 후속 수정.
  1. **#2 거래내역 비어있음** — broker 호출 실패 시 Order 가 `created` 상태로 멈춰서 trade_log 에 `order_created` 만 남았었다. orders.py 의 try/except 를 재구성: `place_order` 가 예외 발생해도 `BrokerOrderResponse(status="failed", message=...)` 로 합성해 `submit_order` 호출 → `order_failed` 이벤트 + Order.status=FAILED 로 마킹.
  2. **#3 전략 흐름도 하드코딩** — `MomentumStrategyFlow` 에 `params: MomentumFlowParams` prop 추가 (screenThreshold/volumeRatio/stopLossPct/takeProfitPct/entryEndTime). /strategy 페이지에서 strategy_config DB 값 그대로 전달.
  3. **#4 approved + applied_at row** — `StatusBadge` 에서 approved 분기의 `applied_at` 검사 제거. status==="approved" 면 항상 "승인됨 — 다음 실행 시 후보" 노랑. 카드 상단의 "적용:" 텍스트도 `status === "applied"` 일 때만 표시.
- **파일**:
  - `src/api/v1/orders.py` — BrokerOrderResponse import 추가, broker 예외 시 failed 응답 합성 + submit_order 호출.
  - `frontend/src/components/strategy/strategy-flow.tsx` — MomentumStrategyFlow props 화 (MomentumFlowParams interface 추가).
  - `frontend/src/app/(authenticated)/strategy/page.tsx` — MomentumStrategyFlow 에 실 config prop 전달.
  - `frontend/src/app/(authenticated)/decisions/page.tsx` — StatusBadge.approved 분기 단순화, "적용:" 텍스트는 applied 한정.
- **결정**:
  - mock broker 통신 자체를 우회하지는 않음. 실 흐름(`KiwoomClient.place_order`) 유지, 실패 시에만 합성 응답으로 status 전이 보장. 실거래 전환 시에도 동일 안전망 적용.
  - approved 상태에서 applied_at 가진 기존 row 는 마이그레이션하지 않고 UI 가 status 만 신뢰. DB 쪽 정리는 별건.
- **검증**: `uv run ruff check src tests` clean / `uv run pytest tests/api/test_orders.py tests/api/test_trade_history.py tests/api/test_decisions.py tests/trading -q` 477 PASS / `npm run lint` 0 errors / `npm run build` 성공 / `npm test -- --run` 9/9 PASS.
- **TODO**: docker rebuild 완료 대기 → 사용자 재검증 (#2 #3 #4) → PR 생성 → TeamDelete.

### 19:56 - [lead] PR #400/#401 머지 + claude 동기화 + 팀 해제
- **수행**: 사용자 재검증 통과(#2 order_failed row 표시 / #3 흐름도 75% 0.3x 13:00 / #4 카드 "적용:" 사라짐) 후 릴리스 사이클 진행.
- **PR**:
  - PR #400 (feat/qa-fixes-batch → dev) — Ruff format 1차 fail → `ruff format` 적용 후 `fix(qa): ruff format apply on test files` 추가 커밋 → 전체 checks pass (Pytest 6m35s) → squash 머지.
  - PR #401 (dev → main, merge commit) — Pytest 6m50s + 모든 보안/lint pass → merge.
- **검증**: 무쿠키 curl 회귀 OK (이전 entry). main 머지 후 claude 브랜치를 `git merge origin/main` (non-ff) 로 동기화, force-push 없이 push 성공 (99a6629 → 4fd143e).
- **결정**: 로컬에서 ruff format을 평소에 적용하지 않아 CI 1차 fail. 이후 작업부터는 `uv run ruff format src/ tests/` 도 commit 전 단계에 포함할 것.
- **TODO**: feat/qa-fixes-batch 로컬/원격 브랜치 정리. 팀 `qa-fixes-batch` TeamDelete.

### 20:03 - [frontend] middleware fail-closed + JWT env 주입 + Next.js 16.2.6 업그레이드
- **수행**: 4건 일괄 수정. (1) `isTokenValid` fail-open 제거 → JWT_SECRET_KEY 미설정 시 `return false` + `console.error`. (2) docker-compose.yml frontend 서비스에 `JWT_SECRET_KEY=${JWT_SECRET_KEY}` env 추가. (3) Next.js 16.1.6 → 16.2.6 + eslint-config-next 동기 업그레이드, lodash high 해소. (4) middleware 단위 테스트 4건 추가 (fail-closed / valid token / bad token / no cookie).
- **파일**:
  - `frontend/src/middleware.ts` — isTokenValid fail-open→fail-closed
  - `docker-compose.yml` — frontend environment에 JWT_SECRET_KEY 추가
  - `frontend/package.json` + `frontend/package-lock.json` — next 16.2.6, eslint-config-next 16.2.6
  - `frontend/__tests__/smoke/middleware.test.ts` — 신규 (4 test cases)
- **결정**: postcss moderate (next 내부 번들) 는 fix 시 next@9.x 다운그레이드 필요 → 별건 PR 로 보류. lodash high 는 `npm audit fix` 로 해소.
- **검증**: `npm run lint` → 0 errors, 3 warnings(기존). `npm run build` → 성공, /decisions·/strategy-config ƒ(Dynamic). `npm test -- --run` → 13/13 PASS. `npm audit --omit=dev` → postcss moderate 1건만 잔존 (next high + lodash high 해소).

### 20:07 - [backend] 실거래 전환 서버 게이트 구현
- **수행**: broker credential POST/PUT API에서 `is_mock=False` 저장을 환경 플래그(`ALLOW_REAL_TRADING`) + confirm phrase(`REAL_TRADING_CONFIRM_PHRASE`) 2중 검증으로 차단. 기본값은 항상 모의투자.
- **파일**:
  - `src/config/settings.py` — `allow_real_trading: bool = False`, `real_trading_confirm_phrase: str = ""` 추가.
  - `src/api/v1/settings.py` — `_validate_real_trading_gate()` 헬퍼 추가. POST/PUT 에서 `is_mock=False` 시 3단계 검증(env allow → phrase 비어있으면 거부 → phrase 일치). 통과 시 audit warning 로그. 스키마에 `real_trading_confirm` 필드 추가, 응답에는 노출 안 됨.
  - `tests/api/test_settings.py` — 기존 update 테스트 회귀 수정 + `TestRealTradingGateCreate`(5건), `TestRealTradingGateUpdate`(4건) 신설.
- **결정**: 모든 403 에러 메시지를 동일 문자열("실거래 전환이 환경에서 허용되지 않습니다")로 통일해 정보 누출 방지. `get_settings`는 `_validate_real_trading_gate` 함수 내 lazy import로 lru_cache 패턴 유지.
- **검증**: `uv run ruff check` clean / `uv run ruff format --check` clean / `uv run pytest tests/api/test_settings.py` 16/16 PASS / `uv run pytest -q` 1922 passed, 2 xpassed (회귀 0건).

### 10:07 - [lead] 보안 P0 통합 검증 + docker rebuild + curl 회귀 (5/14 KST)
- **수행**: backend `_validate_real_trading_gate` + frontend middleware fail-closed + JWT_SECRET_KEY env 통합. `docker compose up -d --build --force-recreate` 로 컨테이너 swap.
- **검증**:
  - `uv run ruff check src tests` clean / `uv run ruff format --check` clean
  - `uv run pytest tests/api -q` → **199 PASS** (전체 1922 + 2 xpassed)
  - `cd frontend && npm run lint` → 0 errors / `npm test -- --run` → **13/13 PASS** (smoke 9 + middleware 4)
  - `npm run build` 성공
- **P0 회귀 curl** (브라우저 노출 차단 확인):
  - fake cookie + `/decisions` → **307 /login** (이전 200 HTML → 차단) ✅
  - fake cookie + `/strategy-config` → **307 /login** ✅
  - no cookie + `/decisions` → 307 회귀 유지 ✅
  - fake cookie + `/api/v1/decisions` → 401 회귀 유지 ✅
  - 컨테이너 `$JWT_SECRET_KEY` → **set** (이전 missing) ✅
- **TODO**: feat/security-p0-fixes push → PR (dev base) → Actions → dev/main 머지 → claude 동기화 → TeamDelete.

### 11:00 - [lead] PR #402/#403 머지 + claude 동기화 + 팀 해제 (5/14 KST)
- **수행**:
  - PR #402 (feat/security-p0-fixes → dev) — 전체 checks pass (Pytest 6m33s) → squash 머지.
  - PR #403 (dev → main, merge commit) — Pytest 6m7s + 보안/lint 전부 pass → merge.
  - claude ← origin/main merge. sessions 파일 conflict 는 main 쪽 (보안 P0 entries 포함) 채택.
- **정리**: feat/security-p0-fixes 로컬·원격 삭제. 팀 `security-p0-fixes` TeamDelete 예정.
- **TODO**: P1/P2 (백엔드 prod deps, docker DB 비번, Bandit/secret 정리) 별건 PR 검토.

### 12:30 - [lead] PR #404/#405 머지 — backend/frontend audit clean (5/14 KST)
- **수행**:
  - 백엔드 prod 의존성 8개 업그레이드 (cryptography 48.0.0, curl-cffi 0.15.0/yfinance 1.3.0, ecdsa 0.19.2, requests 2.34.1, urllib3 2.7.0, mako 1.3.12, pyasn1 0.6.3, pygments 2.20.0).
  - 프론트 `package.json` 에 `overrides: { "postcss": "^8.5.10" }` 추가해 Next 번들 postcss XSS 해소.
- **PR**: #404 → dev squash (test.yml 트리거는 paths `**.py` 라 secutiy/audit/scan만 도는데 모두 pass), #405 → main merge commit.
- **검증**:
  - `uv run pip-audit -r prod-export` → **No known vulnerabilities found**
  - `npm audit --omit=dev --audit-level=moderate` → **found 0 vulnerabilities**
  - `uv run pytest -q` 로컬 → 1922 passed, 2 xfailed (이전 xpassed→xfailed, 회귀 0).
- **결정**:
  - cryptography 46.0.x → 48.0.0 major 이지만 audit 해소 + 회귀 없음 확인 후 채택.
  - ecdsa CVE-2024-23342 (타이밍 공격) 는 fix 미공개 → python-jose JWT 라이브러리 교체는 별건.
- **TODO**: P2 (docker-compose DB 기본 비번, Bandit low 정리, 테스트 secret false positive 정리). 사용자가 우선순위 결정 시 진행.

### 13:01 - [lead] docker-compose POSTGRES_PASSWORD fail-fast (5/14 KST)
- **수행**: `docker-compose.yml:29` 의 `POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}` → `${POSTGRES_PASSWORD:?POSTGRES_PASSWORD env required}` 로 fail-fast. env 누락 시 컨테이너가 silently 디폴트 'password' 로 뜨던 사고 차단.
- **파일**: `docker-compose.yml`
- **결정**: 보안 QA P2 중 가성비 최고 (1줄, 미래 배포 사고 차단). 나머지 P2 (Bandit low, 테스트 secret naming, python-jose → PyJWT) 는 가성비 낮아 보류.
- **사용자 측 요구**: `.env` 에 `POSTGRES_PASSWORD=<값>` 한 줄 추가. 보안 hook 으로 리드가 직접 .env 변경 불가.
- **검증**: `docker compose config` → POSTGRES_PASSWORD 미설정 시 명확한 에러 메시지 출력 (예상 동작).

### 15:02 - [lead] /dashboard 주문가능금액 음수 표시 버그 — entr vs ord_alow_amt 분리 (5/14 KST)
- **수행**: 사용자 보고 `available_cash=-21440980` 음수 표시 버그 수정. Kiwoom kt00001 응답에서 `entr`(예수금)와 `ord_alow_amt`(주문가능금액)를 분리 매핑. 예수금은 정산 중 음수 가능, 주문가능금액은 0 으로 clamp.
- **파일**:
  - `src/broker/schemas.py` — `AccountBalance.deposit: int` 추가. available_cash 의미 정정.
  - `src/broker/kiwoom.py` — `ord_alow_amt` 우선, `entr` fallback, max(0,…) clamp.
  - `src/broker/constants.py` — kt00001 주석 정정.
  - `tests/broker/test_kiwoom.py` — 기존 케이스 + 음수 clamp + ord_alow_amt 우선 2건 신설.
  - `frontend/src/types/api.ts` — AccountBalance.deposit 추가.
  - `frontend/src/app/(authenticated)/dashboard/page.tsx` — 보조 텍스트 "예수금 ₩X (정산 중)".
- **결정**: trading logic 은 `available_cash` 만 사용. backend 단 clamp 로 KillSwitch/포지션 산정 보호. `deposit` 은 진단/UI용.
- **검증**: ruff clean / pytest 1924 passed, 2 xpassed / npm lint 0 errors / vitest 13/13 / build OK.

### 17:16 - [lead] 사용자 검증 통과 — balance 음수 표시 해소 (5/14 KST)
- **사용자 검증**: PR #408/#409 적용 + docker rebuild 후 OK.
  - API: `deposit=-21440980` (정산 중), `available_cash=11523649` (clamp 후 양수)
  - /dashboard: 주문가능금액 ₩11,523,649 + 보조문구 "예수금 ₩-21,440,980 (정산 중)" 정상 표시
  - 옛 보조문구 "현재 매수 가능한 금액" 사라짐 확인
- **이슈/메모**:
  - 1차 검증 실패는 컨테이너가 옛 빌드였기 때문. PR #406 적용 후 .env에 `POSTGRES_PASSWORD=` 가 없어서 docker compose up 자체가 실패했고, 사용자가 임시로 `POSTGRES_PASSWORD=password` 주입 후 재빌드 → 정상.
  - 사용자 측 영구 액션: 로컬 `.env` 에 `POSTGRES_PASSWORD=<값>` 한 줄 추가 (현재 DB 데이터 유지하려면 `password` 그대로).

### 18:43 - [frontend] /strategy ACTIVE_STRATEGY 분기 + cross_momentum dashboard
- **수행**: /strategy 페이지를 GET /api/v1/strategy/current 기반으로 active_strategy 3분기(cross_momentum / multi_regime / none) 렌더링. CrossMomentumDashboard 신규 컴포넌트(6개 카드: 수식/유니버스/필터/일정/포지션 설정/예상 주문). useStrategyCurrent 훅 + 타입 + 상수 + formatRebalanceDate 유틸 추가. 스모크 테스트 4건 추가(cross_momentum dashboard 렌더 + universe_size/n_positions 확인, none placeholder, multi_regime 기존 ParamSummary 유지).
- **파일**:
  - `frontend/src/types/api.ts` — ExpectedOrdersPreview, CrossMomentumDetail, StrategyCurrentResponse 타입 추가
  - `frontend/src/lib/constants.ts` — API_PATHS.STRATEGY_CURRENT, QUERY_KEYS.STRATEGY_CURRENT 추가
  - `frontend/src/hooks/queries/use-strategy-current.ts` — 신규
  - `frontend/src/lib/format.ts` — formatRebalanceDate 추가 ("2026-05-29 (목) 14:55" 형태)
  - `frontend/src/components/strategy/cross-momentum-dashboard.tsx` — 신규 (6카드 그리드)
  - `frontend/src/app/(authenticated)/strategy/page.tsx` — active_strategy 3분기 + MultiRegimeView 분리
  - `frontend/__tests__/smoke/pages.test.tsx` — useStrategyCurrent 동적 mock + 4 신규 테스트 케이스
- **검증**: `npm run lint` → 0 errors, 2 warnings(기존). `npm run build` → 성공, /strategy ○(Static). `npm test -- --run` → 15/15 PASS (middleware 4 + pages 11).

### 19:16 - [lead] 정리 + SHORT_SWING_STRATEGY_DESIGN.md 트리거 메모리 등록 (5/14 KST)
- **수행**:
  - PR #410/#411 머지 후 환경 정리. worktree 1개(메인만), 로컬 브랜치 3개(claude/dev/main) 잔존 — 모두 origin 과 ff-sync. feat 브랜치 잔존 없음, 팀 디렉토리 비어있음(`~/.claude/teams/` 모두 해제됨).
  - 새 메모리 `project_short_swing_design_trigger.md` 등록 — `SHORT_SWING_STRATEGY_DESIGN.md` 파일이 repo 에 추가되면 자동으로 전략 전면 재수정 트리거. 사용자가 추가 지시 없어도 즉시 진행하라는 의도.
- **결정**: 기존 cross_momentum 코드는 그대로 두고, 새 설계 등장 시 별 PR 로 정리(보관/폐기) — 한 PR 회귀 위험 큼.
- **TODO**: 사용자가 `SHORT_SWING_STRATEGY_DESIGN.md` 추가하면 즉시 작업 시작.

### 19:32 - [backend-short-swing] PR 1: config + model + screener 구현
- **수행**: short_swing 전략 PR 1 전체 구현. ActiveStrategy.SHORT_SWING enum 추가, alembic 016 (strategy_config 23키 seed), alembic 017 (short_swing_candidates 테이블), ShortSwingCandidate 모델, short_swing_screener (유니버스→지표 계산→6개 필터→점수→상위 N 저장), 테스트 9케이스, 설계 문서 docs/design/ 이동.
- **파일**:
  - `src/config/active_strategy.py` — SHORT_SWING = "short_swing" 추가
  - `alembic/versions/016_short_swing_config_seed.py` — 23키 멱등 seed (ON CONFLICT DO NOTHING)
  - `alembic/versions/017_short_swing_candidates_table.py` — 테이블 + UNIQUE(trade_date, symbol) + 인덱스
  - `src/models/short_swing.py` — ShortSwingCandidate (14컬럼, UUIDMixin + TimestampMixin)
  - `src/models/__init__.py` — ShortSwingCandidate 등록
  - `src/screening/short_swing_screener.py` — run_short_swing_screening 진입점, _build_universe/_load_candles/_calc_metrics/_apply_filters/_calc_score 헬퍼
  - `tests/screening/test_short_swing_screener.py` — 9케이스 (MA20/MA60/눌림률/거래대금/과열/최소가격/점수정렬/멱등)
  - `docs/design/SHORT_SWING_STRATEGY_DESIGN.md` — 루트에서 이동
- **결정**: reason_json 컬럼은 PostgreSQL JSONB 대신 generic JSON 사용 (SQLite 테스트 호환). 시장지수 MA20 비교는 데이터 미확보로 0점 고정 + TODO 주석.
- **검증**: ruff check/format clean, pytest tests/screening 9 passed, pytest 전체 1950 passed + 2 xpassed (회귀 0).
