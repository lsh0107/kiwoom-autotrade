# 세션 로그 — 2026-03-09

### 21:11 - [lead/backend/frontend/qa] 토큰 DB 캐시 + 에러 핸들링 수정
- **수행**: 조회 버튼 연타 시 BrokerAuthError 발생하는 근본 원인(레이스 컨디션) 수정
- **파일**:
  - `src/broker/token_store.py` (신규): asyncio.Lock + Double-Check Locking 토큰 캐시
  - `alembic/versions/002_broker_token_cache.py` (신규): 토큰 캐시 컬럼 마이그레이션
  - `src/models/broker.py`: cached_token 등 4개 컬럼 추가 (cached_token 암호화 저장)
  - `src/broker/kiwoom.py`: _ensure_token DB 캐시 경로 추가 (하위 호환 유지)
  - `src/api/v1/market|account|orders.py`: db 파라미터 전달
  - `src/utils/exceptions.py`: CredentialNotFoundError(NO_CREDENTIALS) 추가
  - `src/api/deps.py`: NotFoundError → CredentialNotFoundError
  - `frontend/.../trade/page.tsx`: Enter 키 가드 + NO_CREDENTIALS 에러 처리
  - `tests/broker/test_token_store.py` (신규): 동시성 포함 5개 테스트
- **결정**: 토큰 공유를 DB 캐시로 해결 (인메모리 캐시 X, 멀티프로세스 고려)
- **결과**: pytest 308 통과, 커버리지 86%, pre-commit 전체 통과, PR #52 Actions 통과
- **TODO**: PR #52 dev 머지 후 alembic upgrade head 실행 필요

### 10:00 - [lead] 키움 모의투자 라이브 테스트 실행
- **수행**: 장 시작 시 전체 API 테스트 (토큰, 현재가, 호가, 잔고, 주문+취소)
- **결과**: 2/8 통과 — 파싱 버그 다수 발견
- **발견된 버그**:
  - 종목코드 형식: `KRX:005930` → `005930` (6자리만)
  - 가격 부호 접두사: `-173500` → `abs()` 필요
  - `ka10085` body 필드 누락: `stex_tp='0'` 필요
  - `kt00005` 모의 미지원 → `kt00001` + `qry_tp='0'` 로 교체
  - 호가 필드명 오류: `sel_1st_pre_bid` → `sel_fpr_bid` (1st), `sel_2th_pre_bid` (2nd+)

### 13:30 - [lead] 브로커 API 파싱 버그 수정
- **수행**: kiwoom.py, schemas.py, constants.py, tests/broker/test_kiwoom.py 수정
- **파일**:
  - `src/broker/schemas.py`: `to_kiwoom_symbol` 6자리 반환으로 변경
  - `src/broker/constants.py`: `deposit` → `kt00001`, `ORDINAL_SUFFIXES` 제거
  - `src/broker/kiwoom.py`: `_safe_price()` 추가, get_quote/get_orderbook/get_balance 수정
  - `tests/broker/test_kiwoom.py`: 4곳 종목코드, 호가/잔고 mock 데이터 업데이트
  - `scripts/test_kiwoom_live.py`: KiwoomClient 기반 전면 재작성
- **결정**: 가격 abs() 처리는 `_safe_price()` 헬퍼로 캡슐화

### 19:20 - [lead] Settings UX 개선 + 배포
- **수행**:
  - `src/api/v1/settings.py`: BrokerCredentialResponse에 created_at 추가, 새 등록 시 기존 활성 자격증명 비활성화
  - `frontend/src/app/(authenticated)/settings/page.tsx`: 삭제 버튼(Trash2) 추가, 비활성 배지 표시
  - `frontend/src/types/api.ts`: BrokerCredential 타입에 is_active, broker_name, masked 필드 추가
- **파일**: 위 3파일
- **결정**: 중복 등록 방지를 위해 새 자격증명 등록 시 기존 is_active=True 자격증명 전부 비활성화
- **배포**: PR #48 (feat/broker-api-fix → dev) squash merge, PR #49 (dev → main) merge commit 완료
- **CI**: Security Scanning, CodeQL, Python SAST, Bandit 전부 통과
- **테스트**: 303개 통과, 커버리지 86%

### 19:50 - [lead] 에러 처리 일관성 개선
- **수행**: rate limit 등 에러 타입별 사용자 안내 메시지 개선
- **파일**:
  - `src/api/deps.py`: HTTPException(404) → NotFoundError로 통일
  - `src/main.py`: HTTPException 핸들러 추가 → {error, message} 포맷 표준화
  - `frontend/src/app/(authenticated)/dashboard/page.tsx`: 에러 타입별 분기 (no_credentials/rate_limit/broker_auth/unknown)
  - `frontend/src/app/(authenticated)/trade/page.tsx`: rate limit·인증오류·404 별 토스트 메시지 개선
  - `tests/api/test_deps.py`: NotFoundError 기대값 업데이트
  - `tests/api/test_account.py`: error 포맷 기대값 업데이트
- **배포**: PR #50 (feat/error-handling → dev) → PR #51 (dev → main) 완료
- **테스트**: 303개 통과, 커버리지 86%
- **이슈**: rate limit 에러 재시도 로직 없음 — 향후 Phase 2에서 처리 예정
- **다음**: Phase 2 — WebSocket 실시간 시세 구현, APScheduler 장 시간 관리

### 20:52 - [frontend] 트레이딩 페이지 에러 핸들링 수정
- **수행**: Enter 키 중복 요청 가드 추가, NO_CREDENTIALS 에러 처리 추가
- **파일**: frontend/src/app/(authenticated)/trade/page.tsx
- **결정**: searchLoading 상태로 Enter 키 연타 방지, NO_CREDENTIALS를 catch 블록 최상단에 배치

### 20:56 - [backend] 토큰 캐시 구현 (DB + Double-Check Locking)
- **수행**: 키움 API 토큰 DB 캐시 + asyncio.Lock 동시 발급 방지 구현
- **파일**:
  - `src/models/broker.py`: cached_token, token_expires_at, token_type, token_updated_at 컬럼 추가
  - `alembic/versions/002_broker_token_cache.py`: 마이그레이션 신규 생성
  - `src/broker/token_store.py`: 신규 — load/save/get_or_refresh_token (Double-Check Locking)
  - `src/broker/kiwoom.py`: __init__에 db/credential_id 파라미터, _ensure_token에 DB 캐시 경로 추가
  - `src/api/v1/market.py`: _create_kiwoom_client에 db 전달, 엔드포인트에 DBSession 추가
  - `src/api/v1/account.py`: 동일
  - `src/api/v1/orders.py`: 동일
  - `src/utils/exceptions.py`: CredentialNotFoundError 추가
  - `src/api/deps.py`: NotFoundError → CredentialNotFoundError 변경
  - `src/main.py`: NO_CREDENTIALS → 422 매핑 추가
- **결정**: Double-Check Locking 패턴으로 credential_id별 asyncio.Lock 사용, 하위 호환 유지 (db=None이면 인메모리)

### 21:08 - [qa] 태스크 #6/#7 완료
- **수행**: token_store 테스트 5개 작성 + 회귀 테스트 4건 수정 + pre-commit + pytest 전체 검증
- **파일**:
  - `tests/broker/test_token_store.py` (신규): load/save/concurrent 테스트 5개
  - `tests/api/test_deps.py`: NotFoundError → CredentialNotFoundError 기대값 수정
  - `tests/api/test_market.py`: 404 → 422 (NO_CREDENTIALS) 기대값 수정
  - `tests/api/test_account.py`: 404 → 422 (NO_CREDENTIALS) 기대값 수정
- **결정**: 커버리지 86%
- **TODO**: 없음
