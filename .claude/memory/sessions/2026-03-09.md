# 세션 로그 — 2026-03-09

### 21:11 - [lead/backend/frontend/qa] 토큰 DB 캐시 + 에러 핸들링 수정
- **수행**: 조회 버튼 연타 시 BrokerAuthError 발생하는 근본 원인(레이스 컨디션) 수정
- **파일**:
  - `src/broker/token_store.py` (신규): asyncio.Lock + Double-Check Locking 토큰 캐시
  - `alembic/versions/002_broker_token_cache.py` (신규): 토큰 캐시 컬럼 마이그레이션
  - `src/models/broker.py`: cached_token 등 4개 컬럼 추가 (cached_token 암호화 저장)
  - `src/broker/kiwoom.py`: _ensure_token DB 캐시 경로 추가 (하위 호환 유지)
  - `src/api/v1/market|account|orders.py`: db 파라미터 전달
  - `src/utils/exceptions.py`: CredentialNotFoundError(NO_CREDENTIALS) 추가
  - `src/api/deps.py`: NotFoundError → CredentialNotFoundError
  - `frontend/.../trade/page.tsx`: Enter 키 가드 + NO_CREDENTIALS 에러 처리
  - `tests/broker/test_token_store.py` (신규): 동시성 포함 5개 테스트
- **결정**: 토큰 공유를 DB 캐시로 해결 (인메모리 캐시 X, 멀티프로세스 고려)
- **결과**: pytest 308 통과, 커버리지 86%, pre-commit 전체 통과, PR #52 Actions 통과
- **TODO**: PR #52 dev 머지 후 alembic upgrade head 실행 필요

### 10:00 - [lead] 키움 모의투자 라이브 테스트 실행
- **수행**: 장 시작 시 전체 API 테스트 (토큰, 현재가, 호가, 잔고, 주문+취소)
- **결과**: 2/8 통과 — 파싱 버그 다수 발견
- **발견된 버그**:
  - 종목코드 형식: `KRX:005930` → `005930` (6자리만)
  - 가격 부호 접두사: `-173500` → `abs()` 필요
  - `ka10085` body 필드 누락: `stex_tp='0'` 필요
  - `kt00005` 모의 미지원 → `kt00001` + `qry_tp='0'` 로 교체
  - 호가 필드명 오류: `sel_1st_pre_bid` → `sel_fpr_bid` (1st), `sel_2th_pre_bid` (2nd+)

### 13:30 - [lead] 브로커 API 파싱 버그 수정
- **수행**: kiwoom.py, schemas.py, constants.py, tests/broker/test_kiwoom.py 수정
- **파일**:
  - `src/broker/schemas.py`: `to_kiwoom_symbol` 6자리 반환으로 변경
  - `src/broker/constants.py`: `deposit` → `kt00001`, `ORDINAL_SUFFIXES` 제거
  - `src/broker/kiwoom.py`: `_safe_price()` 추가, get_quote/get_orderbook/get_balance 수정
  - `tests/broker/test_kiwoom.py`: 4곳 종목코드, 호가/잔고 mock 데이터 업데이트
  - `scripts/test_kiwoom_live.py`: KiwoomClient 기반 전면 재작성
- **결정**: 가격 abs() 처리는 `_safe_price()` 헬퍼로 캡슐화

### 19:20 - [lead] Settings UX 개선 + 배포
- **수행**:
  - `src/api/v1/settings.py`: BrokerCredentialResponse에 created_at 추가, 새 등록 시 기존 활성 자격증명 비활성화
  - `frontend/src/app/(authenticated)/settings/page.tsx`: 삭제 버튼(Trash2) 추가, 비활성 배지 표시
  - `frontend/src/types/api.ts`: BrokerCredential 타입에 is_active, broker_name, masked 필드 추가
- **파일**: 위 3파일
- **결정**: 중복 등록 방지를 위해 새 자격증명 등록 시 기존 is_active=True 자격증명 전부 비활성화
- **배포**: PR #48 (feat/broker-api-fix → dev) squash merge, PR #49 (dev → main) merge commit 완료
- **CI**: Security Scanning, CodeQL, Python SAST, Bandit 전부 통과
- **테스트**: 303개 통과, 커버리지 86%

### 19:50 - [lead] 에러 처리 일관성 개선
- **수행**: rate limit 등 에러 타입별 사용자 안내 메시지 개선
- **파일**:
  - `src/api/deps.py`: HTTPException(404) → NotFoundError로 통일
  - `src/main.py`: HTTPException 핸들러 추가 → {error, message} 포맷 표준화
  - `frontend/src/app/(authenticated)/dashboard/page.tsx`: 에러 타입별 분기 (no_credentials/rate_limit/broker_auth/unknown)
  - `frontend/src/app/(authenticated)/trade/page.tsx`: rate limit·인증오류·404 별 토스트 메시지 개선
  - `tests/api/test_deps.py`: NotFoundError 기대값 업데이트
  - `tests/api/test_account.py`: error 포맷 기대값 업데이트
- **배포**: PR #50 (feat/error-handling → dev) → PR #51 (dev → main) 완료
- **테스트**: 303개 통과, 커버리지 86%
- **이슈**: rate limit 에러 재시도 로직 없음 — 향후 Phase 2에서 처리 예정
- **다음**: Phase 2 — WebSocket 실시간 시세 구현, APScheduler 장 시간 관리

### 20:52 - [frontend] 트레이딩 페이지 에러 핸들링 수정
- **수행**: Enter 키 중복 요청 가드 추가, NO_CREDENTIALS 에러 처리 추가
- **파일**: frontend/src/app/(authenticated)/trade/page.tsx
- **결정**: searchLoading 상태로 Enter 키 연타 방지, NO_CREDENTIALS를 catch 블록 최상단에 배치

### 20:56 - [backend] 토큰 캐시 구현 (DB + Double-Check Locking)
- **수행**: 키움 API 토큰 DB 캐시 + asyncio.Lock 동시 발급 방지 구현
- **파일**:
  - `src/models/broker.py`: cached_token, token_expires_at, token_type, token_updated_at 컬럼 추가
  - `alembic/versions/002_broker_token_cache.py`: 마이그레이션 신규 생성
  - `src/broker/token_store.py`: 신규 — load/save/get_or_refresh_token (Double-Check Locking)
  - `src/broker/kiwoom.py`: __init__에 db/credential_id 파라미터, _ensure_token에 DB 캐시 경로 추가
  - `src/api/v1/market.py`: _create_kiwoom_client에 db 전달, 엔드포인트에 DBSession 추가
  - `src/api/v1/account.py`: 동일
  - `src/api/v1/orders.py`: 동일
  - `src/utils/exceptions.py`: CredentialNotFoundError 추가
  - `src/api/deps.py`: NotFoundError → CredentialNotFoundError 변경
  - `src/main.py`: NO_CREDENTIALS → 422 매핑 추가
- **결정**: Double-Check Locking 패턴으로 credential_id별 asyncio.Lock 사용, 하위 호환 유지 (db=None이면 인메모리)

### 21:08 - [qa] 태스크 #6/#7 완료
- **수행**: token_store 테스트 5개 작성 + 회귀 테스트 4건 수정 + pre-commit + pytest 전체 검증
- **파일**:
  - `tests/broker/test_token_store.py` (신규): load/save/concurrent 테스트 5개
  - `tests/api/test_deps.py`: NotFoundError → CredentialNotFoundError 기대값 수정
  - `tests/api/test_market.py`: 404 → 422 (NO_CREDENTIALS) 기대값 수정
  - `tests/api/test_account.py`: 404 → 422 (NO_CREDENTIALS) 기대값 수정
- **결정**: 커버리지 86%
- **TODO**: 없음

### 21:40 - [docs] 전략 설계 문서 + 프로젝트 상태 업데이트
- **수행**: 모멘텀 돌파 전략 설계 문서 생성, project.md Phase 2 상태 업데이트, decisions-pending.md 미결정 4건 추가
- **파일**:
  - `.claude/memory/strategy-momentum.md` (신규): 전략 파라미터, 진입/청산 조건, 거래비용 정리
  - `.claude/memory/project.md`: Phase 2 #10 상태 🔶→🟡, 다음 세션 컨텍스트 업데이트
  - `.claude/memory/decisions-pending.md`: #11~14 미결정 항목 추가 (volume_ratio, 익절 방식, 종목 유니버스, LLM 트리거)
- **결정**: 백테스트 결과 후 파라미터 확정 (고정 파라미터: 오버나이트 금지, 손절 필수)
- **TODO**: 백테스트 완료 후 strategy-momentum.md 결과 테이블 갱신

---

## 세션 — 자동매매 전략 설계 + 백테스트 팀 구성

### 전략 결정 (대화 기반)

- **수행**: 자동매매 수익 목표 및 전략 방향 확정
- **결정 내용**:
  - 목표: 일 평균 0.5% (월 ~11.6%), 복리 아닌 평균 기준
  - LLM: 이벤트 드리븐 (연속 호출 X → 비용 감당 불가)
  - 트리거: 전일 고가 돌파 + 거래량 1.5배 이상 시 LLM 호출
  - 타임프레임: 5분봉 기반 당일 청산 (초단타 아님)
  - 손절: -0.5% 자동 (LLM 개입 없음, 절대 고정)
  - 익절: +1.0% 고정 (첫 버전)
  - 오버나이트: 금지
  - 아키텍처: 퀀트 메인(상시 감시) + LLM 보조(이벤트 발생 시)
  - 거래비용 왕복: ~0.21% → 그로스 0.71%+ 필요
- **주요 튜닝 파라미터** (백테스트 후 조정):
  1. volume_ratio (승률 결정, 1순위)
  2. stop_loss (MDD 제어, 2순위)
  3. take_profit 방식 (수익률, 3순위)

### 팀 구성 및 작업

- **수행**: backtest-strategy 팀 생성, Task #1~4 분배 및 실행
- **팀**: backend + docs + qa

### [backend] Task #1: 키움 분봉 API + 데이터 수집 모듈 — 완료
- **파일**:
  - `src/broker/schemas.py`: MinutePrice, DailyPrice Pydantic 모델 추가
  - `src/broker/constants.py`: ka10080(분봉), ka10081(일봉차트) API ID + `/api/dostk/chart` 엔드포인트
  - `src/broker/kiwoom.py`: `get_minute_price(symbol, interval, base_dt)`, `get_daily_chart(symbol, base_dt)` 추가
  - `src/backtest/__init__.py` (신규)
  - `src/backtest/data_fetcher.py` (신규): `fetch_daily_data`, `fetch_minute_data`, `fetch_minute_data_multi_day` (rate limit 대응 sleep 포함)

### [backend] Task #2: 백테스트 엔진 + 모멘텀 돌파 전략 — 완료
- **파일**:
  - `src/backtest/strategy.py` (신규): MomentumParams, check_entry_signal, check_exit_signal, calc_trade_pnl
  - `src/backtest/engine.py` (신규): BacktestEngine.run(), run_with_symbol()
  - `src/backtest/metrics.py` (신규): calc_metrics (승률, MDD, 샤프비율, 프로핏팩터, 월평균수익률)
  - `src/backtest/result.py` (신규): TradeRecord, BacktestResult
  - `tests/backtest/test_strategy.py` (16개 테스트)
  - `tests/backtest/test_metrics.py` (13개 테스트)
  - `tests/backtest/test_engine.py` (14개 테스트)
  - `tests/backtest/test_data_fetcher.py` (6개 테스트)
- **결과**: 전체 364 pass, 커버리지 86%, pre-commit 통과
- **커밋**: `9099530` — `feat(backtest): 백테스트 엔진 + 모멘텀 돌파 전략 + 데이터 수집 모듈 구현`
- **브랜치**: `feat/backtest-engine`

### 21:57 - [qa] 백테스트 엔진 코드 리뷰 + 엣지케이스 테스트 추가
- **수행**: src/backtest/ 전체 코드 리뷰 + 15개 엣지케이스 테스트 추가
- **파일**:
  - `tests/backtest/test_strategy.py`: 5개 추가 (stop_loss 경계값, take_profit 우선순위, 음수 진입가, 시간 추출 fallthrough)
  - `tests/backtest/test_engine.py`: 4개 추가 (run_with_symbol 빈 데이터/강제청산, 진입-청산-재진입 사이클, max_positions 초과 거부)
  - `tests/backtest/test_metrics.py`: 3개 추가 (MDD 정확한 수치, 같은 날 월평균, 거래비용>수익)
  - `tests/backtest/test_data_fetcher.py`: 3개 추가 (fetch_minute_data_multi_day 멀티/빈/단일)
- **코드 리뷰 결과**:
  - 손절 -0.5%: `pnl_pct <= -0.005` 정확 동작 ✓
  - 거래비용 왕복 0.21%: `0.00015*2 + 0.0018 = 0.0021` 정확 ✓
  - MDD: 누적 자산곡선 피크 대비 최대 낙폭 정확 ✓
  - 14:30 강제 청산: HHMM 문자열 비교 정확 ✓
  - type hint 전부 있음 ✓, 보안 이슈 없음 ✓
- **결과**: 71개 테스트 전체 통과, backtest 모듈 커버리지 99%+ (engine/strategy/data_fetcher/result 100%), pre-commit 전체 통과
- **TODO**: 없음

### Task 최종 현황
| Task | 내용 | 담당 | 상태 |
|------|------|------|------|
| #1 | 키움 분봉 API + 데이터 수집 | backend | ✅ 완료 |
| #2 | 백테스트 엔진 + 전략 구현 | backend | ✅ 완료 |
| #3 | 전략 설계 문서화 | docs | ✅ 완료 |
| #4 | 백테스트 QA | qa | ✅ 완료 |

---

## 다음 세션 시작 순서

1. **PR #52 머지**: feat/token-cache-fix → dev (`gh pr merge 52 --squash`)
2. **feat/backtest-engine → dev PR 생성**: `gh pr create --base dev`
3. **Task #4 QA 투입**: backtest-strategy 팀 qa 에이전트 (tests/backtest/ 검증)
4. **백테스트 실행**: 키움 모의투자 실데이터로 전략 성과 측정
5. **결과 분석 + 문서 갱신**: strategy-momentum.md 결과 테이블, decisions-pending.md #11~14 확정

## 참조 문서
- 전략 설계: `.claude/memory/strategy-momentum.md`
- 미결정: `.claude/memory/decisions-pending.md` (#11~14)
- 팀: `~/.claude/teams/backtest-strategy/config.json`

---

## 세션 2 — 백테스트 실행 + API 디버깅

### PR 머지 + 브랜치 정리
- **수행**: PR #52 (feat/token-cache-fix → dev) squash 머지, feat/backtest-engine → dev PR #53 생성
- **이슈**: PR #52 머지 후 dev 변경으로 feat/backtest-engine 충돌 발생
  - 충돌 파일: `kiwoom.py`, `project.md`, `sessions/2026-03-09.md`
  - 해결: dev merge + 충돌 수동 해결
- **결과**: PR #53 Actions 3/3 통과, squash 머지 완료, claude 브랜치 싱크

### QA 투입 + 완료
- **수행**: qa 에이전트 투입 → 엣지케이스 테스트 22건 추가
- **결과**: 379개 테스트 통과, 커버리지 87%
- **이슈**: qa 에이전트 429 rate limit 맞고 종료 → lead가 커밋 마무리

### 백테스트 스크립트 작성 + API 디버깅
- **수행**: `scripts/run_backtest.py` 생성 후 실데이터 백테스트 시도
- **파일**: `scripts/run_backtest.py` (신규)
- **발견된 문제 + 해결**:
  1. **ka10081 미지원**: 모의투자에서 차트 API(ka10081) 빈 응답 → ka10086(일별주가) 사용으로 전환
  2. **qry_dt 필수**: ka10086은 qry_dt가 필수 파라미터 (빈 문자열 불가)
  3. **429 Rate Limit**: 모의투자 초당 5건, AsyncLimiter 버스트 허용 → 연속조회 페널티
     - 해결: 요청 간 0.5초 대기 + 429 시 3초 재시도
  4. **일봉 필드명**: `close_pric` (부호 접두사 포함 문자열) → `_safe_int()` 파싱
  5. **분봉 cntr_tm 형식**: HHMMSS만 반환 (날짜 없음) → 날짜 prefix 수동 부착
  6. **일봉 페이징**: 1회 요청 당 20개만 반환 → 13회 연속 조회로 260거래일 수집

### 백테스트 결과 (삼성전자 005930)
- **일봉**: 260개 (20250228~20260309, 52주 커버)
- **분봉**: 226개 (3거래일)
- **거래 0건**: 삼성전자가 전략 조건 미충족
  - 52주 최고가: 223,000원
  - 최근 종가: 173,500원 (77.8%) — 95% 기준 211,850원에 미달
  - 거래량: 43M < 1.5배 기준 55M
- **엔진 검증**: threshold 75%로 낮추면 거래 발생 확인 → 엔진 자체는 정상 동작

### 다음 세션 할 일
1. **종목 스크리닝 기능 추가**: 52주 신고가 근처 + 거래량 급증 종목 자동 선별
2. **해당 종목으로 백테스트 재실행**: 실제 전략 조건(95%, 1.5배)으로 테스트
3. **결과 분석**: strategy-momentum.md 결과 테이블 갱신
4. **decisions-pending.md #11~14 확정**

### 기술적 발견사항 (다음 세션 참고)
- 키움 모의투자 차트 API(ka10080/ka10081)는 일부 제한 있음
- ka10086(일별주가)은 안정적으로 동작하나 20개/페이지 제한
- 분봉 API(ka10080)는 900개/요청, cntr_tm은 HHMMSS 형식 (날짜 없음)
- AsyncLimiter만으로는 키움 연속조회 페널티 방지 불가 → 명시적 sleep 필요
- `docs/backtest-results/`에 결과 JSON 저장됨
