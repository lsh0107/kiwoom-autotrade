# 세션 로그 2026-03-20

### 06:06 - [lead] Airflow 3.x 데이터 수집 파이프라인 전체 구동 (전날 이어서)

- **수행**:
  - Docker Desktop SIGSEGV 크래시 후 재시작, 이미지 재빌드
  - Dockerfile: `uv sync --group airflow` → `pip install` 전환 (Airflow 시스템 Python `/home/airflow/.local` 경로)
  - docker-compose Airflow 3.x 호환 전면 수정:
    - `airflow-dag-processor` 서비스 추가
    - `AIRFLOW__CORE__EXECUTION_API_SERVER_URL=http://airflow-webserver:8080/execution/` 추가
    - `AIRFLOW__API_AUTH__JWT_SECRET` 공유 설정 (컨테이너별 랜덤 생성 문제 해결)
    - `AIRFLOW_CONN_KIWOOM_DB` 추가 (`localhost` → `host.docker.internal`)
  - dart.py: `import opendartreader` → `import OpenDartReader` (모듈명 대소문자 불일치), `AirflowRuntimeError` 폴백 처리
  - storage.py: `postgresql+asyncpg://` 접두사 변환 추가, `market_data` INSERT에 `id`(gen_random_uuid()), `collected_at` 추가
  - postmarket DAGs: Asset 기반 트리거 적용 (`param_adjustment`, `trade_review`)

- **파일**:
  - `airflow/Dockerfile`
  - `airflow/docker-compose.yml`
  - `airflow/plugins/collectors/dart.py`
  - `airflow/plugins/collectors/storage.py`
  - `airflow/dags/postmarket/param_adjustment.py`
  - `airflow/dags/postmarket/trade_review.py`

- **결정**:
  - Airflow 3.x LocalExecutor는 webserver Execution API와 HTTP 통신 필요 → `EXECUTION_API_SERVER_URL` 필수
  - JWT secret은 각 컨테이너가 독립 생성 → docker-compose에 공유값 명시 필수
  - Docker Mac 로컬 개발: `localhost` → `host.docker.internal` 변환

- **커밋**: `e5f638f`

- **결과**:
  - `premarket_data_collection` DAG 전체 태스크 success
  - DB `market_data` 테이블: `dart_disclosure`, `fred_macro`, `overseas_index` 2026-03-20 저장 확인

- **TODO**:
  - `.env`에 `AIRFLOW_JWT_SECRET` 추가 (현재 docker-compose 기본값 사용 중)
  - SimpleAuthManager 자동 생성 비밀번호 → `.env` 연동 확인
  - 나머지 full-review 미완료 항목: B-C02/C03/C04, F-C01/C02/C03

### 06:13 - [frontend] F-C01/C02/C03 CRITICAL 수정

- **수행**:
  - F-C01: ApiClient 내부 캐시 확인 → 이미 단순 fetch 래퍼, skipCache 미사용. 수정 불필요
  - F-C02: `useRealtime` symbols 배열 비교 안정화 — `useMemo`로 중복 제거+정렬, `symbolsKey` 문자열로 useEffect 의존성 안정화
  - F-C03: 미들웨어 JWT 검증 추가 — `jose` 라이브러리로 서명/만료 검증, `JWT_SECRET_KEY` 미설정 시 fallback, 만료 토큰 쿠키 제거
- **파일**:
  - `frontend/src/lib/api.ts` (변경 없음 — 이미 정상)
  - `frontend/src/hooks/use-realtime.ts`
  - `frontend/src/middleware.ts`
  - `frontend/package.json` (jose 직접 의존성 추가)
- **결정**:
  - F-C01은 현재 코드에 내부 캐시/skipCache가 없어 수정 불필요
  - jose는 Edge Runtime 호환 — Next.js 미들웨어에서 안전 사용
  - `JWT_SECRET_KEY` 미설정 시 토큰 존재만 확인하는 fallback 유지 (로컬 개발 편의)
- **검증**: `npx tsc --noEmit` 에러 0건

### 06:15 - [backend] B-C02/C03/C04 CRITICAL 안전장치 수정

- **수행**:
  - B-C02: `order_service.py` — `current_invested` DB 쿼리에서 문자열 리터럴(`"submitted"` 등)을 `OrderStatus` enum(`OrderStatus.SUBMITTED` 등)으로 교체
  - B-C03: `kill_switch.py` — `_save_states` atomic write 적용 (임시 파일 → `Path.replace`로 rename, 크래시 시 파일 손상 방지)
  - B-C04: `drawdown_guard.py` — 인메모리 `_user_states`를 파일 기반 영속화로 전환 (`data/.drawdown_state.json`). `_state_to_dict`/`_dict_to_state` 직렬화, `_load_drawdown_states`/`_save_drawdown_states` 추가. 상태 변경 함수마다 `_save_drawdown_states()` 호출. 날짜 다르면 일간 리셋, 주 다르면 주간 리셋
- **파일**:
  - `src/trading/order_service.py`
  - `src/trading/kill_switch.py`
  - `src/trading/drawdown_guard.py`
  - `tests/trading/test_drawdown_guard.py` (영속화 테스트 5건 추가)
- **결정**:
  - atomic write: `tempfile.mkstemp` → `Path.replace` 패턴 (kill_switch, drawdown_guard 양쪽 동일)
  - 파일 로드 실패 시 빈 dict 반환 (안전한 기본값)
- **검증**: 63 tests passed, ruff 에러 0건. drawdown_guard 커버리지 85→91%

### 07:XX - [lead] QA + PR 머지 (full-review 1·2순위 전체 완료)

- **수행**:
  - QA: pytest 969 passed (커버리지 91.21%), tsc --noEmit 0건, pre-commit 14개 훅 통과
  - feat/fullreview-critical-b-f → dev PR #183 Actions 전체 pass → squash 머지
  - dev → main PR #184 Actions 전체 pass → merge commit 머지
  - claude 브랜치 main 싱크 완료 (`git merge origin/main`)
- **결정**:
  - F-C01 수정 불필요 (현재 코드에 내부 캐시 없음)
- **커밋**: `0351fbe` (PR #183 squash), `f4e9ff9` (PR #184 merge)
- **결과**: full-review CRITICAL 1순위(B-C02/C03/C04) + 2순위(F-C01/C02/C03) 전체 완료 및 main 배포
- **TODO**:
  - `.env`에 `AIRFLOW_JWT_SECRET` 추가 (현재 docker-compose 기본값 사용 중)
  - 3-8: 텔레그램 양방향 통신 (미시작)
  - EKS 배포 (kiwoom-infra, 미시작)

### 07:05 - [lead] 시스템 전체 상태 점검 + news_collection 버그 수정

- **수행**:
  - 전체 시스템 상태 확인: 백엔드(8000) ✅, 프론트(3000) ✅, DB ✅, Airflow 5컨테이너 healthy ✅
  - `news_collection` DAG pause 상태 → unpause
  - `save_news_articles` 버그 발견 및 수정:
    - `source` 컬럼 (테이블 미존재) → 제거
    - 네이버 API 필드 매핑 오류: `link`→`url`, `pubDate`→`published_at`
    - `keyword`, `id`, `collected_at` 컬럼 누락 → 추가
  - 수정 후 재실행: news_articles 81건 저장 확인 (10종목 × ~8건)
- **파일**: `airflow/plugins/collectors/storage.py`
- **커밋**: `d448a3c`
- **decisions-pending.md**: #21 종목 DB 정규화 + 연관종목 모델링 추가
- **TODO (내일 장 전)**:
  - 웹 UI(http://localhost:3000)에서 봇 시작 버튼 눌러야 자동매매 재개
  - kill_switch 상태에 soft/hard_stopped 사용자 다수 존재 (이전 실행 이력 — 현재 사용자면 확인 필요)

### 07:41 - [arch-reviewer] architecture.md + design-001/002/003 vs 백엔드 코드 검토

- **수행**: ADR-001~024 전체 + design-001/002/003 문서를 src/ 코드와 대조 검토
- **파일**: `.claude/memory/architecture.md`, `.claude/memory/design-001-system-v1.md`
- **불일치 수정 내용**:
  1. architecture.md — 마지막 검토일 2026-03-14 → 2026-03-20, 검토 결과 요약 추가
  2. design-001 섹션 10 API 엔드포인트 전체 재작성:
     - `DELETE /orders/{id}` → `POST /orders/{id}/cancel` 변경 반영
     - `/market/search` 미구현, `/market/chart/{symbol}` → `/chart/{symbol}/daily` 변경 반영
     - `/bot/status` → `/bot/trading/status`, `/bot/logs` → `/bot/trading/logs` 변경 반영
     - 신규 엔드포인트 추가: `/bot/signals`, `/bot/trading/start`, `/bot/trading/stop`, `/bot/trade-history`, `/settings/broker DELETE`
     - admin `/users`, `/users/{id}` 미구현 표시
     - WebSocket `/ws/orders`, `/ws/bot` 미구현 표시
  3. design-001 섹션 6 DB 스키마 실제 구현 기준으로 수정:
     - users: `display_name` → `nickname`, `totp_secret` 제거, `updated_at` 추가
     - invites: `code` VARCHAR(32), `used_at`/`is_used` 추가
     - broker_credentials: 컬럼명 변경 (`app_key_enc` → `encrypted_app_key` 등), `is_mock_trading` → `is_mock`
     - orders: side Enum 소문자, `kiwoom_order_no` → `broker_order_no`, 상태 목록 수정
- **ADR 코드 일치 확인**:
  - ADR-007 JWT httpOnly cookie ✅, ADR-013 Fernet 암호화 ✅
  - ADR-012 LLM Phase 1 승격 ✅ (src/ai/ 구현)
  - design-003 리스크 파라미터: DRAWDOWN_STOP_BUY -2%, FORCE_CLOSE -3% ✅
  - backtest/strategy.py tax_rate 0.0020 ✅, force_close_time "15:15" ✅
- **TODO**: design-001의 신규 엔드포인트(/universe/) 및 Phase 2+ 계획 업데이트 필요

### 07:41 - [airflow-reviewer] design-005-data-pipeline.md 코드-문서 일치 검토 및 수정

- **수행**: design-005 문서와 실제 Airflow 코드 전체 비교, 불일치 수정
- **파일**: `.claude/memory/design-005-data-pipeline.md`
- **불일치 수정 내용**:
  1. 크론 표현식 전면 수정 — 문서 KST 기준 → UTC 기준으로 정정
     - premarket: `0 8 * * 1-5` → `0 23 * * 0-4`
     - postmarket_trade_review: `30 15 * * 1-5` → `30 6 * * 1-5`
     - macro_weekly: `0 8 * * 1` → `0 23 * * 0`
     - news_collection: `0 */2 9-15 * * 1-5` (잘못된 6필드) → `0 0,2,4,6 * * 1-5`
  2. DAG 코드 패턴 예시 수정 — `from airflow.decorators import dag, task` → `from airflow.sdk import Asset, dag, task`, `days_ago(1)` → 고정 날짜 `datetime(2026, 1, 1, tzinfo=UTC)`, `store` 함수 시그니처 실제 코드 반영
  3. docker-compose 설명 수정 — 단일 서비스 → 멀티서비스(webserver/scheduler/triggerer/dag-processor/postgres) 구조 반영
  4. DB 테이블 목록 추가 — `news_articles`에 `sentiment_score` 컬럼 추가, `strategy_config_suggestions`/`stock_universe`/`monthly_signals` 테이블 추가
  5. DAG 2 (llm_briefing) 플로우 수정 — 실제 태스크명 반영, `adjust_entry_weights` 별도 태스크 없음 확인
  6. DAG 4 (postmarket_trade_review) 플로우 수정 — Asset 발행 추가
  7. DAG 5 (macro_weekly) 수정 — `fetch_fred_weekly` 태스크 없음, ECOS만 수집
  8. DAG 6 (monthly_rebalance) 신규 추가 — 문서에 미기재된 DAG
  9. 프로젝트 구조 수정 — `dags/monthly/` 디렉토리 추가
  10. 구현 순서에 3-M (monthly_rebalance) 추가
- **TODO**: 3-8 텔레그램 양방향 통신 미시작
