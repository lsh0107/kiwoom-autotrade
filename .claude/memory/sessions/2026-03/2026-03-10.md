# 세션 로그 — 2026-03-10

## 세션 4 — 프론트엔드 UX 개편 + 버그 수정

### 00:30 - [frontend] 대시보드 + 매매결과 + 사이드바 구현
- **수행**: shadcn dashboard-01 패턴으로 대시보드 전면 재작성, 매매결과 페이지 신규, 사이드바 개선
- **파일**:
  - `frontend/src/app/(authenticated)/dashboard/page.tsx`: SectionCards 패턴, @container 반응형, Skeleton 로딩, gradient 카드, 종목 아바타
  - `frontend/src/app/(authenticated)/bot/page.tsx`: 요약 카드 3개, StatusBadge 컴포넌트, Skeleton
  - `frontend/src/app/(authenticated)/results/page.tsx` (신규): 백테스트 결과 카드 + recharts 승률 차트
  - `frontend/src/components/app-sidebar.tsx`: 로고 블록, 메뉴/시스템 그룹 분리, tooltip
  - `frontend/src/types/api.ts`: ResultFile, BacktestResult 등 타입 추가
  - `frontend/package.json`: recharts 추가
  - `frontend/src/components/ui/chart.tsx, progress.tsx, scroll-area.tsx` (shadcn 추가)

### 00:45 - [backend] 매매결과 조회 API + live_trader 테스트 + 공휴일 체크
- **수행**: 결과 API, 테스트 59건, 공휴일 스킵
- **파일**:
  - `src/api/v1/results.py` (신규): GET /list, GET /{filename} (경로 조작 방지)
  - `tests/test_live_trader.py` (신규): 30건 mock 매매 로직 테스트
  - `scripts/korean_holidays.py` (신규): 2026년 15개 공휴일
  - `tests/test_korean_holidays.py` (신규): 29건 테스트
  - `scripts/cron_backtest.sh`: 공휴일 체크 연동

### 01:00 - [lead] 버그 수정 3건
- **수행**:
  1. **rate limit 근본 수정**: `src/api/v1/account.py` — credential별 10초 인메모리 캐시. get_balance가 토큰+잔고+예수금 3건 호출 × React 더블 마운트 = 6건 → 초당 5건 초과하던 문제 해결
  2. **존재하지 않는 종목 에러**: `src/broker/kiwoom.py` — stk_nm 비고 cur_prc 0이면 BrokerError 반환 (이전: 0으로 표시)
  3. **보유종목 없을 때**: dashboard — 손익/수익률 "—" + "보유종목 없음" 표시 (이전: ₩0)
  4. **프론트 GET 캐시**: `frontend/src/lib/api.ts` — 10초 TTL 캐시 (이중 방어)
- **파일**: `src/api/v1/account.py`, `src/broker/kiwoom.py`, `frontend/src/lib/api.ts`, `frontend/.../trade/page.tsx`

### PR #56
- **브랜치**: feat/frontend-dashboard → dev
- **커밋**: 8개
- **Actions**: 전체 통과
- **테스트**: 438개 통과, src 커버리지 86%

### 교훈
- 팀 에이전트(frontend/backend) 각자 작업 시 API 스펙 합의 없이 진행 → 연동 불일치 발생
- 다음부터 SendMessage로 프론트-백 API 스펙 먼저 합의 후 구현

---

## 세션 5 — 트레이딩/설정 UX 개선 + results API 테스트

### 02:06 - [backend] results API 테스트 작성
- **수행**: tests/api/test_results.py 신규 작성, 11개 테스트 전부 통과
- **파일**: `tests/api/test_results.py` (신규)
- **테스트**: list (4건) + get (7건) — 인증, 빈 디렉토리, 404, 경로 조작(.., /, \), 비-JSON
- **결정**: slash 경로 조작은 Starlette %2F 디코딩 이슈로 함수 직접 호출 방식 검증

### 02:06 - [frontend] 트레이딩 + 설정 페이지 UX 개선
- **수행**: 대시보드 수준으로 shadcn 패턴 통일
- **파일**:
  - `frontend/src/app/(authenticated)/trade/page.tsx`: gradient 현재가, 호가 바 차트, 매수/매도 색상, TradeSkeleton, @container/main
  - `frontend/src/app/(authenticated)/settings/page.tsx`: 아이콘 레이아웃, 활성/비활성 Badge, 모의(녹)/실거래(빨) 토글, 실거래 경고, SettingsSkeleton

### PR #58
- **브랜치**: feat/ux-and-tests → dev
- **커밋**: 1개
- **Actions**: 전부 통과, 머지 완료

---

## 세션 6 — 레이트 리밋 해결 + 스크리닝 조건 완화

### 10:40 - [lead] 429 레이트 리밋 근본 해결
- **수행**: kiwoom.py `_request()`에 429 자동 재시도 (지수 백오프 2→4→6초) 중앙 처리 추가
- **파일**:
  - `src/broker/kiwoom.py`: _request()에 retry 루프 추가, HTTP 429 + 키움 에러코드 1700 모두 대응
  - `src/broker/constants.py`: MOCK_RATE_LIMIT 5→4 (마진 확보)
- **결정**: 요청 간 230ms 고정 대기 대신 AsyncLimiter(4/s) + 429 시 백오프 조합
- **참고**: koapy 프로젝트, 한국투자증권 throttling 사례 리서치 반영

### 10:40 - [lead] 스크리닝 조건 완화 + 호출 간격 확대
- **수행**: 기존 조건이 너무 엄격해 30종목 전부 탈락 → 완화
- **파일**:
  - `scripts/screen_symbols.py`: threshold 0.90→0.75, volume_ratio 1.2→0.8, 페이지 간 0.5→0.8초, 종목 간 1→2초
  - `scripts/run_backtest.py`: 일봉 후 쿨다운 3→5초, 분봉 간 0.5→1.5초, 페이지 간 0.5→0.8초
  - `scripts/live_trader.py`: 종목 간 간격 0.3→0.5초
  - `scripts/cron_backtest.sh`: 조건 완화 반영 + 스크리닝↔백테스트 간 10초 쿨다운

### 10:50 - [lead] 전체 파이프라인 검증 성공
- **결과**: cron_backtest.sh 수동 실행 → 스크리닝(펄어비스 PASS) → 백테스트(완료) → 자동매매(실행 중)
- **테스트**: 449개 전부 통과, 커버리지 87%

### 11:00 - [lead] results 페이지 버그 수정
- **수행**: `r.results` undefined 에러 → optional chaining + nullish coalescing 적용
- **파일**: `frontend/src/app/(authenticated)/results/page.tsx`
