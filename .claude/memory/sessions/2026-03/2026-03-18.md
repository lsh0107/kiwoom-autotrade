# 세션 로그 2026-03-18

### 00:52 - [backend-c] Task #4 + #5 — Layer 2 파라미터 개선 + 레짐별 자본 배분

- **수행**:
  - `ATR_STOP_MULT` 1.5 → 1.2 (scripts/live_trader.py 상수)
  - argparse `--entry-start-time` (기본 09:30) + `--max-loss-pct` (기본 -2%) 추가
  - `TradingState.max_loss_pct` 필드 추가
  - 풀백 매수 조건: 52주 고점 대비 5% 이상 조정 시만 진입 허용 (poll_cycle + handle_tick 양쪽)
  - DEFENSIVE/CRISIS 레짐 모멘텀 신규 매수 중단 (poll_cycle + handle_tick)
  - 이중 안전망: `dyn_stop = max(atr_based_stop, state.max_loss_pct)` (ATR vs 고정 -2%)
  - `StrategyBudget.apply_regime()` 메서드 추가 (src/ai/signal/position_sizer.py)
  - `main()`에서 레짐 판단 후 `state.budget.apply_regime(regime, account_balance)` 호출
- **파일**:
  - `scripts/live_trader.py`
  - `src/ai/signal/position_sizer.py`
  - `tests/test_live_trader.py` (12개 새 테스트 + 6개 기존 테스트 high_52w 값 조정)
- **결정**:
  - `apply_regime` 내 `REGIME_ALLOCATION` import는 메서드 내 lazy import로 처리 (ruff unused-import 회피)
  - 풀백 조건 추가로 기존 진입 테스트의 `high_52w` 값을 현재가 대비 10% 이상으로 상향 조정
- **결과**: 84개 테스트 전체 통과, ruff/bandit 통과
- **TODO**: 없음 (Task #4, #5 완료)
