# 2026-03-07 세션 로그

### 16:09 - [backend] orders.py KiwoomClient 실제 주문 연동
- **수행**: create_order_endpoint에 KiwoomClient place_order 호출 추가
- **파일**: src/api/v1/orders.py
- **결정**:
  - ActiveBrokerCredential 의존성으로 credential.is_mock 사용 (settings.is_mock_trading 대체)
  - account.py와 동일한 _create_kiwoom_client 헬퍼 패턴 적용
  - OrderSide(lowercase) → OrderSideEnum(uppercase) 변환 처리
  - 브로커 오류 시 주문 레코드 유지 (PENDING 상태), 성공 시 submit_order로 SUBMITTED 전환
- **TODO**: QA 테스트 검증 필요

### 16:12 - [qa] test_orders.py 업데이트 및 전체 테스트 검증
- **수행**: orders.py의 ActiveBrokerCredential + KiwoomClient 연동에 맞춰 테스트 업데이트
- **파일**: tests/api/test_orders.py
- **결정**:
  - `_mock_kiwoom_client` 픽스처 추가 (place_order → BrokerOrderResponse, close → None)
  - 모든 주문 생성 테스트에 `broker_credential` 픽스처 + `_create_kiwoom_client` 패치 적용
  - 기대값 status `"created"` → `"submitted"` 변경 (submit_order 성공 시 SUBMITTED)
  - test_account.py 패턴과 동일한 구조 유지
- **결과**: 302 passed, 86% 커버리지
