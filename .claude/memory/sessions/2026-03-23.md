# 세션 로그 2026-03-23

### 21:10 - [lead] Airflow 로그인 문제 해결

- **수행**:
  - Airflow 3.x SimpleAuthManager가 환경변수 비밀번호를 무시하고 랜덤 생성하는 문제 발견
  - docker-compose.yml: init에서 .env의 AIRFLOW_ADMIN_PASSWORD로 비밀번호 파일 생성
  - airflow-auth named volume으로 init↔webserver 간 파일 공유
  - user: "0:0"으로 init 실행해 권한 문제 해결
- **파일**: `docker-compose.yml` (수정)
- **검증**: admin/changeme 로그인 201 성공

### 21:20 - [lead] DAG 전체 unpause

- **수행**: 8개 DAG 전부 활성화 (docker compose 재시작으로 paused 상태 됐음)

### 21:52 - [backend] llm_briefings/trade_reviews DB 모델 + 마이그레이션 추가

- **수행**: LLM 브리핑 및 장후 리뷰 결과를 DB에 저장하는 모델·마이그레이션·storage 함수 생성
- **파일**:
  - `src/models/llm_briefing.py` (신규): LLMBriefing 모델 (date unique, JSON 컬럼 포함)
  - `src/models/trade_review.py` (신규): TradeReview 모델 (date unique, JSON 컬럼 포함)
  - `alembic/versions/008_llm_results.py` (신규): llm_briefings + trade_reviews 테이블 마이그레이션
  - `src/models/__init__.py` (수정): LLMBriefing, TradeReview import + __all__ 알파벳순 추가
  - `airflow/plugins/collectors/storage.py` (수정): save_briefing(), save_trade_review() 추가

### 22:00 - [lead] DAG store 수정 + 마이그레이션 적용 + 검증

- **수행**:
  - llm_briefing DAG: save_json → save_briefing (DB+JSON)
  - trade_review DAG: save_json → save_trade_review (DB+JSON)
  - 007 마이그레이션 IMMUTABLE 인덱스 오류 수정 (created_at::date → signal_date 컬럼)
  - rebalance.py: ON CONFLICT (symbol, signal_date)로 변경
  - alembic 005~008 마이그레이션 적용 완료 (19개 테이블)
  - DB 저장 함수 직접 테스트: llm_briefings/trade_reviews INSERT+SELECT 성공
- **파일**:
  - `airflow/dags/premarket/llm_briefing.py` (수정)
  - `airflow/dags/postmarket/trade_review.py` (수정)
  - `alembic/versions/007_monthly_signals.py` (수정)
  - `airflow/dags/monthly/rebalance.py` (수정)
- **커밋**: `2b6087e` (모델+마이그레이션), `bf8a612` (DAG 수정), `8af35d6` (인덱스 fix)
- **DB 현황**: 19개 테이블, market_data 6건, news_articles 392건, strategy_config 10건

### 22:10 - [lead] Airflow DAG 테스트 실행

- **수행**: `docker compose exec airflow-webserver airflow dags test premarket_data_collection`
- **결과**: store 태스크 실패 — `RuntimeError: 시장 데이터 DB 저장 실패: category=dart_disclosure, date=20260323`
- **원인**: Airflow 컨테이너 → 호스트 PostgreSQL 연결 불가 (Docker→host DB connectivity 이슈)
  - `AIRFLOW_CONN_KIWOOM_DB`가 `localhost:5432`를 가리키지만 컨테이너 내부에서는 호스트 접근 불가
  - 별도 네트워크 설정 필요 (host.docker.internal 또는 docker-compose network 연결)
- **TODO**: Docker→host DB 연결 해결 (docker-compose 네트워크 설정 또는 DB도 컨테이너화)

### 22:20 - [lead] Airflow auth 커밋

- **커밋**: `6f013ee` — docker-compose.yml SimpleAuthManager 비밀번호 파일 기반 설정
- **내용**:
  - airflow-auth named volume 추가
  - init 서비스: root(0:0)로 실행, .env의 AIRFLOW_ADMIN_PASSWORD로 passwords.json 생성
  - AIRFLOW__CORE__SIMPLE_AUTH_MANAGER_PASSWORDS_FILE 환경변수 설정
  - gosu → `su -s /bin/bash airflow` 로 변경 (gosu 미설치 문제)

### 22:40 - [lead] LLM DB 컨텍스트 설계 (design-008)

- **배경**: 사용자 요청 — "LLM이 DB에 접근해서 장 마감 후/장 시작 전 데이터로 동적 투자결정 가능하게"
- **수행**:
  - PM·데이터엔지니어·백엔드·투자전문가 4개 관점으로 설계 토론 진행
  - (Plan 에이전트 3개 rate limit 걸려서 리드가 직접 종합 작성)
  - `.claude/memory/design-008-llm-db-context.md` 작성 완료
- **설계 핵심**:
  - **시간별 데이터 흐름**: 장중→장마감→야간→장전 4단계
  - **신규 DAG 2개**: overnight_index_collection (야간 해외지수 수집), overnight_analysis (LLM 야간 분석)
  - **DB 컨텍스트 빌더**: `plugins/context/` 모듈 — 토큰 예산(~15K) 내에서 DB 데이터 요약·압축
  - **llm_decisions 테이블**: LLM 의사결정 기록 + 피드백 루프
  - **야간 수집 대상**: yfinance 활용 (^GSPC, ^IXIC, ^VIX, ES=F, NQ=F, KRW=X, ^TNX)
  - **4단계 구현 계획**: Phase A(야간 수집) → B(DB 컨텍스트 빌더) → C(피드백 루프) → D(자율 파라미터 조정)
- **투자전문가 관점 포함**:
  - 야간 선물(ES=F, NQ=F)이 익일 KOSPI 갭에 직접 영향
  - VIX 20 이상 시 방어 모드, VIX 30+ 시 현금 비중 확대
  - 원달러 환율 급변 시 외인 매도 압력 경고
  - ADR 변동 해석 (삼성전자 등)
- **파일**: `.claude/memory/design-008-llm-db-context.md` (신규)
- **TODO**: 사용자 리뷰 후 Phase A 착수 결정

### 23:00 - [lead] 세션 마무리

- **미결정 사항 (3건)**:
  1. M-07: PortfolioManager 동시 접근 race condition → Lock 추가 여부
  2. M-10: sentiment_score 컬럼 추가 여부
  3. M-14: mypy pre-commit hook 추가 여부
- **PR #187**: feat/audit-fixes 브랜치, 아직 push 필요
- **다음 세션 TODO**:
  - Docker→host DB 연결 해결
  - design-008 사용자 리뷰 → Phase A 착수
  - 미결정 3건 사용자 판단 요청
  - PR #187 push + Actions 확인
