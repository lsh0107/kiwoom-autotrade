# 2026-05-18 세션 로그

## 오전 [lead] HOTFIX D — alembic 019 id 단축 + 상태머신 SUBMITTED 직접 전이 + partial fill 잔량 cancel

- **수행**: 사용자 리뷰 운영 차단 P0/P1 3건 해소.
- **PR**: #430 (feat/dev) → #431 (dev/main) → main `4cfbc1d` → claude `93cf738`.
- **결정**:
  - alembic revision id `019_short_swing_state_machine_user_scope` (40자) → `019_ss_state_user_scope` (22자). varchar(32) invariant 준수.
  - `OrderStatus.SUBMITTED → FILLED/PARTIAL_FILL/CANCELLED` 직접 전이 허용. 키움 WebSocket 체결 이벤트가 ACCEPTED 단계 없이 바로 떨어지는 케이스 / cancel API 응답 직접 CANCELLED 케이스 누락 해소.
  - `short_swing_cancel` PARTIAL_FILL 상태도 stale 잔량 cancel 대상에 포함.
- **검증**: focused regression `46 passed, 8 warnings`. Alembic `001 → 020` clean upgrade.

## 오전~정오 [lead] HOTFIX E — small-real 전환 게이트 4 항목 해소

- **수행**: 사용자 추가 게이트 4건 코드 해소.
- **PR**: #432 (feat/dev) → #433 (dev/main) → main `ce3dbdd` → claude `e1f81ed`.
- **파일**:
  - `src/trading/order_state.py` — `PARTIAL_FILL → PARTIAL_FILL` self-transition 추가.
  - `src/api/v1/realtime.py` — `on_order_exec` 누적 (`filled_quantity += event_qty`, `filled_price` 가중평균).
  - `src/trading/short_swing_reconciler.py` — OPEN 포지션 추가 체결 누적 단계 1-b (`open_qty_updated` counts). entry_price 갱신 전 OLD 값 캡처 후 stop/take_profit 비율 보존 재계산.
  - `tests/trading/test_order_state.py` — self-transition 케이스.
  - `tests/trading/test_short_swing_state_machine.py` — `TestOpenAdditionalFill` 2 케이스.
- **활성화 잠금 메모리** 갱신: 코드 잠금 해소, 운영 잠금(mock 1주 dry-run + 사용자 명시 OK) 유지.

## 정오 [lead] mock trading QA Pass Criteria 검증 (MOCK_TRADING_QA_FOR_CLAUDE.md)

- **결과**: 8/8 항목 통과.
  - Docker rebuild OK / Alembic `upgrade head` OK / 로그인 OK.
  - `/trade` 005930 1주 mock 주문 제출 가능.
  - `/bot` 오늘 매매 이력 row 정상 (주문가/주문수량/주문금액/체결수량/체결금액 컬럼 분리).
  - `/dashboard` 주문가능금액 음수 미표시.
  - `/api/v1/short-swing/positions?limit=5` HTTP 200.
  - `/api/v1/short-swing/status` HTTP 200 (`active_strategy=cross_momentum` 환경 → short_swing `enabled=false` 정상).
- **결정**: mock 1주 dry-run 운영 진행 가능. small-real 전환은 사용자 명시 OK 전 금지.

## 오후 [lead] **사고 + 수습** — docker-compose.override.yml 통째 덮어쓰기

- **사고**: short_swing 자동전략 QA 시도 중 `ACTIVE_STRATEGY=short_swing` 만 담긴 override.yml 을 `Write` 로 덮어써, 사용자 원본 (DEBUG/USE_LLM_DECISIONS/CROSS_MOMENTUM_REBALANCE_FREQ=weekly + volumes 마운트) 을 통째로 파괴.
- **수습**: `git checkout HEAD -- docker-compose.override.yml` 로 원본 복원 + backend 재기동. 현재 `git status` clean, `git diff HEAD` 비어있음 확인.
- **부분 검증 결과** (override.yml 일시 변경 동안 ACTIVE_STRATEGY=short_swing 으로 확보):
  - `/api/v1/strategy/current` short_swing 분기 OK.
  - `/api/v1/short-swing/status` `enabled=true` OK.
  - `run-entry-check?dry_run=true` → `outside_entry_window` OK.
  - `run-exit-check?dry_run=true` → `no_open_positions` OK.
  - `/api/v1/short-swing/screen` HTTP 200, `created=0` (daily_candles 2026-05-08 stale).
- **TODO**:
  - 사용자 환경 daily_candles 새로고침 (10일 stale).
  - short_swing 자동전략 운영 전환 시점에는 override.yml 을 사용자가 직접 편집 또는 `.env` 의 ACTIVE_STRATEGY 추가 방식으로 진행. 본 lead 가 override.yml 재작성 금지.
- **재발 방지 규칙**: 사용자 로컬 설정 파일(`docker-compose.override.yml`, `.env`)은 read-only 로 본다. 환경 변경은 사용자 직접 또는 명시 위임 후에만.

## 자가 점검 사이클 결과 (저녁)

- Active task: 없음.
- 미커밋 변경: 없음 (`git status` clean).
- 열린 PR: #399 (dependabot trufflehog), #307 (dependabot checkout). 모두 무관, 후속 처리 가능.
- 활성 short-swing 팀: 없음 (HOTFIX E 머지 후 모두 해제).
- 세션 로그: **본 entry 로 누락 보강 완료**. 작업 단위마다 즉시 append 규율 재확립.

## 저녁 [lead] HOTFIX F 팀 구성 — cross_momentum 주문 broker_order_no 정합성

- **사용자 P0 보고**: 브로커 계좌 27개 보유 vs DB orders SUBMITTED/fill_qty=0. 원인 7건 명시.
- **확인**: 코드에서 1:1 확인. `_place_buy_order/_place_sell_order` resp.order_no 미반환 + `_persist_rebalance` fake id 저장 + realtime.py 가 broker_order_no 매칭 → 체결 이벤트 drop.
- **추가 발견 (사용자 지목 7)**: `scripts/live_trader.py::_check_monthly_rebalance` line 1949 가 `is_last_business_day_of_month` 선행 return 으로 env weekly 무시. UI `_compute_next_rebalance_kst` 도 monthly hardcode.
- **팀**: `hotfix-f-cross-momentum-order-no` 생성. Task #1~5 분해 (F.1 broker_order_no/order_type, F.2 freq 단일진실원, F.3 UI weekly 분기, F.4 reconcile 스크립트, F.5 QA). 의존성 F.1→F.2→F.3 + F.1→F.4 + 전부→F.5.
- **현재**: backend agent F.1 worktree 투입 (background).

### 14:59 - [backend] HOTFIX F.1 — cross_momentum broker_order_no 실제 저장 + market order_type
- **수행**: P0 사용자 보고 4항 해소. 브로커 주문번호 실제 전달, fake id 제거, order_type=market 저장.
- **파일**:
  - `src/trading/cross_momentum_rebalance.py` — _place_buy_order 4-tuple, _place_sell_order (qty, order_no), _execute_buys/_execute_sells dict 에 order_no 포함, _persist_rebalance fake id 제거 + None 시 스킵
  - `src/trading/live_order_persist.py` — persist_order_submitted 에 order_type kwarg 추가 (backward compatible)
  - `tests/trading/test_cross_momentum_rebalance.py` — 7개 신규 케이스 + 기존 회귀 수정
  - `tests/trading/test_live_order_persist.py` — 2개 신규 케이스 (market type, default limit)
- **결정**: _persist_rebalance 에서 unused `today` 파라미터 제거 (fake id 생성 삭제에 따른 dead arg)
- **검증**: ruff check/format clean, 98/98 tests PASS
- **브랜치**: `feat/hotfix-f1-cross-momentum-broker-order-no` (5 commits)

### 15:36 - [backend] HOTFIX F.2 — _is_rebalance_trigger_date freq 단일진실원 + live_trader adapter DB params 주입
- **수행**: P0 사용자 지목 6, 7 항목 해소. rebalance_freq 단일 진실원 + weekly 정식 지원.
- **파일**:
  - `src/trading/cross_momentum_rebalance.py` — _is_rebalance_trigger_date(today, freq=None) 시그니처 확장, check_monthly_rebalance 가 adapter.params.rebalance_freq 전달, execute_monthly_rebalance weekly fallback 경고 제거
  - `scripts/live_trader.py` — _get_rebalance_adapter async 화 + DB load_rebalance_params 주입, _check_monthly_rebalance 월말 선행 return 제거
  - `tests/trading/test_cross_momentum_rebalance.py` — 9개 신규 케이스 (freq 파라미터 6 + check_monthly_rebalance weekly 3)
  - `tests/scripts/test_live_trader_rebalance.py` — 3개 신규 케이스 (adapter DB 주입 2 + 선행 return 제거 1)
- **결정**: freq 우선순위: 명시 인자 > env > "monthly" 기본값. DB params 우선.
- **검증**: ruff check/format clean, 88/88 PASS (cross_momentum + t2 + live_trader)
- **브랜치**: `feat/hotfix-f2-rebalance-freq-single-source` (3 commits)

### 15:51 - [backend] HOTFIX F.3 — UI _compute_next_rebalance_kst weekly 분기
- **수행**: _compute_next_rebalance_kst 에 freq 파라미터 추가. weekly 시 다음 금요일 영업일 반환.
- **파일**:
  - `src/api/v1/strategy.py` — _find_next_weekly_trigger 헬퍼, _compute_next_rebalance_kst(today, freq) 시그니처, _build_cross_momentum_detail 에서 params.rebalance_freq 전달
  - `tests/api/test_strategy_current.py` — 9개 신규 케이스 (monthly 3 + weekly 5 + _build 통합 1)
- **결정**: 금요일 휴장 시 직전 영업일(목요일) fallback. 최대 4주 탐색.
- **검증**: ruff check/format clean, 16/16 PASS
- **브랜치**: `feat/hotfix-f3-ui-next-rebalance-weekly` (2 commits + 세션 로그)

## $(date +%H:%M) [lead] HOTFIX F.1/F.4 PR 큐 + #434 머지

- **PR #434** (F.1 feat→dev, squash 머지 완료, SHA `69e7bfe`). Actions 7/7 PASS (pytest 7m41s 포함).
- **PR #435** (F.4 reconcile 스크립트, base=feat/hotfix-f1-cross-momentum-broker-order-no, stacked). #434 머지로 base 재타깃 필요 (dev). 충돌 가능 → F.4 commits만 cherry-pick 후 새 push 계획.
- **PR #436** (dev→main release F.1) 생성, Actions 대기.
- **F.2** backend 진행 중 (`feat/hotfix-f2-rebalance-freq-single-source`).
