### 13:32 - [lead] 통합 QA + 수동 주문 cap 제거
- **수행**: 수동 주문 금액 cap 제거 (`order_service.py`) + 검색·Top API 통합 회귀
- **파일**:
  - `src/trading/order_service.py` — strategy_id 없는 수동 주문은 max_amount/max_investment 1e18 (사실상 무제한). 가격제한폭·드로우다운·일일 100건은 유지
  - `tests/trading/test_order_service.py` — kill_switch_blocks 테스트를 manual_no_amount_cap + price_limit_still_enforced 로 재정의
- **검증**: pytest 1902 PASS / ruff clean / frontend build OK / backend restart 후 smoke (search "삼성"→5건, "005"→3건, top→[] (cache empty))
- **이슈**: `daily_screening_cache` 테이블 비어있어 `/api/v1/market/top` 항상 빈 응답. Airflow `daily_screening` DAG 적재 점검 또는 stocks/cross_momentum 결과 fallback 필요
- **TODO**: PR 만들기 + top fallback 데이터 소스 후속

### 13:23 - [frontend] /trade 한글 검색 자동완성 + Top 위젯
- **수행**: /trade 페이지에 한글/코드 통합 검색 자동완성 드롭다운과 오늘의 추천 종목 위젯 추가
- **파일**:
  - `frontend/src/lib/constants.ts` — SEARCH_STOCKS, TOP_STOCKS 경로·쿼리키 추가
  - `frontend/src/types/api.ts` — StockSearchResult, TopStockItem 타입 추가
  - `frontend/src/hooks/queries/use-search-stocks.ts` — 신규 (300ms 디바운스 지원)
  - `frontend/src/hooks/queries/use-top-stocks.ts` — 신규
  - `frontend/src/app/(authenticated)/trade/page.tsx` — 검색 UI 전면 개편 + Top 위젯
- **결정**:
  - 드롭다운 blur/click 레이스 → onMouseDown + e.preventDefault() 로 해결
  - 디바운스는 page.tsx 내 useEffect로 처리 (별도 hook 불필요)
  - Top 위젯은 searchSymbol 없는 초기 상태에만 표시 (빈 상태 대체)
- **TODO**: QA #3 검증 대기. backend API 스펙 확정 시 타입 정합 재확인

### 13:20 - [backend] /api/v1/market 검색·Top API
- **수행**: 한글 종목명 ILIKE + 종목코드 prefix LIKE 검색 엔드포인트, daily_screening_cache 기반 Top 종목 엔드포인트 추가
- **파일**: src/api/v1/market.py, tests/api/test_market.py
- **결정**: OR(name ilike '%q%', symbol like 'q%') 단일 쿼리로 검색. rank IS NOT NULL 조건은 non-nullable 컬럼이지만 명세대로 포함. 빈 q → 조기 빈 리스트 반환. SQLite ilike 호환 확인
- **TODO**: 없음 (프론트에서 API 소비)
