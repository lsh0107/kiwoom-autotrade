### 13:55 - [lead] /trade QA 이슈 3건 영구 수정
- **수행**: 로컬 QA에서 발견된 3건 영구 수정
  1. KiwoomWebSocket connect()/subscribe() race — `_ready_event` 추가, connect()는 ready 또는 task done 중 먼저 끝나는 쪽까지 대기, subscribe()는 ready event 대기 + connect 미호출 즉시 raise
  2. `get_daily_chart` `base_dt` 빈 값 → 키움 1511 오류. 빈 값이면 오늘 KST 자동 채움
  3. middleware `AUTH_ROUTES`/`matcher`에 `/strategy-config`, `/decisions` 누락 → 추가
- **파일**:
  - `src/broker/realtime.py` — ready_event + race-free connect + subscribe guard
  - `src/broker/kiwoom.py` — get_daily_chart base_dt 자동 채움
  - `frontend/src/middleware.ts` — AUTH_ROUTES + matcher 보강
  - `frontend/src/app/(authenticated)/trade/page.tsx` — 차트 에러/빈 데이터 상태 카드 추가
  - `tests/broker/test_realtime.py`, `tests/broker/test_kiwoom.py`, `tests/broker/test_ws_contract.py` — fixture/케이스 보강
- **검증**: pytest 1892 PASS / ruff clean / frontend build OK / curl `/api/v1/market/chart/005930/daily?days=10` 정상 응답
- **TODO**: PR 생성 + Actions 확인
