### 01:46 - [backend] WebSocket 상수/스키마/클라이언트 구현

- **수행**: Task #1 constants.py/schemas.py WebSocket 추가, Task #2 realtime.py KiwoomWebSocket 구현
- **파일**:
  - `src/broker/constants.py` — WS_ENDPOINT, WS_API_ID, WS_TRNM_REG/REMOVE/REAL, WS_DEFAULT_GRP 추가
  - `src/broker/schemas.py` — RealtimeTick 모델 추가
  - `src/broker/realtime.py` — KiwoomWebSocket 클래스 신규 생성
- **결정**: get_token callable 주입 방식으로 KiwoomClient와 느슨한 결합 유지
- **TODO**: Task #3 — test_realtime.py 테스트 작성

### 01:52 - [backend] 스펙 확장 — 재연결·다중종목·타입별콜백 추가

- **수행**: 새 태스크 스펙(backend 메시지) 반영하여 세 파일 확장
- **파일**:
  - `src/broker/constants.py` — REALTIME_TYPES dict, 재연결 상수(BASE/MAX_DELAY/MAX_RETRIES), ENDPOINTS["websocket"] 추가
  - `src/broker/schemas.py` — RealtimeSubscription, RealtimeOrderExec, RealtimeBalance 추가
  - `src/broker/realtime.py` — subscribe(list[str]), 재연결 루프(_run_loop), _replay_subscriptions, on_tick/on_order_exec/on_balance 콜백, run_until(), close() 전면 재작성
- **결정**: get_token callable 패턴 유지 (토큰 만료 자동 처리), api-id를 data_type으로 설정(타입별 상이)
- **TODO**: Task #3 test_realtime.py QA 담당

### 03:18 - [backend-trader] live_trader.py WebSocket 이벤트 기반 전환
- **수행**: Task #1 — scripts/live_trader.py WebSocket 모드 추가, 기존 폴링 유지
- **파일**:
  - `scripts/live_trader.py` — run_trading_loop_ws() 신규, --mode ws|polling 인자 추가, ws 실패 시 폴링 폴백
  - `tests/test_live_trader.py` — TestRunTradingLoopWs 클래스 5개 테스트 추가
- **결정**:
  - KiwoomWebSocket.on_tick = handle_tick 클로저 방식 (state, client 캡처)
  - 연결 수립 대기 루프 최대 10초 (100 * 0.1s), 실패 시 ConnectionError
  - 강제매수 로직은 asyncio.create_task(_force_buy_checker) 별도 태스크
  - finally 블록에서 task cancel + ws.close() 보장
- **검증**: 49개 전체 통과, ruff 에러 0개

### 03:20 - [backend-api] FastAPI WebSocket 엔드포인트 구현
- **수행**: Task #2 — FastAPI WebSocket 엔드포인트(/api/v1/ws/market) 신규 구현
- **파일**:
  - `src/api/v1/realtime.py` — market_websocket 엔드포인트 신규 (JWT 인증, 구독/해지, on_tick 브릿지)
  - `src/api/v1/router.py` — realtime_router 등록
  - `tests/api/test_realtime.py` — 9개 WebSocket 테스트 (starlette.testclient)
- **결정**:
  - JWT는 쿠키에서 직접 읽기 (websocket.cookies), Depends(get_db)로 DB 세션 주입
  - 인증 실패: close(4001), 자격증명 없음: error JSON + close(4002)
  - KiwoomWebSocket.on_tick 콜백으로 키움→클라이언트 브릿지, finally에서 close() 보장
  - 테스트: starlette TestClient 동기 방식, KiwoomWebSocket/KiwoomClient/decrypt 모킹
- **검증**: 9/9 통과, ruff 에러 0개

### 03:30 - [frontend] 프론트엔드 실시간 UI (Task #3)
- **수행**: WebSocket 실시간 시세 훅 신규 구현, 대시보드/매매 페이지 실시간 UI 적용
- **파일**:
  - `frontend/src/hooks/use-realtime.ts` — useRealtime 훅 신규 (WebSocket 연결, 구독, ticks Map 상태관리, graceful degradation)
  - `frontend/src/app/(authenticated)/dashboard/page.tsx` — HoldingsTable에 실시간 현재가 표시, 연결 시 "실시간" 인디케이터 추가
  - `frontend/src/app/(authenticated)/trade/page.tsx` — 종목 선택 시 실시간 가격 오버레이, "실시간" 인디케이터 추가
  - `frontend/next.config.ts` — /api/v1/ws/:path* 명시적 rewrite 추가
  - `frontend/__tests__/smoke/pages.test.tsx` — useRealtime mock 추가
- **결정**:
  - symbols 배열을 useEffect 의존성으로 사용, 상위 컴포넌트에서 useMemo로 안정화
  - SSR 방지: typeof window !== "undefined" 체크
  - isConnected=false 시 기존 REST 데이터 표시 유지 (graceful degradation)
  - 실시간 가격: liveTick?.price ?? 기존가격 패턴으로 폴백
  - change 방향에 따른 색상: 상승=red, 하락=blue (한국 주식 컬러 컨벤션)
- **검증**: 9/9 스모크 테스트 통과

### 02:06 - [qa] test_realtime.py 작성 + 전체 검증
- **수행**: KiwoomWebSocket 테스트 파일 신규 작성 및 전체 테스트 스위트 검증
- **파일**: `tests/broker/test_realtime.py` 신규 (74개 테스트)
- **결정**:
  - async with connect() 패턴을 위해 _make_mock_connect() 헬퍼로 컨텍스트 매니저 모의
  - subscribe/unsubscribe 테스트는 ws_client._ws 직접 설정 (connected_ws fixture)
  - max retries 테스트: reconnect_attempt가 async with 진입 시 리셋되므로 __aenter__ 에서 예외 발생시킴
- **검증**: 636개 전체 통과, 커버리지 91.68% (85% 기준 통과), ruff 에러 0개

---

## 세션 17 — WebSocket 전체 완료 + main 머지

### 04:00 - [lead] WebSocket 4단계 PR 머지 완료
- **수행**: WebSocket 마이그레이션 4단계 전체 완료, dev→main 머지
- **PR 이력**:
  - PR #106: Step 1 (WebSocket 클라이언트) → dev (squash, merged)
  - PR #107: Steps 2+3 (live_trader WS + FastAPI 엔드포인트) → dev (squash, merged)
  - PR #108: Step 4 (프론트엔드 실시간 UI) → dev (squash, merged)
  - PR #109: dev → main (merge commit, merged)
- **검증**: 650 tests passed, 91.57% coverage (backend) + 9/9 (frontend smoke)
- **후속**: claude 브랜치 main 싱크 완료

### 04:10 - [lead] 문서 업데이트
- **수행**: project.md WebSocket 완료 반영, 세션 로그 기록
- **파일**: `.claude/memory/project.md`, `.claude/memory/sessions/2026-03-12.md`

---

## 세션 18 — WebSocket 키움 스펙 준수 재작성 + 버그 조사

### 08:30 - [lead] 크론 정상 실행 확인
- **수행**: 08:30 크론 실행 확인, 스크리닝 30종목 진행 중 확인
- **문제**: 토큰 발급 429 에러로 live_trader.py 실행 실패 (스크리닝 후 레이트 리밋)
- **원인**: authenticate()에 429 재시도 로직 없음 + 쿨다운 10초 부족

### 10:00 - [team:ws-fix] WebSocket 키움 스펙 준수 재작성
- **수행**: 사용자 제공 3개 문서(project-evaluation.md, websocket-guide.md, contract-testing-guide.md) 기반 전면 수정
- **팀**: backend(Sonnet) + qa(Sonnet) + caller-fix(Sonnet)
- **변경**:
  - realtime.py: 포트 10000, LOGIN 패킷, PING/PONG, flat JSON + item/type 배열, values 숫자코드 파싱
  - constants.py: WS_PORT, WS_TRNM_LOGIN/PING/PONG, WS_DEFAULT_GRP "1"
  - fixture 13개 + contract test 22개 + test_realtime.py 79개 재작성
  - 호출부(live_trader.py, api/realtime.py): 수정 불필요 (인터페이스 호환)
- **PR #110**: feat/ws-spec-fix → dev (squash, merged), Actions 통과
- **검증**: 677 tests, 91.27% coverage

### 10:30 - [lead] 대시보드 0원 + 백테스트 승률 버그 조사 시작
- **수행**: project-evaluation.md 미반영 사항 + 사용자 보고 버그 조사
- **이슈**: 대시보드 보유종목 금액 0원 표시, 백테스트 승률 이상
- **조사중**: 키움 API 필드명 불일치 가능성 (kiwoom.py get_balance() 파싱)

## 세션 18 — realtime.py + constants.py 키움 WS 스펙 준수 재작성

### 10:04 - [backend] realtime.py + constants.py 전면 재작성 (키움 WebSocket 스펙 준수)
- **수행**: websocket-guide.md 기반으로 constants.py와 realtime.py를 실제 키움 스펙에 맞게 재작성
- **파일**:
  - `src/broker/constants.py` — WS_DEFAULT_GRP "0000"→"1", WS_PORT=10000 추가, WS_TRNM_LOGIN/PING/PONG 추가
  - `src/broker/realtime.py` — 전면 재작성 (URL 포트 추가, _ws_symbol 제거, 로그인 패킷, PING/PONG, flat+배열 구독/해지, flat 응답 파싱, values 숫자코드 필드)
- **결정**:
  - `_to_ws_url()`: 포트 10000 포함 (`wss://mockapi.kiwoom.com:10000/api/dostk/websocket`)
  - `_send_login()`: Bearer 접두사 제거 후 순수 토큰 전송, return_code=0 확인 후 구독 replay
  - 구독(REG): flat JSON, item/type 배열, authorization 제거, 여러 종목→단일 배열
  - REAL 파싱: values 숫자코드 사용 (0B: "10"=현재가, "20"=체결시간, "15"=거래량, "13"=누적거래량 등)
  - ping_interval=None: 키움 자체 PING/PONG 사용, websockets 자동 ping 비활성화
- **검증**: ruff 에러 0개
- **TODO**: Task #2 Contract Test 체계 구축, Task #3 live_trader.py + api/realtime.py 호출부 수정

### 10:15 - [caller-fix] Task #3 — live_trader.py + api/realtime.py 호환성 검증
- **수행**: Task #1 realtime.py 재작성 후 호출부 파일 4개 검토 및 검증
- **파일**: 변경 없음 (기존 코드가 이미 새 인터페이스와 완전 호환)
- **결정**:
  - `scripts/live_trader.py` — `_ws_symbol()` 미사용, `subscribe(symbols, "0B")` 순수 종목코드 사용, `get_token=client._ensure_token` 그대로 (realtime.py가 Bearer 자동 제거)
  - `src/api/v1/realtime.py` — `subscribe(symbols, data_type=data_type)` / `unsubscribe(symbols)` 인터페이스 일치
  - `tests/test_live_trader.py` — `RealtimeTick(symbol, price, volume, timestamp)` 필드 동일, 5개 WS 테스트 정상
  - `tests/api/test_realtime.py` — mock 호출 검증(`data_type="0B"` 키워드 인자) 일치, 9개 테스트 정상
- **검증**: 58/58 통과, ruff 에러 0개

### 15:30 - [QA] Contract Test 체계 구축 (Task #2)
- **수행**:
  1. fixture JSON 생성 (13개) — PDF 스펙 기준, _meta + payload 구조
  2. contract test 작성 (`tests/broker/test_ws_contract.py`) — 22개 테스트
  3. `tests/broker/test_realtime.py` 전면 재작성 — 79개 테스트 (구 스펙 제거, 신 스펙 적용)
- **파일**:
  - `tests/fixtures/kiwoom/websocket/` — login_request, login_response_ok, subscribe_*.json, unsubscribe.json, real_*.json, ping.json, pong.json, reg_response_*.json (13개)
  - `tests/broker/test_ws_contract.py` (신규) — TestLoginContract, TestSubscribeContract, TestUnsubscribeContract, TestResponseParsingContract, TestPingPongContract
  - `tests/broker/test_realtime.py` — _ws_symbol 제거, flat 메시지 포맷, values 숫자코드, recv() 로그인 mock, WS_PORT 포트 확인 추가
- **결정**:
  - `_make_mock_ws`에 `recv = AsyncMock(return_value=_LOGIN_OK_RESPONSE)` 추가 — _send_login이 연결 직후 recv() 호출하므로 필수
  - TestWsSymbol 클래스 제거 — `_ws_symbol()` 함수 자체가 제거됨
  - `asyncio.sleep(0.05)` — login 포함 여러 await 처리 위해 0 → 0.05으로 증가
  - contract test: fixture가 틀리면 같은 문제 반복 → fixture는 PDF 기준으로만 작성
- **검증**: 101/101 통과, ruff 에러 0개 (test_realtime.py 79 + test_ws_contract.py 22)

---

## 세션 19 — project-evaluation.md 반영 (6개 태스크 일괄 처리)

### 12:40 - [lead] project-evaluation.md 기반 6개 태스크 병렬 처리
- **수행**: 사용자 제공 project-evaluation.md의 개선사항 6건 일괄 처리
- **팀**: lead(직접 #2,#3) + backend(Opus,#1) + devsecops(Opus,#4) + frontend(Opus,#5)

### Task #1: 대시보드 0원 버그 수정 [backend, Opus]
- **파일**: `src/broker/kiwoom.py`, `tests/broker/test_kiwoom.py`
- **수정**: `deposit_data.get("ord_alow_amt", 0)` → `deposit_data.get("entr", 0)` (kt00001 실제 필드명)
- **원인**: kt00001 응답에 `ord_alow_amt` 필드 없음 → 항상 0 반환
- **검증**: 33/33 통과, kiwoom.py 커버리지 87%

### Task #2: 백테스트 --days 3→60 [lead, 직접]
- **파일**: `scripts/cron_backtest.sh`
- **수정**: `--days 3` → `--days 60` (기술지표 20일 이평/14일 RSI/볼린저밴드 계산에 최소 20일 필요)

### Task #3: CORS 화이트리스트 [lead, 직접]
- **파일**: `src/main.py`
- **수정**: `allow_methods=["*"]` → `["GET","POST","PUT","DELETE","OPTIONS"]`, `allow_headers=["*"]` → `["Content-Type","Authorization"]`

### Task #4: CI pytest+ruff 자동화 [devsecops, Opus]
- **파일**: `.github/workflows/test.yml` (신규)
- **구성**: lint job (ruff check + format) + test job (pytest --cov-fail-under=85, PostgreSQL 17 service)
- **트리거**: PR/push (main, dev), `**.py` paths 필터

### Task #5: 프론트엔드 API 타임아웃 + WS 재연결 [frontend, Opus]
- **파일**: `frontend/src/lib/api.ts`, `frontend/src/hooks/use-realtime.ts`
- **수정**: AbortController 10초 타임아웃 + exponential backoff 재연결 (2s→30s, 최대 5회)
- **검증**: npm build 성공, 9/9 vitest 통과

### Task #6: 문서화 [lead, 직접]
- **파일**: `.claude/memory/project.md`, `.claude/memory/sessions/2026-03-12.md`
- **수정**: 현재 상태 세션 19 기준 업데이트, 즉시 로드맵 완료 항목 반영

### 전체 검증
- **테스트**: 677개 통과, 커버리지 91.27%
- **ruff**: All checks passed
- **TODO**: feat 브랜치 생성 → dev PR → main 머지

---

## 세션 20 — 잔고 조회 3건 버그 수정 + main 배포

### 13:00 - [lead] 잔고 조회 버그 3건 수정 + CI 수정
- **수행**: 대시보드 잔고 0원 표시 근본 원인 3건 수정 + .gitignore CI 에러 수정
- **파일**:
  - `src/broker/kiwoom.py` — return_code 에러 감지, 토큰 8005 자동 재시도, kt00018 기반 정확한 금액 계산(prsm_dpst_aset_amt), ord_alow_amt로 주문가능금액 조회
  - `tests/broker/test_kiwoom.py` — balance mock 3-call 구조, 에러 핸들링 테스트 3건 추가
  - `.gitignore` — `!src/ai/data/` 예외 추가 (data/ 패턴이 src/ai/data/ 차단)
- **버그 원인**:
  1. `_request()`가 `return_code` 에러 미감지 → 무효 토큰 응답을 정상으로 처리
  2. 토큰 8005 에러 시 재시도 로직 없음
  3. `entr`(예수금)에 D+2 미결제 매수금 포함 → total_eval 이중계산 (12M vs 실제 ~9.85M)
- **검증**: 689 tests, 94.06% coverage

### 13:30 - [lead] main 배포 완료
- **수행**: feat/project-eval-fixes → dev → main 순차 머지
- **PR 이력**:
  - PR #111: feat/project-eval-fixes → dev (squash, merged) — CI 전체 통과
  - PR #112: dev → main (merge commit, merged) — CI 전체 통과
- **후속**: claude 브랜치 main 싱크 완료

---

## 세션 21 — 문서-코드 정합성 감사 + 수정

### 00:45 - [docs] 문서-코드 정합성 감사 (Task #1) 완료
- **수행**: 7개 활성 문서 + 실제 코드 비교 감사
- **발견된 불일치**:
  1. project.md — 세션 20 결과 미반영 (테스트 677→689, PR #112 싱크)
  2. bug-dashboard-balance-zero.md — "수정 대기" 상태 (실제 PR #111,#112로 완료)
  3. design-v1.1.md 16D — AI 모듈 구조 설계안과 실제 구현 불일치
  4. doc-registry.md — sessions/2026-03-12, 루트 .md 4개 미등록
  5. decisions-pending.md — 헤더에 #15 미결정 누락
  6. sessions/2026-03-06.md — 충돌 마커 없음 (이미 해결됨)
  7. project.md — LLM 엔진 완료 표시 vs bot.py 미연동

### 00:45 - [docs] 문서 불일치 전체 수정 (Task #2) 완료
- **수행**: 감사 결과 기반 5개 문서 수정
- **파일**:
  - `.claude/memory/project.md` — 세션 20 기준 업데이트, AI 엔진 상태, API 라우터 9개, 단기 TODO 추가
  - `.claude/memory/bug-dashboard-balance-zero.md` — 수정 완료 상태로 업데이트, 최종 수정 내용 추가
  - `.claude/memory/design-v1.1.md` — 16D AI 모듈 구조 실제 구현 기준으로 갱신
  - `.claude/memory/decisions-pending.md` — 헤더 #15 추가, 마지막 검토 날짜 갱신
  - `.claude/memory/doc-registry.md` — sessions/2026-03-12, 루트 .md 4개 등록, 검증일 갱신
- **커밋**: feat/doc-reconciliation (2개 커밋)
- **TODO**: PR 생성 → dev 머지
