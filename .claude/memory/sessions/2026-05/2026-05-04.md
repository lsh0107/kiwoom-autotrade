# 2026-05-04 세션 로그

### 10:24 - [team-lead] 운영 점검 (4일만에 재개)

- **수행**:
  - docker compose 모든 서비스 healthy 확인 (3일째 안정)
  - **live_trader idle (pid=None)** — 04-30 backend 강제 재기동 후 ProcessManager 자동 재기동 실패 발견
  - daily_candles 최신 2026-04-30 (5/1 근로자의 날 + 5/2~3 주말 → 정상)
- **결정**: live_trader 재기동 + 5/29 trigger 전 critical 부채 해결

### 10:30 - [team-lead] live_trader 재기동 + 강제 trigger

- **수행**:
  - `POST /api/v1/bot/trading/start` → starting → running
  - 강제 trigger: 18 → **21종목** (+028260, 064760, 088800)
  - mock 잔고 9,789,497 그대로 (mock 한계 — 잔고 차감 미시뮬)
  - 평가손익 -20,870 (-0.20%) — 거의 break-even (3일치 단기)
- **사용자 보고**: "왜 21개만? 109종목이더만?" — 109(monitor) vs 176(universe) vs 21(매수 대상) 구분 설명
- **사용자 보고**: "종목마다 전략 달라?" — cross_momentum 모드는 단일 전략. multi_regime 시절 잔재가 코드에 보이는 것
- **사용자 보고**: "왜 자본 안 늘어?" — mock 서버 cash 차감/증가 시뮬 미적용. 실거래/모의 사이트 자본 변경으로 우회 필요

### 11:50 - [team-lead] Codex 멀티에이전트 전략 검토 결과 수령

- **사용자**: gpt-5.5 high (Codex multi-agent)로 매매 전략 + 리스크 검토 의뢰. 결과 받아옴
- **Codex 5건 발견**:
  1. multi_regime 단타 폐기 (이미 ADR-024로 처리)
  2. cross_momentum 강한 알파 아님 — 후보 수준 (V2 1/8 PASS)
  3. **지정가 매수 체결 전 포지션 등록** — multi_regime polling 전용. cross_momentum 무영향
  4. **공통 리스크 게이트 우회** — `live_trader` / `cross_momentum_rebalance` 모두 `order_service.run_all_checks` 미호출. cross_momentum도 영향
  5. HWM 드로우다운 청산 후 갱신 — multi_regime 전용
- **본 세션 처리 critical**: #4 + 04-29 audit 미처리 `_last_rebalance_date` 영속화

### 12:20 - [team-lead] PR #375/#376 — 공통 리스크 게이트 통합 + DB 영속화

- **변경 파일**: `src/trading/cross_momentum_rebalance.py` (+176/-25)
  - `_place_buy_order` / `_place_sell_order` 시그니처: `db: AsyncSession | None`, `user_id: uuid.UUID | None` keyword args 추가
  - `run_all_checks` 호출: `side, price, quantity, db, prev_close, max_investment=5M, max_daily_orders=200`
  - 게이트 차단 시 종목 1건만 skip (다른 종목 계속 처리)
  - `execute_monthly_rebalance`에서 db session + user_id 1회 resolve, finally close
  - `_get_last_rebalance_date_db()` / `_set_last_rebalance_date_db()` 헬퍼 — `strategy_config` KV 테이블 재사용 (`key=last_rebalance_date_cross_momentum`). 마이그레이션 불필요
  - 가드 강화: instance cache + DB 둘 다 확인
- **PR 사이클**:
  - PR #375 (feat → dev) — squash merge sha 0a7518a
  - PR #376 (dev → main) — merge commit sha c0881ae
  - claude 싱크 sha e5290f5
- **회귀**: 1874 PASS / ruff clean
- **테스트 호환**: 기존 `_place_buy_order(...)` 호출은 db=None/user_id=None로 게이트 skip
- **결정 근거**:
  - Codex가 본 5건 중 cross_momentum 영향은 #4와 그가 못 본 _last_rebalance_date 2건 — critical 한정
  - #3, #5는 multi_regime polling 전용 — 폐기 모드라 본 PR 범위 밖
  - 5/29 정상 trigger 전 process restart 사고 + 일일 주문수 초과 같은 운영 사고 차단
- **운영 상태**:
  - main: c0881ae / claude: e5290f5
  - live_trader running (재기동 후 강제 trigger 1회 — 21종목 보유)
  - 다음 자연 trigger: 2026-05-29 (금) 14:55
- **남은 follow-up**:
  - **Med**: TestFlowSignal/ThemeBoost fixture 격리 (xfail 정식화)
  - **Med**: multi_regime 코드 정리 또는 deprecation 주석 (Codex #3, #5 영향 경로)
  - **Med**: docker compose airflow-postgres auto-start 정책
  - **Med**: Airflow daily candle DAG 일일 갱신 보장 점검 (5/29 데이터 stale 방지)
  - **Med**: backend 컨테이너 pykrx 의존성 정책
  - **Low**: 매도가 추적 (체결가 회수 또는 sell quote 추가)
  - **Low**: 텔레그램 재발 모니터링
  - **Low**: 사용자 메모리 reference_live_trader_ops.md admin@local.dev 항목 — 04-29 audit 정정 원복 (실제 DB에 admin@local.dev 1건 존재 확인됨, 04-30 패스워드 reset 시 검증)
- **결정**: 사용자 판단 — 5/29 정상 trigger까지 추가 작업 없이 대기 (옵션 A)
