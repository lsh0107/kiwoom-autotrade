# 2026-05-22 세션 로그

오늘 한 일 전체 — 작업자 (Claude / Codex / human) 가 처음 들어와도 컨텍스트 잡히도록 정리.

## 컨텍스트 (전제)

두 워크스페이스 병행 작업:
- `/Users/sanghyuklee/individual/stock/kiwoom-autotrade` — 트레이딩 엔진. GitHub `lsh0107/kiwoom-autotrade`. main HEAD = `806f07f` (어제 #479 머지 후, 오늘은 kiwoom 쪽 코드 변경 없음).
- `/Users/sanghyuklee/individual/stock/ai-hedge-fund-lab` — Korea AI hedge proposal lab. GitHub `lsh0107/ai-hedge-fund-lab` (별도 private repo, 어제 init). 오늘 PR C/D 머지 진행.

운영 인계 / 로드맵:
- `docs/ai-hedge/AI_HEDGE_FUND_INTEGRATION_HANDOFF.md` — 두 repo 연결 안전 경계.
- `docs/ai-hedge/PROPOSAL_QUALITY_ROADMAP.md` — PR 분할 + 정책 (PR A → G → F → B → C → D → E → H).

자동 재빌드 (kiwoom 쪽):
- `scripts/post_merge_rebuild.sh` + `.claude/rules/post-merge-rebuild.md`.
- 오늘 kiwoom 머지 없어 호출 없음.

---

## 10:30 — [lead] PR A 보완 (Codex 리뷰 반영)

- 어제 PR A (평가 인프라) 1차 보완 후 Codex 가 5개 항목 지적 (BLOCKER + P1×2 + P2×2).
- 진행:
  - close-to-close evaluation 명명 통일 (`__init__`/`harness`/`cli`/summary MD).
  - `n_directional` / `n_evaluable_directional` / `coverage_rate` / `conservative_hit_rate` 추가.
  - `pyproject.toml` `[tool.pytest.ini_options] pythonpath = ["src"]` 로 `uv run pytest` 단독 통과.
  - CLI 잘못된 horizon → `parser.error` 경유 SystemExit(2).
- BLOCKER (#1) — git versioning 미정 — 사용자에게 옵션 (A) 별도 repo / (B) kiwoom 편입 / (C) parent monorepo 제시.
- 결과: 60 passed.

## 11:00 — [lead] ai-hedge-fund-lab 별도 private repo init

사용자 결정: (A) 별도 repo.

- `lsh0107/ai-hedge-fund-lab` private 생성.
- `us/ai-hedge-fund/` (upstream 원본 nested git) 는 `.gitignore` 로 제외. `us/README.md` 만 추적.
- repo local `user.name=lsh0107`, `user.email=101846036+lsh0107@users.noreply.github.com`.
- `[tool.uv] default-groups = ["dev"]` — `data` 그룹은 명시적 sync 만.
- 초기 commit (`c059974`) + GH Actions CI 통과 (60 passed).

## 11:30 ~ 14:00 — [lead] PR G — Market Constraints

브랜치: `feat/pr-g-market-constraints` → 머지 `5675f54`.

- `models.MarketConstraints` (frozen): is_suspended / is_management_issue / is_investment_warning / is_investment_alert / upper_price_limit / lower_price_limit / metadata_source.
- `risk/market_constraints.py` — 순수 함수 `apply_market_constraints`. flag: TRADING_HALTED / MANAGEMENT_ISSUE / INVESTMENT_WARNING / INVESTMENT_ALERT / UPPER_LIMIT_NEAR / LOWER_LIMIT_NEAR / NO_MARKET_METADATA.
- 정책 매트릭스:
  - suspended → buy/sell 양방향 hold
  - management/warning → buy 차단, sell 허용
  - alert → flag only
  - upper_limit_near (≤3%) → buy 차단
  - lower_limit_near (≤3%) → sell 강행 금지 (sell→hold)
  - no metadata → flag만, 기존 결정 유지
- export 매핑 갱신: 이전엔 문자열 하드코딩이었으나 review 받아 `BUY_BLOCKING_FLAGS` import 로 single source 화 (drift 방지).
- 86 passed → 보완 후 93 passed.

## 14:30 ~ 16:00 — [lead] PR F — Sizing

브랜치: `feat/pr-f-sizing` → 머지 `0659744`.

- 신규 모듈 `risk/sizing.py`:
  - `compute_atr(bars, period=14)` — 표준 True Range 평균.
  - `korean_tick_size(price)` — 한국 호가 단위 밴드 (2023 개편 기준).
  - `round_price_down_to_tick(price)` — 주문 가격 내림 정렬.
  - `compute_buy_sizing(...)` — BuySizingResult. method=`atr_risk`/`position_pct_fallback`/`insufficient`.
- 산정 순서: ref → tick aligned → budget_cap → limit_cap → atr_cap → min → qty → notional.
- `RuntimeConfig` 추가: `atr_period=14`, `atr_risk_pct=0.005`, `atr_stop_mult=2.0`.
- Codex P1 — `total_asset_krw=None` 일 때 `risk_krw=0` → atr_cap=0 → INSUFFICIENT 회귀 가능성. 수정: ATR cap 활성화는 `total_asset_krw > 0` 일 때만, 그 외 fallback 유지 + `atr_cap_skipped_reason="missing_total_asset"` evidence 부착. tests 추가.
- 118 → 121 passed.

## 16:00 ~ 17:00 — [lead] PR B — Portfolio Context

브랜치: `feat/pr-b-portfolio-context` → 머지 `55c48f5`.

- `models.PositionSnapshot` (frozen): broker quantity 1차, orders 기반 avg_entry_price/holding_days/unrealized_pnl_pct/orders_derived_quantity 는 optional.
- `risk/portfolio_context.py::apply_holding_review`:
  - flag: HOLDING_LOSS_REVIEW / HOLDING_PROFIT_REVIEW / HOLDING_STALE_REVIEW / HOLDING_DATA_MISMATCH.
  - **action 변경 없음** — export 에서 매핑.
- export 매핑: `hold + HOLDING_*_REVIEW + holding_qty>0 → review_sell`. block_buy 우선.
- HOLDING_DATA_MISMATCH 단독은 review_sell emit 안 함 (정보성).
- broker holdings 우선 정책 — orders 적분으로 quantity 재구성 금지 (과거 정합성 사고 회피).
- evidence 키: `remaining_buy_capacity_krw` 신규.
- 150 passed.

## 17:00 ~ 18:00 — [lead] PR C — Investor Flows (외인+기관)

브랜치: `feat/pr-c-investor-flows` → 머지 `8188e42`.

- `models.InvestorFlow` (frozen) + `SymbolMarketData.investor_flows: tuple = ()` + RuntimeConfig 임계값.
- `agents/flow_signal.py`:
  - `compute_flow_signal` — 외인+기관 누적 → **거래대금 대비 정규화** (raw KRW 금지).
  - `apply_flow_adjustment` — threshold step ±0.15(5d)/±0.30(20d) × action × direction → confidence ±10.
  - flags: FLOW_DATA_MISSING / FLOW_DATA_STALE.
- `adapters/flows_pykrx.py` — optional lazy import. 네트워크 호출은 별도 fetch 경로만.
- `pyproject.toml`: `[dependency-groups] data = ["pykrx>=1.0.45"]` + `[tool.uv] default-groups = ["dev"]` → CI 기본 sync 에 pykrx 미포함.
- Codex P1: calendar staleness → 주말/공휴일 false-stale 위험. 수정: trading_dates 인자로 거래일 기준 staleness, fallback calendar. `staleness_basis` evidence 키. default `flow_staleness_days=2 → 4`. Fri→Mon 회귀 가드 테스트.
- Codex P2: input 정렬 가정 위험. 수정: `compute_flow_signal` 내부에서 defensive sort + 회귀 테스트.
- Policy: LOW_CONFIDENCE 게이트가 flow 보정보다 먼저 — "수급이 low-confidence 를 rescue 하지 않는다" 명시 주석 + 회귀 테스트.
- 184 passed.

## 18:00 ~ 19:00 — [lead] PR D — Sector Rotation

브랜치: `feat/pr-d-sector-signal` → 머지 `64ed345`.

- `models.SectorSnapshot` (frozen): symbol/sector_code/sector_name + sector_return_5d/20d + benchmark_return_5d/20d + latest_date. property `relative_strength_5d/20d`.
- `SymbolMarketData.sector_snapshot: SectorSnapshot | None = None`.
- `agents/sector_signal.py::apply_sector_adjustment`:
  - **raw return 금지** — `sector - benchmark` 상대강도 기반.
  - threshold step ±0.02(5d) / ±0.05(20d).
  - action × direction 매트릭스 동일 (PR C 패턴).
  - flags: SECTOR_DATA_MISSING / SECTOR_DATA_STALE.
  - evidence 모두 `sector_` prefix (PR C `flow_*` 와 충돌 없음).
- `adapters/sector_mapping.py`: SectorMappingProvider 인터페이스 + `FixtureSectorMappingProvider.from_json` (단일 하드코딩 truth 금지, fixture/provider 분리).
- `adapters/sectors_pykrx.py`: optional lazy import scaffold.
- LOW_CONFIDENCE rescue 금지 정책 유지.
- 212 passed. eval baseline 동일 (deterministic).

## 19:00 — [lead] PR E 설계 문서

PR E 는 양쪽 repo 동시 변경 + 실 주문 경계 첫 진입이라 코드 전에 설계 합의 필요.

- 문서 작성: `docs/ai-hedge/PR_E_DESIGN.md` (kiwoom-autotrade).
- 다른 Claude / Codex 가 이 문서만 읽고 바로 이어갈 수 있는 self-contained 작성.
- 핵심 내용:
  - bias 재설계 (`boost_buy`/`block_buy`/`review_sell`/`boost_sell` 신규)
  - regime 신호 (KOSPI/KOSDAQ bull/bear/range)
  - live_trader 의 boost_buy/review_sell/boost_sell 소비 정의 (모두 강제 X)
  - 안전 게이트 8개
  - approved↔applied 충돌 분석 — 결론: applied 는 "실제 매수/매도 실행" 기준
  - 모의투자 E2E 10 시나리오
  - PR E1 (lab 측) / PR E2 (kiwoom 측) 2단계 분할 권장
- **다음 액션 (보류)**: 사용자 검토 + §4/§5/§6 합의 → PR E1 진행.

---

## 머지 현황 (오늘 기준)

ai-hedge-fund-lab (`lsh0107/ai-hedge-fund-lab`):
| PR | 머지 | commit |
|---|---|---|
| #1 PR G market_constraints | squash | `5675f54` |
| #2 PR F sizing | squash | `0659744` |
| #3 PR B portfolio_context | squash | `55c48f5` |
| #4 PR C investor_flows | squash | `8188e42` |
| #5 PR D sector_signal | squash | `64ed345` |

main HEAD: `64ed345`. 212 passed on main.

kiwoom-autotrade: 변경 없음. main HEAD: `806f07f`.

## 안전 경계 contract (오늘 모든 PR 에서 유지)

- AI hedge 가 broker order API 를 직접 호출하지 않는다.
- 사용자 승인 없는 결정은 실 주문화되지 않는다.
- 모의투자 기본값.
- 모든 PR e2e QA 에 `orders count 불변` 포함.
- ai_hedge context_source 자동승인 제외 (#464, kiwoom 쪽).
- 실제 반영된 결정만 applied 마킹 (#466).

## 미해결 / 다음 액션

1. **PR E 설계 검토** — 사용자 + Codex 리뷰 대기.
   - 합의 후 PR E1 (lab) → PR E2 (kiwoom) 순.
   - 별도 PR 로 `PR_E_DESIGN.md` commit 도 필요할 수 있음 (현재는 로컬에만 존재).
2. **로드맵 §8 잔여**: PR H (분할 진입 옵션) — 선택적, PR E 안정화 후.
3. **eval baseline 갱신 시점**: PR E2 머지 후, 신호 품질 변화 측정용. 별도 baseline (예: `eval_post_pre_consume.json`) 작성 권장.

## 다른 작업자가 이어가려면 우선 읽을 파일

순서대로:
1. `kiwoom-autotrade/CLAUDE.md` + `.claude/rules/*.md` — 작업 규칙
2. `docs/ai-hedge/AI_HEDGE_FUND_INTEGRATION_HANDOFF.md` — 두 repo 연결
3. `docs/ai-hedge/PROPOSAL_QUALITY_ROADMAP.md` — 전체 PR 분할
4. `docs/ai-hedge/PR_E_DESIGN.md` ← 오늘 추가, 다음 작업의 진입점
5. ai-hedge-fund-lab 의 `kr/ai-hedge-fund/README.md` + `CLAUDE.md`
6. `risk_gate.py` — 전체 파이프라인 흐름 (PR A~D 통합 상태)
