# 2026-05-15 세션 로그

### 13:00 - [backend-short-swing] PR 2: 장중 entry engine 구현
- **수행**: short_swing 장중 진입 엔진 + 테스트 25개 작성
- **파일**:
  - `src/trading/short_swing.py` (신규) — ShortSwingParams, load_short_swing_params, run_entry_check
  - `tests/trading/test_short_swing.py` (신규) — 25 테스트 케이스 (전체 PASS)
- **결정**:
  - VWAP: intraday 분봉 없으므로 `current_price = VWAP` 가정 (가드 통과, TODO)
  - 전일 고가: 후보 테이블에 high 필드 없으므로 `cand.close` 사용 (더 엄격, TODO)
  - KOSPI/KOSDAQ MA20 시장 필터: 조회 경로 없어 미구현 (TODO, 다른 9개 가드로 보호)
  - Order.reason: `"short_swing"` free-text 사용
  - entry signal 에서 `current_price >= intraday_vwap` (VWAP=현재가 가정이므로 >= 필수)
- **검증**:
  - `uv run ruff check` clean
  - `uv run ruff format` clean
  - `uv run pytest tests/trading/test_short_swing.py` → 25/25 PASS
  - `uv run pytest tests/trading -q` → 501/501 PASS (회귀 0)
  - `uv run pytest -q` → 1975 passed, 2 xfailed (전체 유지)
- **TODO**: commit 대기 (lead 승인 후)

### 14:10 - [backend-short-swing-api] PR 4a: /api/v1/short-swing/* 라우터 + 응답 스키마
- **수행**: Short Swing API 라우터 6개 엔드포인트 + /strategy/current ShortSwingDetail 확장 + 테스트 18개
- **파일**:
  - `src/api/v1/short_swing.py` (신규) — 6 엔드포인트: status, candidates, positions, screen, run-entry-check, run-exit-check
  - `src/api/v1/strategy.py` (수정) — ShortSwingDetail 스키마 + _build_short_swing_detail 헬퍼 + SHORT_SWING 분기
  - `src/api/v1/router.py` (수정) — short_swing_router 등록
  - `tests/api/test_short_swing_api.py` (신규) — 18 테스트 케이스 (전체 PASS)
- **결정**:
  - dry_run 구현: run_entry_check/run_exit_check에 dry_run 인자 없으므로 보수적 안 채택 — ACTIVE_STRATEGY 환경변수를 none으로 강제하여 active_strategy_mismatch로 빈 결과 반환
  - admin-only: 현재 role 기반 권한 없으므로 get_current_user만 의존 + TODO 주석
  - exit_window: "09:20-15:10" 하드코딩 (short_swing_exit.py의 _EXIT_START/_EXIT_END와 일치)
  - universe_size: params.candidate_limit 사용 (screener가 사용하는 유니버스 크기)
- **검증**:
  - `uv run ruff check` clean
  - `uv run ruff format` clean
  - `uv run pytest tests/api/test_short_swing_api.py` → 18/18 PASS
  - `uv run pytest -q` → 2008 passed, 2 xpassed (회귀 0)
- **브랜치**: feat/short-swing-pr4a (commit f489c1b, push 완료)

### 14:38 - [frontend-short-swing] PR 4b: /strategy UI 분기 + Short Swing 대시보드
- **수행**: Short Swing 전략 대시보드 컴포넌트 + /strategy 페이지 분기 + 타입/상수/훅 추가
- **파일**:
  - `frontend/src/types/api.ts` (수정) — ShortSwingDetail, ShortSwingStatus, ShortSwingCandidate, ShortSwingPosition 타입 + StrategyCurrentResponse에 short_swing 분기 추가
  - `frontend/src/lib/constants.ts` (수정) — SHORT_SWING_STATUS/CANDIDATES/POSITIONS API 경로 + 쿼리 키
  - `frontend/src/hooks/queries/use-short-swing-status.ts` (신규)
  - `frontend/src/hooks/queries/use-short-swing-candidates.ts` (신규)
  - `frontend/src/hooks/queries/use-short-swing-positions.ts` (신규)
  - `frontend/src/components/strategy/short-swing-dashboard.tsx` (신규) — 파라미터 그리드 6카드 + 보유 포지션 테이블 + 후보 종목 테이블
  - `frontend/src/app/(authenticated)/strategy/page.tsx` (수정) — short_swing 분기 추가
  - `frontend/__tests__/smoke/pages.test.tsx` (수정) — short_swing 분기 2케이스 + mock 추가
- **결정**:
  - /strategy-config short_swing 섹션은 별도 PR로 미룸 (보수적 안)
  - 손익 색상 컨벤션 유지: 수익=red, 손실=blue
  - 후보 종목 return_5d 양수→red, 음수→blue 색상 적용
- **검증**:
  - `npm run lint` → 0 errors (기존 warnings 2개만)
  - `npm run build` → 성공
  - `npm test -- --run` → 17/17 PASS (신규 2 포함)
- **브랜치**: feat/short-swing-pr4b (commit fb744d6, push 완료)

### 15:12 - [backend-short-swing] PR 5: scheduler 연결 + MA20 daily + 미체결 취소
- **수행**: SHORT_SWING_STRATEGY_DESIGN.md 8절+13절 마지막 PR — live_trader SHORT_SWING 분기 + Airflow DAG + 미체결 취소 + MA20 마킹
- **파일**:
  - `scripts/live_trader.py` (수정) — SHORT_SWING 분기 추가: _check_short_swing_entry(09:20~13:00), _check_short_swing_exit(09:20~15:10), _check_short_swing_cancel(30분+15:20 일괄)
  - `src/trading/short_swing_cancel.py` (신규) — cancel_stale_buy_orders: 미체결 매수 주문 취소 (reason LIKE short_swing% + SUBMITTED + 30분 경과)
  - `src/trading/short_swing_exit.py` (수정) — _check_ma20_breakdown 헬퍼 + MA20 이탈 시 exit_reason=MA20_BREAKDOWN 마킹 (실매도는 다음 거래일)
  - `airflow/dags/postmarket/short_swing_screening.py` (신규) — daily_candle_collection Asset trigger, TaskFlow API, tags=[short_swing, screening, postmarket]
  - `tests/scripts/test_live_trader_short_swing.py` (신규) — 20 테스트
  - `tests/trading/test_short_swing_cancel.py` (신규) — 6 테스트
  - `airflow/tests/dags/test_short_swing_screening_dag.py` (신규) — 8 테스트
- **결정**:
  - MA20 이탈: 보수적 안 — 로깅+마킹만, 실매도는 다음 거래일 09:20 첫 exit 사이클
  - 미체결 cancel: threshold 30분 (매 사이클) + 15:20 이후 threshold 0 (전량 즉시)
  - Airflow DAG: Asset trigger (daily_candle_collection 완료 후) — cron 대신 데이터 의존성 기반
  - 브로커 cancel API 실패해도 DB 상태는 CANCELLED로 갱신 (보수적)
- **검증**:
  - `uv run ruff check` clean
  - `uv run ruff format` clean
  - `uv run pytest tests/scripts/test_live_trader_short_swing.py tests/trading/test_short_swing_cancel.py` → 26/26 PASS
  - `uv run pytest tests/ -q` → 2028 passed, 2 xpassed (회귀 0)
- **브랜치**: feat/short-swing-pr5 (commit 2c16868, push 완료)

### 16:08 - [backend-short-swing-hotfix-a] HOTFIX A: 상태머신 + 체결 reconciler + user scope + holdings 검증
- **수행**: 사용자 리뷰 P0/P1.A/P1.C/P1.D 4항목 즉시 수정
- **파일**:
  - `src/models/short_swing.py` (수정) — PositionStatus에 PENDING_ENTRY, RECONCILIATION_ERROR 추가. user_id FK, entry_order_id, exit_order_id, exit_price, exit_quantity, exit_time, realized_pnl 컬럼 추가
  - `alembic/versions/019_short_swing_state_machine_user_scope.py` (신규) — enum 확장 + user_id + exit 컬럼 + partial unique index 교체
  - `src/trading/short_swing.py` (수정) — BUY submitted → PENDING_ENTRY (기존 OPEN 즉시 생성 제거), user_id + entry_order_id 연결
  - `src/trading/short_swing_reconciler.py` (신규) — PENDING_ENTRY→OPEN(체결), PENDING_ENTRY 삭제(취소), CLOSING→CLOSED(체결+realized_pnl), CLOSING→OPEN(취소)
  - `src/trading/short_swing_exit.py` (수정) — user_id 스코핑 + P1.D broker holdings 검증 (0→RECONCILIATION_ERROR, 부족→실수량 SELL) + exit_order_id 기록
  - `src/trading/short_swing_cancel.py` (수정) — user_id 스코핑
  - `src/api/v1/short_swing.py` (수정) — user_id 스코핑 + PENDING_ENTRY open_positions 포함 + exit_price/exit_quantity/realized_pnl 응답 추가
  - `scripts/live_trader.py` (수정) — _check_short_swing_reconcile 5분 주기 통합
  - `tests/trading/test_short_swing_state_machine.py` (신규) — 12 테스트 (7 필수 + 보조)
  - `tests/trading/test_short_swing_exit.py` (수정) — user_id + get_holdings mock 추가
  - `tests/trading/test_short_swing.py` (수정) — PENDING_ENTRY 기대값 갱신
  - `tests/api/test_short_swing_api.py` (수정) — user_id 포함
- **결정**:
  - 부분체결 잔여: 별도 PENDING_ENTRY row 생성 대신 로그 기록 + cancel 모듈 위임 (unique index 충돌 방지)
  - partial unique index: ORM에서 제거, alembic raw SQL 전용 (SQLite 테스트 호환)
  - broker holdings 조회 실패 시: SELL skip + ERROR 로그 + 다음 사이클 재시도 (fail-closed)
- **검증**:
  - `uv run ruff check` clean
  - `uv run ruff format` clean
  - `uv run pytest tests/trading tests/api tests/scripts -q` → 893 passed (회귀 0, 신규 12 추가)
  - `uv run pytest -q` → 2040 passed (전체 유지)
- **브랜치**: feat/short-swing-hotfix-a (commit ac146f0, push 완료)

### 17:53 - [backend-short-swing-hotfix-c] HOTFIX C: cancel 실패 DB 보존 + Airflow KST + MA20 다음날 + max_positions + MA60
- **수행**: 사용자 리뷰 P2.B/P2.C + 추가 3항목 + 테스트 #9, #10, #추가A/B/C 수정
- **파일**:
  - `src/trading/short_swing_cancel.py` (수정) — broker cancel 실패 시 DB 상태 SUBMITTED 유지 + broker_cancel_failed 이벤트 기록 + continue
  - `airflow/dags/postmarket/short_swing_screening.py` (수정) — UTC→DailyCandle.date max 우선, 폴백 Asia/Seoul KST
  - `src/trading/short_swing_exit.py` (수정) — MA20 이탈 우선 청산 (kill_switch 다음 우선순위) + 우선순위 번호 재정렬
  - `src/trading/short_swing.py` (수정) — max_positions: balance.holdings→ShortSwingPosition 자체 포지션 카운트 (_count_short_swing_positions 헬퍼)
  - `src/screening/short_swing_screener.py` (수정) — _calc_metrics candles < 60 → return None (MA60 60캔들 미만 제외)
  - `tests/trading/test_short_swing_cancel.py` (수정) — test_broker_cancel_failure_keeps_submitted: DB cancel 미수행 + errors=1 검증
  - `airflow/tests/dags/test_short_swing_screening_dag.py` (수정) — test_screening_date_uses_kst_or_daily_candle: UTC 미사용 + DailyCandle/KST 존재 검증
  - `tests/trading/test_short_swing_exit.py` (수정) — TestMA20PendingNextDayExit: exit_reason=MA20_BREAKDOWN 마킹 포지션 다음날 우선 청산 검증
  - `tests/trading/test_short_swing.py` (수정) — TestMaxPositionsGuard: ShortSwingPosition 기반 카운트 + 다른 전략 holdings 무시 검증
  - `tests/screening/test_short_swing_screener.py` (수정) — TestMA60CandleMinimum: 59개 캔들 제외 + 60개 캔들 통과 검증
- **결정**:
  - broker cancel 실패 시 fail-closed: DB 상태 유지 (SUBMITTED) → 다음 사이클 재시도
  - Airflow date: DailyCandle.date max 우선 (수집 완료 데이터 기준), 폴백 KST
  - MA20 pending exit: pos.exit_reason == MA20_BREAKDOWN → kill_switch 다음 우선순위로 즉시 청산
  - max_positions: ShortSwingPosition(pending_entry/open/closing) 카운트, balance.holdings 미사용
  - MA60: candles < 60 → _calc_metrics None → 후보 제외
- **검증**:
  - `uv run ruff check` clean
  - `uv run ruff format` clean
  - `uv run pytest` → 2062 passed, 2 xpassed (회귀 0, 신규 5 추가)

## 18:00~19:22 [lead] HOTFIX A/B/C 5/15 일괄 머지 (사용자 리뷰 7건 + 추가 + 테스트 10)

- HOTFIX A PR #424/#425 (`54faac2`) — 상태머신 PENDING_ENTRY + 체결 reconciler + user scope + holdings fail-closed.
- HOTFIX B PR #426/#427 (`6dcc64f`) — 진입 신호 실데이터 (prev_day_high + VWAP) + dry_run 시뮬레이션.
- HOTFIX C PR #428/#429 (`c7cf21d`) — broker cancel 실패 보존 + Airflow KST + MA20 다음날 청산 + max_positions 자체 카운트 + MA60 60 캔들.
- claude `715e46a → e80632d`. 모든 feat 브랜치/팀/worktree 정리. zombie 팀 디렉토리 1건은 rm -rf 로 강제 정리.
- 활성화 잠금 메모리 "코드 잠금 해제 / 운영 잠금 (mock 1주 dry-run 전) 유지" 로 갱신.
