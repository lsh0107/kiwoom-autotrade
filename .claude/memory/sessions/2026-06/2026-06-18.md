# 2026-06-18 세션 로그

### 09:58 - [lead] 6/18 상태 + airflow 인프라 결함 발견
- 6/17 PR C 머지 후 첫 세션. PR D (candidate preflight + runbook hotfix) 의도로 진입.
- 라이브 preflight 실행 → 사용 가능 (PR A2 정책으로 cutoff=2026-06-17, PASS).
- 그러나 `short_swing_candidates total=0` 여전 + `daily_candles max=2026-06-16` (6/17 누락).
- 조사 결과 `airflow_postgres` 2026-06-08 02:10 UTC Exited(255), restart policy=`no` → 자동 복구 안 됨 → scheduler/webserver 가 metadata DB 없이 **5000+ 회 무한 재시작 루프** (10일간). DAG 자동 실행 불가능 → 데이터 결손 사고.

### 10:20 - [lead] PR D-1: airflow-postgres restart policy 수정 (#567 → #568 main cf8496b)
- `docker-compose.yml::airflow-postgres` 에 `restart: unless-stopped` 추가 (다른 airflow 컨테이너와 동일).
- 라이브 수동 복구로 정책 효과 검증: postgres healthy → scheduler 자연 복구 → daily_candle_collection 자동 trigger → success → Asset publish → short_swing_screening asset_triggered.
- 다만 DAG 자체 결함 + screening 실패는 별개.

### 10:54 - [lead] 6/17 daily_candles backfill (universe 165종목, 165건)
- airflow DAG 가 6/17 (장중) 실행돼 빈 결과 → 수동 backfill 필요. `outputs/regime/backfill_universe_20260618.py` (private).
- pykrx 로 6/17 universe (FROZEN_UNIVERSE 176종) → 165건 (실패 0). `daily_candles max=2026-06-17`.

### 11:00 - [lead] short_swing_screening 5/15부터 영구 실패 root cause: ModuleNotFoundError
- screening 5/15 이후 **모든 run 이 fail** (10분 timeout). task log 분석 결과 `ModuleNotFoundError: No module named 'src'` at `short_swing_screening.py:53` (`from src.screening import run_short_swing_screening`).
- airflow 컨테이너 `sys.path` 에 `/opt/airflow` 없음 (PYTHONPATH 미설정). `/opt/airflow/src` 는 mount 되어 있으나 import 경로에 없음.

### 11:18 - [lead] PR D-2: airflow PYTHONPATH=/opt/airflow 추가 (#569 → #570 main c52267c)
- `docker-compose.yml::x-airflow-common.environment` 에 `PYTHONPATH=/opt/airflow` 추가.
- 라이브 검증: scheduler 컨테이너 안에서 `from src.screening.short_swing_screener import run_short_swing_screening` import ok 확인.

### 11:25 - [lead] PR D-3 시도 (airflow 3.x compose 마이그레이션) → 미완성 → 롤백
- screening 재트리거 시 webserver 가 `airflow webserver` 명령으로 Restarting 루프 발견 (airflow 이미지 3.1.8 이지만 compose 명령이 2.x 시절). webserver 가 안 살아있어 task executor 가 API 호출 실패 (Connection refused).
- PR D-3 로컬 변경 시도: `webserver → api-server`, `airflow users create` 제거 (3.x 부재), healthcheck path `/health → /api/v2/monitor/health`, `AIRFLOW__API__BASE_URL=http://airflow-webserver:8080` 추가.
- webserver 자체 (api-server) 는 정상 기동 + healthy. 그러나 screening 재시도 시 task 가 여전히 10분 timeout 으로 fail (구체 원인 더 디버그 필요).
- **사용자 결정**: 시간 누적, PR D-3 롤백 (local docker-compose.yml 변경 git checkout), 세션 종료. airflow 3.x 완전 마이그레이션은 별도 PR 세트 (D-3a/D-3b/...) 로 분리.

### TODO (다음 세션)
- airflow 3.x 완전 마이그레이션 (큰 작업):
  - webserver/init/healthcheck/executor/API URL 등 명세
  - DAG 자체 결함 (`daily_candle_collection` task 내 `now().date()` → `logical_date` 사용)
  - airflow 3.x JWT secret / SimpleAuthManager / FAB user mgmt
  - task 실행 base URL 추가 env 후보
- 후속 lifecycle 검증은 airflow 마이그레이션 완료 후. 단 사용자 원칙 (DB 임의 후보 적재 금지) 유지.
- 현 docker 컨테이너 상태: webserver/scheduler/triggerer 는 수동 변경된 명령(api-server)으로 가동 중. 다음 compose up 시 깨질 위험 (compose 는 옛 명령) → 마이그레이션 PR 머지 후 정상화.
