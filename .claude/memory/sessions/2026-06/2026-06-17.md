# 2026-06-17 세션 로그

### 09:32 - [lead] 상태 확인 + PR A2 결함 발견
- PR C (6/17 mock run) 진행 의도로 진입. 라이브 preflight 실행 → **FAIL** (cutoff=6/17, max_date=6/16).
- 원인: PR A `_resolve_cutoff` 가 영업일이면 무조건 today 요구. 장중에 오늘 일봉이 안 채워지는 현실 미반영.
- 영향: cross_momentum weekly 14:55 trigger 도 같은 이유로 매번 FAIL 위험.

### 10:00 - [lead] PR A2 정책 보정 (#561 dev → #562 main b092a0c)
- `src/utils/krx_calendar.latest_completed_business_day` 신규: 장중(KST `DAILY_CANDLE_READY_HHMM` 기본 1700 이전) → 직전 영업일, 장후 → today, 휴장 → 직전 영업일.
- `_resolve_cutoff` (preflight) + `cross_momentum_rebalance.compute_target_portfolio` 둘 다 동일 헬퍼 사용.
- 회귀 테스트 7건 (장중/장후/16:59/휴장/env override/custom ready/다른 날짜) + 기존 PR A 테스트 deterministic 화.
- 전체 회귀 **2338 / 0 fail**. 라이브 preflight PASS (cutoff=6/16, max=6/16, coverage 0.938).
- observation plan v0.13.

### 10:09 - [lead] short_swing 30분 mock run 실행
- `bash scripts/run_short_swing_mock.sh` 사용자 명시 GO. 6/17 daily regime dry-run 선행 (bull_overheat 90, regime_report.json 생성).
- mock run 30분 (실가동 첫 cycle 10:52, 종료 11:21, 약 30 cycle).
- **PR A2 + PR B 통합 정상 작동 확인**: regime overlay 매 cycle 정상 노출 (`regime=bull_overheat allow=True override=1`), graceful shutdown (KeyboardInterrupt), trap 으로 `short_swing.enabled=false` 자동 복구.

### 11:21 - [lead] PR C: mock run 결과 (#563 dev → #564 main 1ce7284) — 조건부 PASS
- **검증 8 항목 중 7 PASS, 1 미검증**.
  - PASS: toggle 복구 / stale guard / regime max=1 / cross_momentum 매도 0 (Δorders=0 간접) / llm Δ=0 / idle tx 0 / stale pid·kill_switch 없음
  - 미검증: orders/trade_logs lifecycle (`SKIP: 후보 없음`, `short_swing_candidates` total=0)
- **blocker**: 후보 적재 경로 = `airflow/dags/postmarket/short_swing_screening.py` (Asset `daily_candle_collection` 트리거). airflow 4 컨테이너 6/17 오전 재시작 추정 → 6/16 장마감 시점 미가동 → DAG 미실행 + catchup=False → 후보 미적재.
- **부속**: runbook SQL hotfix #1 (`status='OPEN'` → `'open'`, enum 소문자). #2/#3 (사후 `'SELL'`, `orders.strategy` 컬럼) 은 PR D 영역.
- 파일: `docs/observation/2026-06-17-short-swing-mock-result.md` (PR C), `docs/observation/2026-06-17-regime-dryrun-summary.md`, `scripts/run_short_swing_mock.sh`.
- 사용자 지시 수용: DB 임의 후보 적재 금지 (파이프라인 검증 아님). candidate 적재 경로 조사 후 다음 액션.

### TODO (PR D 영역, 사용자 결정 후 진행)
- runbook preflight 에 `short_swing_candidates` 존재 검사 추가 (latest trade_date >= 직전 영업일).
- runbook post-run SQL bug fix #2 (`'SELL'` → `'sell'`) + #3 (`orders.strategy` 컬럼 정확화).
- candidate generation step 통합 여부 결정 (자동 airflow trigger vs 수동).
- airflow DAG (`daily_candle_collection` / `short_swing_screening`) 6/17 장마감 후 자동 실행 확인 → 6/18 mock run 재시도.
