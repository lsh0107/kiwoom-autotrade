# 2026-06-12 세션 로그

### 14:38 - [lead] §11.8 mini trigger smoke 시도 → preflight FAIL → NO-GO (plan v0.11)
- **경과**: 사용자 go (smoke 우선, daily dry-run 후속) → preflight 진입. backend/postgres 컨테이너 down 발견 (Exited 255, 11분 전 — 도커 재시작 추정) → `docker compose up -d postgres backend` 복구 (compose 인터폴레이션용 POSTGRES_PASSWORD 기본값 주입, post_merge_rebuild.sh 방식).
- **preflight**: §9.1 #1~#8/#10 + §11.3 S1~S4 PASS — mock 기본값 유효, ACTIVE_STRATEGY=cross_momentum (DB strategy_runtime cross_momentum 단독 enabled 일치), baseline orders 154 / trade_logs 117 / llm applied 0 (전일 동일), idle 0, kill switch·overnight_positions·stale PID 없음, rebalance_freq="weekly".
- **FAIL**: daily_candles max_date **2026-05-08** (1개월 결손), 13개월+당일 커버 종목 0. **silent stale 경로 확인** — `cross_momentum_rebalance.py` compute_target_portfolio 는 bar 수(≥273)만 보고 최신성 검사 없이 DB 캐시 사용 → 강행 시 한 달 밀린 momentum score 로 6/15 baseline 오염.
- **판정**: 14:48 NO-GO (사용자 확정). §11.8 목적(6/19 전 주문 lifecycle 사전 검증)은 미달성 — 6/19(금) 본 관찰 첫 weekly trigger 로 이월.
- **문서**: plan v0.11 — 변경 이력 + §11.8.1 실행 결과 append.

### 14:50 - [lead] daily_candles backfill
- `scripts/backfill_daily_candles.py --days 40 --market all`. 시행착오 2건: (1) pykrx 가 메인 venv 에 없음 → `uv run --group airflow`. (2) collectors upsert 가 env DATABASE_URL 요구 (.env 자동 로드 안 됨) → export 후 재실행.
- pykrx `get_market_ticker_list` KRX API 실패 (JSON decode) → DB stocks fallback: KOSPI 43 + KOSDAQ 12 종목만 수집. KOSPI 1161건 upsert 완료.
- **한계**: FROZEN_UNIVERSE(~200종목, ADR-022) 는 fallback 범위 밖 — universe 대상 보충 backfill 별도 필요 (후속).

### 15:00 - [lead] universe 대상 daily_candles 보충 backfill
- `outputs/regime/backfill_universe_20260612.py` (private) 신규: FROZEN_UNIVERSE(176) ∖ fresh(55) = 128종목 대상으로 5/3~6/12 pykrx 종목별 OHLCV upsert.
- **결과**: 3159건 upsert, fetch 실패 0종목. 최종 검증 max_date=2026-06-12, 당일 신선 172종목, universe 13개월+당일 커버 **165/176** (나머지 11종목은 13개월 이력 부재 = 신규 상장 추정 → rebalance 시 pykrx fallback 경로).

### 15:37 - [lead] 6/12 daily regime dry-run
- `bash scripts/run_daily_regime_dryrun.sh 2026-06-12` PASS. balance 는 admin 로그인 쿠키로 read-only fetch.
- **regime shift**: 3일 연속 risk_off(88→93→94) → **bull_overheat(83)**. flags=REGIME_VOL_SPIKE, REGIME_OVERHEAT_DISPERSION. timeline 분포 {structural_bull:2, bull_overheat:6, volatile_bull:1, risk_off:1}.
- proposal 15건 / action 변경 0 / boost_sell 자동소비 0 / boost_sell 신규 0.
- DB delta orders/trade_logs/llm_decisions 모두 0, strategy_runtime 불변, idle 0.
- **주의**: regime label 변경은 관찰 데이터일 뿐, [[feedback_no_strategy_judgement_without_live_trader]] 에 따라 threshold/전략/bias 변경 판단 금지.
- 파일: `docs/observation/2026-06-12-regime-dryrun-summary.md` (커밋 가능), `outputs/regime/2026-06-12/` (private, gitignored).

### TODO
- 6/19 전 §9.1 preflight 에 daily_candles 최신성 항목 추가 권고 (사용자 검토).
- `cross_momentum_rebalance.py` silent stale 경로 (캐시 사용 시 최신성 미검사) — 본 관찰 시작 전 후속 검토 대상.
- universe ∖ DB(11종목) 신규 상장 확인 후 stocks 테이블 정리 또는 universe 갱신.
