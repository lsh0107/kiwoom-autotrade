# 2026-06-01 세션 로그

## 컨텍스트 (전제)

- 직전 세션 (5-28) 까지 lab Claude 문서 정리 + waldlust 패턴 이식 + deep-work / /deep-check + backtest 정책 + design-021 evidence add 완료.
- PR E2 보류 유지 (sell 누적 0).
- 오늘은 매 영업일 1회 daily pipeline 실행 (사용자 정책 A 수동 유지).

## 한 일 (시간순)

### 09:46 - [lead] daily pipeline 2026-06-01 시도 → 504 (외부 키움 hang 가설)
- **수행**: `bash scripts/generate_daily_proposals.sh` (15 종목 kiwoom mode).
- **결과**: `/api/v1/account/balance` HTTP 504 (12.16s fail-fast). lab pipeline 자연 실패.
- **1차 진단**: PR #482 fail-fast 가 의도대로 작동. 외부 키움 hang 가설 보고.
- **5-29 자동 관찰도 동일 실패** (`outputs/proposals/2026-05-29/` 빈 디렉토리만).
- **TODO**: 사용자 backend 점검 요청.

### 11:50 - [lead] /api/v1/account/balance 504 root cause 진단 (요청: lab 외 점검)
- **수행**: 사용자 요청으로 backend 내부 진단. 1차 가설 (외부 키움 hang) 기각.
- **증거 체인**:
  1. backend container 안에서 `KiwoomClient.ensure_token()` 직접 호출 → OAuth 토큰 발급 0.13s 정상.
  2. 그 직후 `broker_credentials` UPDATE 가 8s 통째 hang.
  3. `pg_stat_activity`: PID **32195** 가 `broker_credentials` **RowExclusiveLock** 보유.
  4. state=`idle in transaction`, wait_event=ClientRead, **xact_age 3일 22시간** (2026-05-28 03:59 UTC 부터).
  5. client_addr=`172.20.0.7` (docker network 내부 backend container 자기 자신).
- **root cause 위치**: `src/api/v1/realtime.py::market_websocket` 의 `db=Depends(get_db)` AsyncSession 이 WebSocket 종료 전까지 살아 있음. `_get_active_credential` SELECT + `_get_token` 콜백의 `KiwoomClient.ensure_token()` → `broker_credentials` UPDATE 가 commit 안 됨 → 행 lock idle in transaction.
- **결정**: 1) backend restart 로 로컬 검증 환경 복구, 2) realtime.py HOTFIX, 3) 별도 long-lived async audit.

### 12:15 - [lead] backend 재시작 + balance 복구 + HOTFIX PR #495/#496
- **수행**:
  - `docker restart kiwoom-autotrade-backend-1` → 좀비 PID 32195 종료
  - balance: **504/12s → 200/0.36s** 회복
  - `src/api/v1/realtime.py` 수정:
    - `_get_active_credential` SELECT 후 cred 필드 값 변수 추출 + `db.commit()`
    - `_get_token()` 콜백 `try/except` 로 감싸 `ensure_token` 후 commit, 예외 시 rollback + 재발생
  - 테스트 +3 (`TestMarketWebSocketTransactionLifecycle`)
- **파일**:
  - `src/api/v1/realtime.py` (수정)
  - `tests/api/test_realtime.py` (+3 케이스)
- **PR**: lsh0107/kiwoom-autotrade#495 dev squash `2aa09ea` → #496 dev→main `12bb914` → claude sync `ed0d2e8` → backend 재빌드 (healthy)
- **검증**: pytest 2231 → **2234 passed** (+3), pre-commit 통과.
- **정책 준수**: broker/kiwoom.py 미변경 (broker 로직 변경 금지), fake fallback 없음.
- **6-1 daily pipeline 재실행**: 15 종목 정상. buy/hold/sell = 3/12/0, boost_buy 3, boost_sell 0, REGIME/SECTOR/FLOW_DATA_MISSING 7/7/7.

### 12:45 - [lead] long-lived AsyncSession transaction audit (PR #497/#498)
- **수행**: 사용자 요청으로 HOTFIX 후 audit. 코드 변경 0, 진단 리포트만.
- **범위**: WebSocket / live_trader / process_manager / reconciler / background task / scheduler / broker token refresh-save.
- **파일**: `docs/audit/2026-06-01-async-transaction-audit.md` (신규, 222 줄, 8 섹션)
- **결과**:
  - P0 (현재 leak): 0 건 (PR #495 로 해소)
  - P1 (구조적 위험): 2 건
    1. `token_store.save / get_or_refresh_token` commit 의무 호출자 암묵
    2. `cross_momentum_rebalance` line 793 `async_session_factory()` 직접 호출 (finally 보장 없음)
  - P2 (예방): 3 건 (`bot.py _start_bg`, `engine.run_analysis`, 정기 idle in transaction 모니터링)
- **설계안 §6 옵션 A 추천**: `token_store` 에 `get_or_refresh_token_isolated` 헬퍼 추가. `KiwoomClient.ensure_token` 가 isolated 우선 사용. 시그니처 변경 없음.
- **PR**: lsh0107/kiwoom-autotrade#497 dev squash `a291ce9` (Co-authored-by trailer 없음 — `--body` 명시) → #498 dev→main `e5ea597` → claude sync `0a51085`. 재빌드 대상 없음 (docs only).
- **Co-authored-by 출처 정리**: 로컬 git config `user.name=PeterCplat` 라 bash `git commit` 시 commit author 가 잡힘 → GitHub squash merge default body 가 trailer 자동 합산. 해결: `gh pr merge --squash --body "..."` 로 명시 body. 본 audit PR 부터 적용.

### 13:00 - [lead] audit P1 #1 — token_store isolated session (PR #499/#500)
- **수행**: audit §6 옵션 A 구현. token_store 항구적 해소.
- **파일**:
  - `src/broker/token_store.py` (+58)
    - `get_or_refresh_token_isolated(credential_id, authenticate_fn) -> str` 신규
    - `invalidate_cached_token_isolated(credential_id) -> None` 신규
    - 기존 `get_or_refresh_token / save / load` 시그니처/동작 유지 (docstring 에 commit 책임 호출자 명시 + isolated 권장)
  - `src/broker/kiwoom.py` (+18/-14)
    - `KiwoomClient.ensure_token()`: `self._credential_id` 만 있으면 isolated 우선 사용 (self._db 무관)
    - `KiwoomClient._invalidate_cached_token()`: 동일하게 isolated 사용
    - **public signature 변경 없음** (self._db / self._credential_id 인자 그대로)
  - `tests/broker/test_token_store.py` (+142)
    - `TestGetOrRefreshTokenIsolated` × 2 (success commit / failure rollback)
    - `TestInvalidateCachedTokenIsolated` × 2 (commit / rollback)
- **호환성**:
  - realtime.py HOTFIX (PR #495) commit 라인 그대로 유지 (이중 안전)
  - 인메모리 only 경로 변경 없음
- **PR**: lsh0107/kiwoom-autotrade#499 dev squash `c58a39f` → #500 dev→main `c58a39f` (main) → claude sync `0f413d4` → backend 재빌드 (healthy)
- **검증**:
  - pre-commit (ruff/format/mypy/bandit/secret) 통과
  - targeted (token_store + kiwoom + realtime + account): 72 passed (회귀 0 + 신규 4)
  - 전체 회귀: **2234 → 2238 passed** (+4), 2 xfailed (기존 fixture leak `strict=False`, 본 PR 무관)
  - CI Pytest 85%+ 양쪽 PR pass
  - balance API: 200 / 0.67s (cache hit 0.07s)
  - `idle in transaction` 세션: **0 건**

### 14:08 - [lead] audit P1 #2 — cross_momentum gate session async with (PR #501/#502)
- **수행**: audit P1 #2 (`docs/audit/2026-06-01-async-transaction-audit.md` §4.6) 후속 수정.
- **파일**:
  - `src/trading/cross_momentum_rebalance.py` (+72/-32)
    - `execute_monthly_rebalance` 의 게이트 세션 생성 패턴 변경
    - factory 호출 → `try/except` (실패 시 게이트 비활성)
    - 성공 시 `async with async_session_factory()` 진입 → `__aexit__` close 보장
    - `resolve_live_trader_user_id` 실패는 inner try → `gate_user_id=None`
    - Phase 1-3 → 신규 `_execute_rebalance_phases` 헬퍼로 추출 (session lifecycle / phase 분리)
  - `tests/trading/test_cross_momentum_rebalance.py` (+177)
    - `TestGateSessionLifecycle` × 4
      - `test_session_factory_failure_disables_gate`
      - `test_session_closed_on_normal_path`
      - `test_session_closed_when_phase_raises` (예외 전파 + `__aexit__` 보장)
      - `test_user_id_failure_runs_phases_without_gate`
- **호환성**:
  - 리밸런스 주문/게이트/계산 로직 동작 변경 없음
  - broker/order semantics 변경 없음
  - fake fallback 없음
  - KiwoomClient public signature 변경 없음
  - 기존 cross_momentum 테스트 88건 무수정 통과
- **PR**: lsh0107/kiwoom-autotrade#501 dev squash `bef86f0` → #502 dev→main `edd0c64` → claude sync `5b5e85f` → backend 재빌드 (healthy 21s)
- **검증**:
  - pre-commit (ruff/format/mypy/bandit/secret) 통과
  - targeted (cross_momentum 5 모듈): 115 passed
  - 전체 회귀: **2238 → 2242 passed** (+4), 2 xpassed
  - CI Pytest 85%+ 양쪽 PR pass (CodeQL/SAST/Secret/TruffleHog 모두 pass)
  - balance API 외부 호출: 200/401 25ms (auth 없이 — 빠른 응답 = 정상)
  - `idle in transaction` 세션: **0 건**
- **정책 준수**: `gh pr merge --squash --body "..."` 명시 body 사용 → Co-authored-by trailer 차단

## 오늘 머지 통계

| PR | 카테고리 | 머지 |
|---|---|---|
| #495 / #496 | HOTFIX: realtime WebSocket transaction leak | `12bb914` |
| #497 / #498 | audit (docs only) | `e5ea597` |
| #499 / #500 | audit P1 #1: token_store isolated | `c58a39f` |
| #501 / #502 | audit P1 #2: cross_momentum gate session async with | `edd0c64` |

총 4개 PR cycle (dev squash + main merge), 모두 회귀 0.

### 14:15 - [lead] 사용자 결정: P2 코드 수정 중지
- **결정**: P1 #1/#2 해소로 로컬 검증 환경의 잠재 위험 (실가동 시 발현 가능) 해소됨. P2 는 예방/관측 성격이므로 계속 코드 수정으로 달리지 않는다.
- **사유**:
  - P0/P1 잠재 위험 해소 완료 (현재 로컬 백엔드 idle in transaction 0건)
  - P2 는 예방/관측 성격이라 즉시 위험 없음
  - 계속 고치면 거래 로직 주변 refactor 가 커질 수 있음
- **다음에 할 때 우선순위**: P2 #3 (idle in transaction 모니터링) — 코드 동작 무변경 + 재발 조기 감지
  - 후보 작업 (지금은 안 함):
    - `pg_stat_activity` 에서 `idle in transaction` N분 이상이면 경고
    - `broker_credentials` / `orders` / `trade_logs` / `strategies` 관련 lock 이면 HIGH
    - 자동 kill 금지, 알림/보고만
    - trading / order / live_trader 로직 변경 금지
- **금지**: P2 코드 수정 (#1 bot.py, #2 engine.run_analysis 포함) — 사용자 명시 OK 없이는 작업하지 않음

### 14:35 - [lead] 사용자 정책 재확인 + 모의 live_trader 관찰 계획 작성
- **사용자 명시**:
  1. 현재는 자동매매 운영 상태가 아니다. read-only proposal pipeline 만 몇 번 실행한 상태라 전략 성능 / PR E2 진입 여부 판단 근거 없음.
  2. sell 누적 0 / boost_sell 0 / confidence 분포만 보고 threshold / 전략 로직 / bias 소비 변경 금지.
  3. 지금 가능한 판단은 로컬 백엔드 안정성 / timeout / DB transaction leak 같은 인프라 품질만.
  4. 다음 작업은 코드 수정이 아니라 **"모의투자 live_trader 관찰 계획"** 작성. (어떤 전략 / 몇 영업일 / 어떤 로그·DB·지표 / 중단 조건 / 다음 PR 회부 조건 5가지 다룰 것)
  5. PR E2 / threshold 변경 / bias 소비 로직 변경 **모두 보류 유지**.
- **memory 저장**:
  - `feedback_terminology_operations.md` (이전 단계) — "운영" 단어 사용 금지
  - `feedback_no_strategy_judgement_without_live_trader.md` (본 단계) — proposal 분포만으로 전략/threshold 판단 금지
- **파일 작성**: `docs/observation/2026-06-01-mock-live-trader-observation-plan.md` (신규 디렉토리 + 초안)
  - §1 전제 (다루지 않는 것 / 다루는 것 명시)
  - §2 전략 선택 — 옵션 A (cross_momentum weekly) 권장, B/C 비교
  - §3 기간 — Phase O.1 5d + O.2 10d = 15 영업일
  - §4 로그·DB·지표 — orders / trade_logs / llm_decisions / strategies + structlog
  - §5 중단 조건 — 인프라 P0 / 전략 P0 / 데이터 P0 + 중단 절차
  - §6 다음 PR 회부 조건 — 6 항목 모두 충족 + N1~N4 후보
  - §7 plan lifecycle / §8 미해결 8 항목 (사용자 결정 필요) / §9 진입점
- **상태**: 초안. 사용자 §8 항목 결정 필요. 결정 전까지 모의 live_trader 실행 / threshold 변경 / PR E2 / bias 소비 변경 모두 보류.
- **금지** (재확인): 본 plan 승인 전까지 코드 수정 0 건. P2 audit 작업도 보류.

### 14:55 - [lead] 사용자 1차 리뷰 반영 — plan v0.2
- **사용자 지적 7건 반영** (모두 코드 변경 0, 문서만 수정):
  1. **§8 마지막 문구 약화** — "본 문서 승인 = 8항목 결정 완료" 는 위험. → "방향 승인일 뿐이며, 8항목은 별도 체크리스트로 확정" 으로 수정.
  2. **§5.2 `llm_decisions.applied > 0` 중단 조건 좁힘** — 너무 넓음. → `context_source='ai_hedge'` 또는 PR E2 관련 bias 소비 origin 으로 인한 신규 applied **delta** 만 중단. 기존 overnight / manual / strategy_param_hint applied 는 정상 (관찰 시작 시점 baseline 캡쳐 → delta 기준).
  3. **§6.1 "주문 1회 이상" 조건 완화** — cross_momentum weekly 는 주문 0 이 정상. → cycle 실행 / 후보 평가 로그 / no-order reason 기록으로 변경. 주문량은 §6.2 O.2 지표.
  4. **§5.1 backend restart > 0 즉시 P0 수정** — planned rebuild / 수동 restart 는 P0 아님. → unexpected crash/restart 만 P0. planned 는 §5.5 INFO 로 기록.
  5. **§5.1 balance API 5xx 즉시 중단 완화** — 외부 upstream transient 와 내부 lock 구분. → 5xx 발생 시 즉시 `pg_stat_activity` / `pg_locks` 동반 확인. 내부 lock 동반이면 P0, 외부 upstream 만이면 §5.5 DEGRADED 로 기록 + 재시도.
  6. **§9 실행 절차 신규 추가** (preflight 10항 / start command / stop / HARD kill / rollback / daily check SQL 6 query / daily report template / 사용자 확인 트리거).
  7. **문서 상태 명시** — v0.2 헤더에 "초안 — 미커밋" 명시. git untracked 유지. PR 머지는 §8 8항목 결정 + 사용자 OK 후 별도 결정.
- **파일**: `docs/observation/2026-06-01-mock-live-trader-observation-plan.md` (untracked, v0.2)
- **상태**: 사용자 2차 리뷰 대기. 가동 / 코드 수정 / PR E2 / threshold / bias 모두 보류.
- **다음 액션**: 사용자 2차 리뷰 결과 받고, OK 면 §8 8항목 체크리스트 별도 작성 → 그것까지 OK 면 §9 절차에 따라 가동 검토.

### 15:15 - [lead] 사용자 2차 리뷰 반영 — plan v0.3
- **사용자 지적 8건 (P1 ×2 + P2 ×6) 반영** (코드 변경 0, 문서만):
  1. **[P1] §9.3 kill switch 명령이 실제 코드와 불일치** — `src.trading.kill_switch.set_status` 함수 미존재 확인. 실제 API: `KillSwitch.soft_stop/hard_stop/resume(user_id: UUID)`. live_trader 는 `data/.kill_switch` 파일 (`KILL_SWITCH_FILE`, `scripts/live_trader.py:139`) + `data/.trader.pid` (`PID_FILE`, line 140) 기준. `_TRADER_USER_ID` 가 세션마다 UUID 새로 생성 (line 92) → 외부에서 user-level API 호출은 라이브 세션과 분리. → §9.3 전면 재작성: 권장 경로 = `touch data/.kill_switch` + `cat data/.trader.pid` 로 PID 확인 후 종료. user-level API 는 UUID 확보 절차 별도 명시 + 권장 안 함.
  2. **[P1] §9.3 rollback 위험 문구** — "kill switch NORMAL 복귀" 자동 rollback 으로 보일 위험. → "rollback = 관찰 폐기 + 사용자 명시 OK 후 안전장치 해제" 로 재정의. `rm -f data/.kill_switch` 는 사용자 OK 후에만.
  3. **[P2] §4.2 / §6.2 restart 기준 통일** — §4.2 "restart 0" 단독 표기 → "unexpected restart 0 (planned 는 §5.5 INFO)" 로 통일. §6.2 #1 도 동일 수정.
  4. **[P2] §2.2 C 옵션 "운영 모니터링 부담" 표현** → "관찰 / 로컬 검증 부담 증가" 로 교체.
  5. **[P2] 변경 이력 §10 표기 오류** → "§9 실행 절차 추가" 로 정정.
  6. **[P2] Daily SQL KST timezone 미명시** — `current_date - interval '1 day'` 가 DB session timezone 의존. → `TIMESTAMPTZ 'YYYY-MM-DD 00:00:00+09'` + `+ INTERVAL '1 day'` 상한 으로 명시. 모든 6 query 갱신. `:obs_date` 변수화 안내 추가.
  7. **[P2] §6.2 매수/매도 ≥5 분류 오류** — cross_momentum weekly 는 매도 5건 미발생도 정상. → 회부 조건 (#1 unexpected restart 0 / #2 보안 위반 0 / #3 audit P1 재발 0) + 별도 "데이터 충분성 조건" (D.1 매수/매도 ≥5 / D.2 체결률 ≥70% / D.3 게이트 적정) 으로 분리. 데이터 충분성 미충족 시 FAIL 아닌 **§6.3 N1 관찰 연장** 으로 보냄.
  8. **문서 상태** — 본 plan 은 v0.3 untracked 유지. PR 머지는 사용자 v0.3 OK + §8 8항목 체크리스트 확정 후 별도 결정.
- **파일**: `docs/observation/2026-06-01-mock-live-trader-observation-plan.md` (untracked, v0.3)
- **상태**: 사용자 3차 리뷰 대기. 코드 수정 / 가동 / PR E2 / threshold / bias 변경 모두 보류.

### 15:35 - [lead] 사용자 3차 리뷰 OK + plan v0.4 PR 머지 (#503/#504)
- **사용자 지적 2건 (P2 + P3) 반영**:
  1. **[P2] §5.4 중단 절차 구버전 표현** — v0.3 §9.3 은 파일 기반으로 정리됐는데 §5.4 만 `data/.kill_switch_state.json` user-level 표현 잔존 → 문서 내부 충돌. 수정: §5.4 1번도 `touch data/.kill_switch` + `cat data/.trader.pid` 기반으로 통일. user-level `KillSwitch.hard_stop(user_id)` / `.kill_switch_state.json` 직접 조작은 라이브 세션 UUID 없으면 무효이므로 기본 절차 제외 명시 + §9.3 참조.
  2. **[P3] §9.4 SQL 변수화 설명 애매** — 상단에는 `:obs_date` 변수처럼 설명하는데 실제 SQL 은 `2026-06-04` 하드코딩. **A 안 채택** (psql 변수 + `TIMESTAMPTZ` 리터럴 실수 위험). 수정: `:obs_date` 설명 제거 + "매 영업일 `YYYY-MM-DD` 자리를 수동 치환" 명시 + 변수화 필요 시 application layer wrapper 로 안내 (본 plan 범위 밖).
- **파일**: `docs/observation/2026-06-01-mock-live-trader-observation-plan.md` (522 lines, untracked → tracked)
- **PR**: lsh0107/kiwoom-autotrade#503 dev squash `48dad62` → #504 dev→main `70585a9` → claude sync `26cc351`
- **CI**: 양쪽 PR 적용 가능 check 전체 pass (Pytest/Ruff/Bandit/Python SAST 등 code 관련은 docs only 라 skip 정상)
- **post-merge rebuild**: 불필요 (`.claude/rules/post-merge-rebuild.md` 매핑상 `docs/*` 는 none)
- **본 머지 의미**: "관찰 계획의 방향과 절차를 기준 문서로 고정". **실제 가동 승인 아님**. 가동은 §8 8 항목 체크리스트 별도 확정 + 사용자 명시 OK 후에만.
- **여전히 보류**: PR E2 / threshold 변경 / bias 소비 로직 / live_trader 실 가동 / P2 audit 작업.
- **다음 액션**: §8 8 항목 체크리스트 별도 작성 시점 사용자 결정.

### 15:55 - [lead] §8 8 항목 체크리스트 초안 작성 (코드 변경 0, 가동 0)
- **사용자 명시**:
  - 다음 작업은 코드 수정 금지
  - live_trader 실행 금지 / PR E2 / threshold / bias / 전략 로직 변경 금지 / P2 audit 코드 작업 금지
  - 산출물은 문서 초안만 — 커밋/PR 은 사용자 리뷰 후
- **파일**: `docs/observation/2026-06-01-mock-live-trader-checklist.md` (316 lines, untracked 초안)
- **구성**: §8 8 항목 각각에 권장값 / 대안 / 리스크 / 결정 필요 여부 표
  - 1 전략 옵션 — A (cross_momentum) 권장. A/B/C 트레이드오프.
  - 2 cross_momentum 모드 — weekly 권장 (§1 = A/C 인 경우만 의미).
  - 3 관찰 기간 — 15 영업일 (5+10) 권장. 10d/25d 대안.
  - 4 시작일 T+0 — 사용자가 명시 (예: 다음 영업일 월요일). 회피 권장 시점 명시 (FOMC / 만기 / 월말 / weekly trigger 일).
  - 5 lab pipeline 병행 — 병행 (연결 안 함) 권장. 통합은 PR E2 위험으로 금지.
  - 6 kill 임계 — 본 plan §5 추천값 그대로 권장. 13 개 임계 요약 표.
  - 7 다음 단계 우선순위 (N1~N4) — 회부 시점 결정 권장 (사전 N3 PR E2 확정 금지).
  - 8 실행 위치 — host + tmux 권장. docker/nohup/systemd 트레이드오프 + 환경 점검 8a~8e (env / 디스크 / KST 등).
- **추가 절차**:
  - §9 OK 절차 5 단계 (결정값 채움 → B 단독 시 §2 N/A → 환경 점검 PASS → 사용자 OK 명시 → 기준 문서 §8 append + PR 머지)
  - §10 본 문서 lifecycle (사용자 OK 후만 커밋)
  - §11 사용자 결정 입력 영역 (TBD)
- **상태**: untracked 초안 v0.1. 사용자 리뷰 대기. 코드 / 가동 / PR E2 / threshold / bias / P2 audit 모두 보류.
- **다음 액션**: 사용자 리뷰 → 결정값 입력 → OK → 커밋/PR 진행.

### 16:15 - [lead] 체크리스트 v0.2 + ACTIVE_STRATEGY source of truth 정정 (PR #505/#506)
- **사용자 P1 지적**: 체크리스트 §1/§8a + 기준 문서 §9.1/§9.2 가 `ACTIVE_STRATEGY` 를 활성 전략 source of truth 처럼 적었음. design-025 이후 source of truth = DB `strategy_runtime`. env `ACTIVE_STRATEGY` 는 deprecated / UI status 호환 / fallback only (`src/config/active_strategy.py:32-42`). 신규 관찰 계획이 ADR-024 로 되돌리는 표현 → 정정.
- **확인**: `src/config/active_strategy.py:45-74` `is_strategy_enabled_db()` 가 DB 우선 + env fallback. `src/models/strategy_runtime.py` 의 `enabled` boolean / `budget_pct` / `max_order_amount` / `max_daily_orders` 컬럼 확인.
- **수정 4건** (같은 PR 동기):
  1. 체크리스트 §1 권장값: `ACTIVE_STRATEGY=cross_momentum` → DB `strategy_runtime.enabled` 토글 표현 (cross_momentum=true, multi_regime=false, short_swing=false). source of truth 주의 박스 추가.
  2. 체크리스트 §8a: `.env` 필수 = `KIWOOM_MOCK_*` + `DATABASE_URL`. `ACTIVE_STRATEGY` 는 필수 → legacy fallback. 존재 시 DB 결정과 충돌 없는지 보조 점검.
  3. 기준 문서 §9.1 preflight #3/#4 재작성: DB `strategy_runtime` 이 #3 (source of truth), env `ACTIVE_STRATEGY` 가 #4 (충돌 없음만 보조 점검).
  4. 기준 문서 §9.2 start command 주석: 사전 단계에 DB UPDATE 절차 명시 + `ACTIVE_STRATEGY` legacy fallback 명시.
- **파일**: `docs/observation/2026-06-01-mock-live-trader-checklist.md` (신규 v0.2) + `docs/observation/2026-06-01-mock-live-trader-observation-plan.md` (v0.4 → v0.5)
- **PR**: lsh0107/kiwoom-autotrade#505 dev squash `3866210` → #506 dev→main `905eef8` → claude sync `fe6ce0c`
- **CI**: 양쪽 PR 적용 가능 check 전체 pass (docs only)
- **post-merge rebuild**: 불필요 (`docs/*` 매핑 none)
- **본 머지 의미**: "체크리스트 문서 기준 고정 + plan 의 ACTIVE_STRATEGY 잔존 문구 정정". **실제 가동 승인 아님**.
- **여전히 보류**: 모의 live_trader 가동 / PR E2 / threshold / bias / P2 audit.
- **다음 액션**: 체크리스트 §11 결정값 입력 영역에 사용자가 8 항목 + 환경 점검 8a~8e + OK 명시 → 그 후에야 §9.1 preflight 진입 가능.

### 16:35 - [lead] 가동 기본안 8 항목 입력 + 어휘 정정 (PR #507/#508)
- **사용자 입력**: 가동 기본안 8 항목 + deferred 2 건 (T+0 구체 날짜 + 환경 점검 8a~8e)
  - 1: A (DB strategy_runtime cross_momentum.enabled=true)
  - 2: weekly
  - 3: 15 영업일 (5+10)
  - 4: 사용자 여행 후 첫 영업일 (deferred)
  - 5: lab 병행 (연결 X)
  - 6: §5 추천값 채택
  - 7: 회부 시점 결정 (사전 PR E2 금지)
  - 8: host + tmux
  - 8a~8e: 가동 직전 PASS 확인 (deferred)
- **사용자 표현 정정 지적**: "결정값 확정" 어휘 → "가동 기본안 / provisional decision" 통일. T+0 + 8a~8e deferred 상태에서 "확정" 표현은 가동 승인으로 오해 가능.
- **수정**:
  - 체크리스트 §11.1 → "가동 기본안" 표 + §11.2 deferred 명시 + §11.3 OK 명시란 (가동 OK 시각 TBD)
  - 기준 plan §8.2 신규 → 가동 기본안 표 (출처: 체크리스트 §11.1)
  - 양쪽 헤더 v0.4 / v0.7 변경 이력에 표현 정정 사유 명시
- **파일**: `docs/observation/2026-06-01-mock-live-trader-checklist.md` (v0.3 → v0.4) + `docs/observation/2026-06-01-mock-live-trader-observation-plan.md` (v0.5 → v0.7)
- **PR**: lsh0107/kiwoom-autotrade#507 dev squash `9ab370b` → #508 dev→main `16e846d` → claude sync `7763ab0`
- **CI**: 양쪽 PR 적용 가능 check 전체 pass (docs only)
- **post-merge rebuild**: 불필요
- **본 머지 의미**: "가동 기본안 기록" 만. **실제 가동 승인 아님**.
- **여전히 보류**: 모의 live_trader 가동 / PR E2 / threshold / bias / P2 audit.
- **다음 액션**: 사용자 여행 복귀 후 T+0 구체 날짜 + 8a~8e PASS + 가동 OK 명시 → 그 후 §9.1 preflight 진입 가능. 본 세션 내 추가 작업 없음.

### 16:55 - [lead] 사용자 여행 복귀 — preflight 1차 수행 → 재점검 → T+0 기본안 + #7 v0.8 (PR #509/#510)
- **사용자 지시**: 가동 금지. preflight 만 수행. 결과 PASS/FAIL 보고. FAIL 있으면 수정 제안만 (코드 변경 금지).
- **1차 preflight 수행** (코드 변경 0, live_trader 실행 0):
  - 환경 점검 8a~8e: 5/5 PASS (.env / tmux+uv / data perm / Asia/Seoul / 101Gi)
  - §9.1 10항 1차 결과: PASS 7 / PARTIAL 2 (#5, #10) / FAIL 1 (#7)
  - DB strategy_runtime: cross_momentum.enabled=true (budget 0.60 / max_order 50M / max_daily 200), 나머지 false — §8.2 일치
  - strategy_config: cross_momentum.rebalance_freq='weekly' 일치
  - idle in transaction = 0 / broker_credentials row lock 0
  - llm_decisions applied baseline = 0 (모든 source)
  - data/.kill_switch_state.json: 25 user 중 normal 9 / soft_stopped 10 / hard_stopped 6 (admin 미포함)
- **1차 보고 오류 3건 (사용자 지적)**:
  1. T+0 후보 2026-06-03 = **KRX 휴장일 (지방선거)** — 보고 누락. 제외 필요.
  2. 6월 동시만기 = 2026-06-11 (목). 보고에 "6-12가 만기일" 표현 오류. 정정.
  3. balance API #5 PARTIAL 분류 오류 — 인증 없이 401 받은 건 정상 경로 검증 아님. admin@local.dev/1234 로그인 후 200 확인 필요.
- **재점검 결과**:
  - admin user_id = `0103827d-0249-4b17-81b0-acd633b118a7` (`SELECT id FROM users WHERE email='admin@local.dev'`)
  - admin user_id **kill_switch_state.json 미포함** 확인 → UI/API 표시 영향 없음
  - balance API 인증 후 5회 호출: 200 / p95=0.516s / cache hit 0.006~0.011s ✅ PASS
  - T+0 재산정: 6-15 (월) 권장 유지 (만기 회피 + 사이클 3회 관측 가능)
- **사용자 OK** (재점검 후): 가동 금지 유지. 문서 수정만 진행.
  - T+0 기본안 = 2026-06-15 (월) 기록 (대안 6-16~6-18)
  - §9.1 #7 v0.8 명확화: strict 문구 → "data/.kill_switch 파일 없음 + 새 live_trader 세션에 영향을 주는 kill switch 상태 없음" + admin user_id 별도 확인 절차
  - data/.kill_switch_state.json 삭제/초기화 금지 (다른 컨텍스트 영향 불명확)
- **파일**: 체크리스트 v0.4 → v0.5 (§11.1 항목 4 + §11.2.1 환경 점검 PASS + §11.2.2 preflight 결과 + §11.3 OK 명시 갱신), 기준 plan v0.7 → v0.8 (§8.2 항목 4 + §9.1 #7 조항)
- **PR**: lsh0107/kiwoom-autotrade#509 dev squash `c225ed1` → #510 dev→main `9c87874` → claude sync `fe66ba9`
- **CI**: 양쪽 PR 적용 가능 check 전체 pass (docs only)
- **post-merge rebuild**: 불필요
- **본 머지 의미**: T+0 기본안 기록 + #7 조항 명확화만. **실제 가동 승인 아님**.
- **여전히 보류**: 모의 live_trader 가동 / PR E2 / threshold / bias / P2 audit.
- **다음 액션**: 사용자 `"2026-06-15 가동 OK"` (또는 대안 날짜) 명시 → 그 후에야 §9.1 preflight 재확인 + 가동 가능.

### 17:25 - [lead] smoke run plan 초안 작성 (코드 변경 0, live_trader 실행 0)
- **사용자 명시**:
  - 6/15 본 관찰 시작일은 유지 (전략 관찰 + 깨끗한 첫 관찰 구간 목적)
  - 그 전에 별도 **smoke run 1회** 추가 — 모의 live_trader 실행 경로 확인 목적
  - smoke run 목적: live_trader 켜짐 / is_mock=True / DB lock 없음 / balance/token/order path / no-order reason 기록
  - smoke run 금지: 전략 성능 판단 / threshold 변경 / PR E2 진입
  - 기간: 30~60분 또는 1 cycle
  - 주문 0 = FAIL 아님
- **파일**: `docs/observation/2026-06-01-mock-live-trader-smoke-run-plan.md` (228 lines, untracked 초안 v0.1)
- **구성**:
  - §1 목적 (실행 경로 확인 only / 전략 성능 판단 금지 분리)
  - §2 시점 후보 — A: 6-04 (목) 안전 / B: 6-05 (금) 실 리밸런스 사이클 시도 (만기 직전 주 인지)
  - §3 duration: 30~60분 또는 1 cycle
  - §4 preflight 8항목 (본 plan §9.1 일부 즉시 재확인)
  - §5 점검: 진행 중 5건 + 종료 후 R.1~R.8 + FAIL 아닌 것 4건 명시
  - §6 실행 절차 (start/stop graceful/stop 시간제한/긴급 중단 — 본 plan §9.2/§9.3 동일)
  - §7 결과 기록: `mock-live-trader-smoke-run-results.md` 별도 신규 파일
  - §8 smoke run 후 액션: PASS → 6/15 그대로 / 일부 FAIL → 6/15 연기
  - §9 본 plan 변경/폐기 / §10 금지사항 7건 / §11 미해결 5건 / §12 진입점
- **여전히 보류**:
  - smoke run 자체 실행 (사용자 §11 결정 + smoke run OK 명시 전까지)
  - 6/15 본 관찰 가동
  - PR E2 / threshold / bias / P2 audit 코드 작업
- **상태**: untracked 초안 v0.1. 사용자 리뷰 대기. 커밋/PR 은 사용자 OK 후.

### 17:55 - [lead] smoke run plan v0.2 — P0 ACTIVE_STRATEGY guard + 4 추가 정정
- **사용자 P0 지적**: 문서에서는 `ACTIVE_STRATEGY` 를 legacy fallback 으로 정리했지만, **실제 `scripts/live_trader.py` 는 아직 `get_active_strategy()` 직접 사용**. 코드 검증:
  - `scripts/live_trader.py:68` `from src.config.active_strategy import ActiveStrategy, get_active_strategy`
  - `:3385` `active_strategy_mode = get_active_strategy()`
  - `:3386-3390` `ACTIVE_STRATEGY != CROSS_MOMENTUM` → 외부 holdings `strategy="momentum"` 태깅
  - `:3650-3652` `except KeyboardInterrupt: await force_close_all(force_all=True)`
  - `:2289-2302` `force_close_all` force_all=True → cross_momentum 만 보존, momentum = sell 대상
  - **위험 시나리오 실재**: ACTIVE_STRATEGY unset + Ctrl-C = 외부 보유종목 mock sell 발사
- **5 정정 반영** (모두 v0.2 문서만):
  1. **[P0] §0 ACTIVE_STRATEGY compatibility guard 신규** — 코드 검증 표 + 위험 시나리오 + smoke run 동안 `export ACTIVE_STRATEGY=cross_momentum` 명시 + §4 preflight #4/#5 추가 (env guard + DB↔env 일치)
  2. **[P1] §1/§5 PASS 기준 낮춤** — 6/4 (목) 평일은 `check_monthly_rebalance` 가 `current_hhmm == 1455 + trigger date` 아니면 `execute_monthly_rebalance` 미호출. universe / signal / no-order reason 필수 PASS 제거. PASS 기준 = 부팅 / `is_mock=True` / `ACTIVE_STRATEGY=cross_momentum` 로그 / strategy_runtime 로드 / balance / token / 오케스트레이터 tick / 비-trigger skip 확인. R.11 신규 = "의도치 않은 sell 미발생 (rows=0)".
  3. **[P1] §6 Stop 절차 수정** — Ctrl-C 는 compatibility guard 확인 후만 허용. `kill <PID>` 는 "graceful stop" 아님 (SIGTERM ≠ KeyboardInterrupt) — fallback only. `tmux send-keys C-c` 우선. §6.3.1 stale pid 정리 절차 추가.
  4. **[P2] §7 결과 문서 path** — `2026-06-01-...` (plan 작성일) → `YYYY-MM-DD-...` (실제 실행일).
  5. **[P2] B안 (6/5 금) 분리** — smoke run 이 아닌 **mini rebalance test** 로 명칭 분리 → §2.5. 본 plan 범위 밖. 별도 plan 필요. A 안 only.
- **파일**: `docs/observation/2026-06-01-mock-live-trader-smoke-run-plan.md` (228 → 401 lines, untracked 초안 v0.2)
- **상태**: 사용자 2차 리뷰 대기. smoke run 실행 / 6/15 본 관찰 가동 / mini rebalance test 모두 금지 유지.
- **여전히 보류**: live_trader 실행 0 / 코드 변경 0 / PR E2 / threshold / bias / P2 audit.

### 18:15 - [lead] smoke run plan v0.3 PR 머지 (#511/#512)
- **사용자 2차 리뷰 지적 3건 (P1 + P2×2) 반영**:
  1. [P1] §6.4 긴급 중단 제목 정정 — "compatibility guard 무관" (반대로 읽힘) → "guard 상태에 따라 mock sell 위험이 달라짐". 절차 1~4 순서 명확화 (1순위: `touch data/.kill_switch` 안전 / 2순위: guard 확인 / 3순위 guard PASS: Ctrl-C, FAIL: 사용자 즉시 보고 / 4순위 사용자 OK 후 fallback).
  2. [P2] §3 cycle 정의 — v0.1 잔재 "universe 조회 / 신호 계산 / 게이트 평가 / no-order reason" 제거. A안 평일 cycle 정의로 명확화: "strategy_runtime 조회 → orchestrator.tick 호출 → cross_momentum handler 비-trigger executed=False 또는 skip". B안 cycle 정의 mini rebalance test 문서로 인계.
  3. [P2] §8 PASS 시 표현 완화 — "6/15 시작 그대로 진행" → "6/15 본 관찰 기본안 유지. 실제 가동은 6/15 직전 §9.1 preflight + 사용자 최종 OK 후".
- **파일**: `docs/observation/2026-06-01-mock-live-trader-smoke-run-plan.md` (untracked v0.2 → tracked v0.3, 420 lines)
- **PR**: lsh0107/kiwoom-autotrade#511 dev squash `5f73a01` → #512 dev→main `f8f67aa` → claude sync `1df57fa`
- **CI**: 양쪽 PR 적용 가능 check 전체 pass (docs only)
- **post-merge rebuild**: 불필요
- **본 머지 의미**: "smoke run 계획 문서 기준 고정" 만. **smoke run 실행 / 6/15 본 관찰 가동 / mini rebalance test 모두 사용자 별도 OK 필요**.
- **여전히 보류**: smoke run 실행 / mini rebalance test / 6/15 가동 / PR E2 / threshold / bias / P2 audit / live_trader 코드 변경.
- **사용자 추가 정보**: A안 실행 날짜 추천 = **2026-06-04 09:30~10:00 또는 10:00~10:30** (주문 보기 아님, 부팅/토큰/balance/tick/DB lock 확인용).
- **다음 액션**: 사용자 §11 미해결 6 항목 (시점 / 기간 / 시작 시각 / mini test / 결과 path / guard 설정 방식) 확정 + smoke run 실행 OK 명시 → 그 후 §6 절차 진입 가능.

### 18:45 - [lead] smoke run 결정값 6항목 + baseline preflight 기록 PR 머지 (#513/#514)
- **사용자 결정값 6 항목 입력**:
  - 시점: 2026-06-04 (목)
  - 기간: 30 분
  - 시작 시각: 10:00 KST
  - mini rebalance test: smoke run PASS 후 별도 결정 (지금 진행 안 함)
  - 결과 path: `docs/observation/2026-06-04-mock-live-trader-smoke-run-results.md`
  - guard 설정 방식: tmux session 안에서 `export ACTIVE_STRATEGY=cross_momentum`
- **6/01 baseline preflight 수행** (코드 변경 0, live_trader 실행 0):
  - 즉시 PASS 8/10: is_mock_trading=True / KIWOOM_IS_MOCK 미설정 / strategy_runtime cross_momentum only (budget 0.60/max 50M/daily 200) / balance API admin 200 p95=1.677s / idle_in_tx=0 / broker_credentials lock=0 / kill_switch 깨끗 (admin user 미포함) / docker healthy / llm_decisions applied baseline=0
  - DEFERRED 2/10: #4 env ACTIVE_STRATEGY 미설정 / #5 DB↔env 일치 검증 불가 — 6/04 tmux export 후 확인 예정
  - FAIL 0
- **사용자 옵션 A 채택**: 결정값 문서 고정만. 실행은 6/04 직전 재preflight + 별도 OK.
- **smoke-run-plan v0.3 → v0.4 갱신**:
  - §11.1 결정값 6 항목 입력 완료
  - §11.2 2026-06-01 baseline preflight 결과 (PASS 8 + DEFERRED 2 + FAIL 0) — 참고용
  - §11.3 가동 직전 (6/04 10:00 KST) 재수행 + 실행 절차 6 단계 신규
    - tmux session 생성 → export ACTIVE_STRATEGY=cross_momentum → §4 재preflight 10항 → Claude PASS/FAIL 보고 → 사용자 "smoke run 실행 OK" 명시 → §6.1 start
    - 금지: 재preflight 전 실행 / 일부 FAIL 실행 진입 / PASS 여도 사용자 OK 없이 실행 / ACTIVE_STRATEGY guard PASS 없이 실행 / baseline 가정
  - §11.4 OK 명시 (6/04 재preflight + 사용자 OK 둘 다 TBD)
- **PR**: lsh0107/kiwoom-autotrade#513 dev squash `6ffd194` → #514 dev→main `f88e366` → claude sync `cb19988`
- **CI**: 양쪽 PR 적용 가능 check 전체 pass (docs only)
- **post-merge rebuild**: 불필요
- **본 머지 의미**: "결정값 기록 + 계획 문서 기준 고정 + baseline 인프라 확인" 만. **실행 승인 아님**.
- **여전히 보류**: smoke run 자체 실행 / mini rebalance test / 6/15 가동 / PR E2 / threshold / bias / P2 audit / live_trader 코드 변경.
- **다음 액션**: 2026-06-04 10:00 KST 직전 사용자가 "preflight 다시 해줘" 지시 → §11.3 절차 1~4 수행 → PASS/FAIL 보고 → 사용자 "smoke run 실행 OK" 명시 → 그 후에야 §6.1 실행. **본 세션 내 추가 작업 없음**.

## 현재 상태

| 항목 | 값 |
|---|---|
| balance API | 200 / 0.67s (cache hit 0.07s) |
| broker_credentials 행 lock | 0 건 |
| idle in transaction 세션 | 0 건 |
| 누적 daily 일자 | 5-26 / 5-27 / 5-28 / 6-1 (4 일) |
| 누적 sell 신호 | 0 |
| 누적 boost_sell | 0 |
| PR E2 | **보류 유지** |
| audit P1 #1 (token_store) | ✅ 해소 |
| audit P1 #2 (cross_momentum_rebalance) | ✅ 해소 |
| audit P2 (×3) | 미해결 (별도 PR) |

## 결정 / 정책 변경

- **Co-authored-by trailer 제거**: 본 세션 audit PR 부터 `gh pr merge --squash --body "..."` 명시 body 로 GitHub default trailer 차단. 다음 PR 모두 동일.
- **broker token transaction lifecycle**: 호출자 무관 self-contained. realtime.py 외 다른 long-lived 경로 (live_trader / reconciler / scheduler) 가 ensure_token 호출해도 안전.
- **fail-fast wrapper (PR #482) 의 의도된 동작 재확인**: 외부 hang vs 내부 leak 분리 진단 가능. 향후 동일 504 발생 시 backend `pg_stat_activity` 우선 확인.

## TODO (남은 작업, 우선순위 사용자 결정)

- [x] audit P1 #2: `cross_momentum_rebalance` `async with` 전환 — PR #501/#502 (`edd0c64`)
- [ ] audit P2 #1: `bot.py::_start_bg` 명시 commit/rollback 가드 — **보류 (사용자 결정)**
- [ ] audit P2 #2: `engine.run_analysis` (scheduler) session audit — **보류 (사용자 결정)**
- [ ] audit P2 #3: 정기 `idle in transaction` 모니터링 — **보류 (사용자 결정)**
- [ ] **다음 단계: `docs/observation/2026-06-01-mock-live-trader-observation-plan.md` §8 미해결 8 항목 사용자 결정 → 모의 live_trader 가동 → 15 영업일 관찰 → §6 다음 PR 회부 결정**
- [ ] 다음 영업일 daily pipeline 실행 — 5영업일 누적 후 PR E2 진입 판단
- [ ] backtest generated artifacts cleanup (이전 세션 종료 결정 — 별도 작업)
- [ ] AGENTS.md / CODEX.md 동기화 (이전 세션 비범위)

## 다음 세션 진입점

- 본 세션 로그
- `docs/audit/2026-06-01-async-transaction-audit.md` (§7 남은 PR 후보 5건)
- `docs/ai-hedge/PR_E_DESIGN.md` (PR E2 진입 조건)
- `ai-hedge-fund-lab/docs/07_OBSERVATION_PIPELINE.md` (§5 정량 기준)
- `CLAUDE.md` Current Status / 위험 게이트
