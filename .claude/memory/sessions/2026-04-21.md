# 2026-04-21 세션 로그

### 12:30 - [lead] Airflow DAG cron 시각 KST↔UTC 혼동 보정
- **수행**: @dag 기본 timezone=UTC인데 cron 값이 KST 의도로 작성되어 9시간 밀린 버그 수정. 7개 cron DAG 전부 UTC 기준으로 재계산. croniter로 다음 실행 시각 검증 후 schedule assertion 테스트 추가.
- **파일**: airflow/dags/overnight/index_collection.py, airflow/dags/premarket/data_collection.py, airflow/dags/periodic/{macro_weekly,stock_master_sync,news_collection}.py, airflow/dags/postmarket/trade_review.py, airflow/dags/monthly/rebalance.py, airflow/tests/dags/test_dag_integrity.py
- **결정**: 옵션 A(cron을 UTC로 조정) 채택. 옵션 B(pendulum timezone=KST)는 모든 DAG 일괄 변경 부담 커서 보류. 주석에 "UTC X시 = KST Y시" 명시해 향후 혼동 방지. Asset 트리거 DAG(analysis/llm_briefing/param_adjustment)는 cron 없어 변경 없음.
- **PR**: #273 (feat→dev squash, SHA 7f8eab3), #276 (dev→main merge, 옆 에이전트 #274와 묶여 머지됨, SHA 05e5c07). claude 브랜치 싱크 완료 (f455da7).
- **TODO**: Airflow scheduler 재시작하여 새 schedule 적용 (catchup=False라 백필 없음).

### 12:31 - [리드] Airflow LLM 호출 Anthropic→OpenAI 단독 전환
- **수행**: overnight_analysis DAG의 analyze task가 ANTHROPIC_API_KEY 미설정으로 매번 실패 fallback 체인 돌던 문제 해결. airflow/plugins/llm/client.py를 OpenAI 단독으로 간소화. 옵션 A(코드 구조 최소 변경) 채택 — DAG는 `from llm.client import generate, LLMError` 시그니처만 유지. LLMManager(src/ai/llm/manager.py) 3-provider 체인은 live_trader 경로 전용으로 유지. tests/llm 53개 전원 통과, plugins/llm/client.py 커버리지 100%.
- **파일**: airflow/plugins/llm/client.py (Claude/Gemini 호출 제거), airflow/tests/llm/test_client.py (OpenAI 성공/실패/키 미설정/system prompt/max_tokens 6케이스 재작성), airflow/tests/llm/test_briefing.py (provider 기대값 claude→gpt, ANTHROPIC_API_KEY→OPENAI_API_KEY), airflow/tests/llm/test_review.py (동일)
- **결정**: 옵션 A 채택 근거 — DAG 레벨에서 `from llm.client import generate` 만 쓰고 응답 타입 LLMResponse도 그대로 유지. 함수 시그니처 변경 없어 DAG 파일 수정 0. 미래 다른 provider 재도입 필요 시 별도 PR에서 manager 패턴으로 재설계 권장. `_get_api_key` 의미도 명확화(env 우선 → Airflow Variable fallback)하여 기존 테스트의 잘못된 expectation("Variable 우선") 수정.
- **PR**: #274 (fix/airflow-openai-only→dev squash, SHA ec6dfdb), #276 (dev→main merge commit, SHA 05e5c07). CI 전원 통과. claude 브랜치 싱크 완료 (f455da7).
- **TODO**: Airflow 컨테이너 재시작 후 overnight_analysis 수동 트리거로 실제 LLM 호출 확인. overnight.py 단위 테스트는 이 PR 범위 밖 — 추후 별도 PR 권장.

### 13:17 - [lead] backend 컨테이너 (unhealthy) 원인 진단 + 수정
- **수행**: `kiwoom-autotrade-backend-1` `Up 27 minutes (unhealthy)` 원인 진단. `docker inspect` 확인 결과 healthcheck 명령이 `curl --fail http://localhost:8000/api/health`인데 python:3.12-slim 베이스 이미지에 curl 미설치로 `executable file not found in $PATH` 에러가 FailingStreak 79회 누적. /api/health 엔드포인트는 src/main.py:98에 이미 존재했으므로 엔드포인트 추가 불필요. Dockerfile에 curl 추가하지 않고 python 표준 라이브러리(urllib.request) 기반 healthcheck로 교체하여 이미지 슬림 유지.
- **파일**: docker-compose.yml (backend.healthcheck 추가 — python urllib 기반), tests/api/test_health.py (회귀 방지 5 케이스 신규)
- **결정**: 옵션 A(python urllib) 채택 근거 — curl 설치 시 이미지 증가·의존성 증가, wget도 미설치. urllib은 표준 라이브러리라 런타임 비용 0. start_period=20s로 uvicorn 기동 여유 확보.
- **PR**: #279 (fix/backend-healthcheck→dev squash, SHA b7120bf), #281 (dev→main merge commit, SHA 5dba58b). CI 전원 통과(Pytest coverage 85%+ 유지). claude 브랜치 싱크 완료 (c139fda).
- **검증**: `docker compose up -d --force-recreate --no-deps backend` 후 30초 대기 → `Status=healthy FailingStreak=0`, `docker compose ps` 에서 `Up 39 seconds (healthy)` 확인 완료.
- **TODO**: 없음. 재발 시 test_health.py가 CI에서 회귀 감지.

### 13:17 - [리드] T5-C: tests/test_live_trader.py 대규모 정리
- **수행**: 감사 문서(test-audit-2026-04-20.md)의 T5-C 지침에 따라 193 mock 파일인 test_live_trader.py 정리. parametrize 통합 + useless assertion 삭제 + implementation coupling 재작성 위주.
- **파일**: tests/test_live_trader.py, docs/test-audit-2026-04-20.md
- **수치**: def test_ 111→79 (-32), mock refs 421→393 (-28), assert_called 7→4 (-3), LoC 2644→2561 (-83). 실제 테스트 항목(parametrize 확장 포함) 111→108.
- **커버리지**: 전체 src 90.66% 유지. scripts.live_trader 개별 52% 유지(변화 없음).
- **주요 변경**: TestSafeInt/CalcTimeRatio/BuildStrategies/RegimeAllocation/UpdateRiskAfterTrade parametrize 통합. TestDualStopLoss의 Python `max()` 동작 테스트 삭제. TestRunTradingLoopWs.test_main_ws_fallback_to_polling(SUT 미호출 useless) 삭제. test_kill_switch_force_close_after_sell는 `mock_force_close.assert_called_once()` → `state.positions == {}` 상태 기반으로 재작성해 실제 force_close_all 경로 커버. TestThemeBoost/FlowSignal 6~7개 simple 테스트를 scenarios parametrize로 통합. WS/주문/포지션 핵심 행동 테스트 전부 보존.
- **PR**: #282 (test/cleanup-live-trader→dev squash, SHA 6c2be1c), #284 (dev→main merge, SHA 428ef23). 전 CI 통과. claude 싱크 완료 (bb17d73).

### 13:26 - [리드] trailing_stop armed threshold 추가 (299660 과민 컷 방지)
- **수행**: 2026-04-20 299660(현대로템) 1차 사이클에서 진입가 4,440 → 매도 4,435(-0.11%) trailing_stop 과민 발동 진단. 원인은 src/backtest/strategy.py:158의 `peak_price > entry_price` 조건이 1틱 상승만으로도 만족되어 trailing gap(-1.0%)에 쉽게 닿는 구조. src/strategy/momentum.py 래퍼에 armed threshold 적용: `high_since_entry < entry_price * (1 + TRAILING_ARMED_PCT)`이면 내부 호출 시 `peak_price=entry_price` 전달해 trailing 무효화. armed 전에도 stop_loss/dynamic_stop는 작동.
- **파일**: src/strategy/momentum.py, tests/strategy/test_base.py (TestMomentumStrategyTrailingArmed 5 케이스 신규)
- **결정**: 옵션 A(armed threshold) 채택. `USE_TRAILING_ARMED=false` 기본 → 기존 동작 100% 보존. `TRAILING_ARMED_PCT=0.005` 기본 0.5%. src/backtest/* 미수정 (라이브 래퍼에서만 처리).
- **PR**: #280 (fix/trailing-stop-armed→dev squash, SHA a13ea7f), #283 (dev→main merge, SHA 0f34fb7). 전 CI 통과. claude 싱크 완료 (8330333).
- **롤백**: `USE_TRAILING_ARMED=false` 또는 unset (기본값) → 즉시 롤백. 코드 revert 불필요.
- **TODO**: 운영 환경 `.env`에 `USE_TRAILING_ARMED=true` 추가 후 live_trader 재시작해 활성화.

### 14:41 - [리드] 텔레그램 봇 토큰 URL 로그 마스킹 강화
- **수행**: httpx INFO 로그가 `https://api.telegram.org/bot<token>/getUpdates` 평문 기록한 유출 사고(2026-04-21) 회귀 방지. 2중 방어층 구성: (1) secret_masking.py에 `_TELEGRAM_URL_RE` 추가(스킴 캡처 그룹 보존), 단독 토큰 패턴보다 먼저 적용. (2) live_trader setup_logging에서 httpx/httpcore 로거 WARNING 하향. .gitignore에 *.log 추가로 실수 커밋 차단.
- **파일**: .gitignore, src/utils/secret_masking.py, scripts/live_trader.py, tests/utils/test_secret_masking.py
- **결정**: 기존 `bot\d+:[A-Za-z0-9_-]+` 패턴은 token을 잡지만, URL 단위 통째 치환이 디버깅 가능성(경로 세그먼트 보존)과 가독성에 유리. URL 패턴 우선 적용 후 fallback으로 단독 토큰 패턴. 스킴(http/https) 보존을 위해 캡처 그룹 도입.
- **PR**: #285 (feat/mask-telegram-url-log→dev squash, SHA 4fa0386), #286 (dev→main merge, SHA 4fc2e7a). 전 CI 통과, secret_masking 커버리지 95%. claude 싱크 완료 (0d2a35d).
- **TODO**: 유출된 토큰은 사용자가 직접 재발급. docs/backtest-results/live_20260421.log는 로컬에서 이미 삭제(git 이력 미포함).

### 15:36 - [리드] Design 011 PR 1: daily_candles 테이블 + Alembic 012
- **수행**: 장중 live_trader 26분 hang 원인(스크리닝/초기화 시 종목당 ka10086 페이징 5~6회 × 100종목으로 HTTP 429 누적) 해소 위한 일봉 DB 캐싱 시리즈 착수. PR 1은 런타임 영향 0의 테이블/모델 스캐폴딩.
- **파일**: src/models/daily_candle.py (신규), src/models/__init__.py, alembic/versions/012_daily_candles.py (신규), tests/models/test_daily_candle.py (신규), docs/design/design-011-daily-candle-caching.md (신규)
- **결정**: (symbol, date) 복합 PK 채택. source 컬럼(기본 pykrx)으로 수집 출처 구분. PR 2에서 DAG + backfill, PR 3에서 DailyCandleStore, PR 4에서 live_trader/screen_symbols flag-off 리팩토링.
- **PR**: #287 (feat/daily-candle-model→dev squash, SHA a311466), #288 (dev→main merge, SHA 8adb674). 전 CI 통과. claude 싱크 완료 (85bcff4).
- **테스트**: tests/models/test_daily_candle.py 6/6 passed.
- **TODO**: PR 2 (Airflow DAG + backfill) 착수.

### 15:56 - [리드] Design 011 PR 2: daily_candle_collection DAG + backfill 스크립트
- **수행**: PR 1 daily_candles 테이블을 채우는 수집 파이프라인. 소비자 없어 런타임 영향 0.
- **파일**: airflow/plugins/collectors/candles.py (신규), airflow/dags/postmarket/daily_candle_collection.py (신규), scripts/backfill_daily_candles.py (신규), airflow/tests/collectors/test_candles_collector.py (신규), airflow/tests/dags/test_daily_candle_dag.py (신규), airflow/tests/dags/test_dag_integrity.py (expected 목록·cron 업데이트)
- **결정**: DAG 스케줄 `0 9 * * 1-5` UTC = KST 월~금 18:00(장 마감 후). pykrx `get_market_ohlcv_by_ticker`로 시장별 일괄 + psycopg2 `ON CONFLICT (symbol,date) DO UPDATE`. backfill은 종목별 `get_market_ohlcv_by_date` 루프(2000건 배치 upsert). 티커 6자리 zero-pad. tags=[postmarket, daily_candle, tier1]. retries=3, timeout=60m. Asset 발행.
- **PR**: #289 (feat/daily-candle-dag→dev squash, SHA d5c5789), #290 (dev→main merge, SHA 25245d2). 전 CI 통과. claude 싱크 완료 (b14dbb7).
- **테스트**: airflow/tests 로컬 9/9 passed (collectors). DAG 무결성 테스트는 로컬 structlog 호환성 이슈로 skip, CI는 src/ 만 수행해 간섭 없음. tests/models+scripts regression 69/69 passed.
- **TODO**: PR 3 (DailyCandleStore 조회 모듈) 착수.

### 16:12 - [리드] Design 011 PR 3: DailyCandleStore 조회 모듈
- **수행**: daily_candles DB 우선 조회 + 키움 폴백 + 프로세스 캐시 레이어. 호출부 미변경이라 런타임 영향 0.
- **파일**: src/trading/daily_candle_store.py (신규), tests/trading/test_daily_candle_store.py (신규)
- **결정**: `USE_DB_DAILY_CANDLES` 환경변수로 on/off. `database_url is None`이면 강제 off(안전장치). DB bars < 20이면 키움 ka10086 폴백. MarketContext 스타일 lazy async engine + dict 캐시. `get_daily_context(symbols)` 반환 타입이 live_trader.load_daily_context와 동일해 PR 4에서 드롭인 교체.
- **PR**: #291 (feat/daily-candle-store→dev squash, SHA b815947), #292 (dev→main merge, SHA 2bd58a7). 전 CI 통과. claude 싱크 완료 (2ec2877).
- **테스트**: tests/trading/test_daily_candle_store.py 24/24 passed. trading+models 287/287 regression 통과.
- **TODO**: PR 4 (live_trader/screen_symbols flag-off 리팩토링) 착수.

### 16:29 - [리드] Design 011 PR 4: live_trader/screen_symbols flag 분기 (시리즈 완료)
- **수행**: scripts/live_trader.load_daily_context, scripts/screen_symbols.fetch_daily_pages 내부를 USE_DB_DAILY_CANDLES 분기로 교체. 시그니처/반환 타입 동일. 기본 off → 운영 영향 0.
- **파일**: scripts/live_trader.py, scripts/screen_symbols.py, tests/scripts/test_daily_candle_consumer_flag.py (신규 4 케이스)
- **결정**: 두 호출부에서 동일한 `_is_db_daily_candles_enabled()` 헬퍼로 분기 후 `DailyCandleStore` 위임. `lookback_days = max(60, max_pages * 30)`로 기존 페이징 범위 유사 매핑. flag off 시 DailyCandleStore 생성조차 안 하도록 early branch — 테스트로 보장.
- **PR**: #293 (feat/daily-candle-consumer-refactor→dev squash, SHA 8f14e02), #294 (dev→main merge, SHA 9f9c433). 전 CI 통과. claude 싱크 완료 (c928201).
- **테스트**: tests/scripts/test_daily_candle_consumer_flag.py 4/4 passed. tests/test_live_trader + screen + scripts + trading 411/411 regression passed. mypy 에러 수 기존 9개와 동일(내 변경이 추가한 오류 없음).
- **롤백**: `USE_DB_DAILY_CANDLES=false`(또는 unset) → 즉시 원복. 코드 revert 불필요.
- **다음**: Airflow DAG 실배포 후 backfill 1회 실행 → DB 적재 확인 → `.env`에 `USE_DB_DAILY_CANDLES=true` 설정해 활성화.
