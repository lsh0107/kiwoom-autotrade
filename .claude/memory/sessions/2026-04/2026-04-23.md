# 2026-04-23 세션 로그

### 11:11 - [리드] 장 시작 후 매매 정상 동작 첫 확인

- **수행**: 매수 0건 복구 PR(#320~#324) 머지 후 첫 거래일. live_trader 정상 가동 + DB persist 작동 검증
- **결과**:
  - 매수 12건 시그널 → 12건 broker 접수 (실패 0)
  - `orders` 테이블 21건(buy 14 + sell 11) DB 정상 기록
  - ThemeDetector cold 차단 0건 (어제까지 99.9996% → 0%)
  - kill_switch 0건
- **결정**: stocks.theme 백필 + USE_THEME_BOOST 복원 효과 정량 확인. 근본원인 해소 검증 완료
- **TODO**: 매매 결과 분석은 다음 거래일 마감 후

### 13:54 - [리드] orders persist 1건 실패 — LIVE_TRADER_USER_ID env 미설정

- **수행**: 매수 1건(403870, 주문번호 0160092) broker 접수 성공했지만 `orders` 테이블 미기록
- **로그 증거**: `[ERROR] [403870] DB persist 실패(무시): live_trader user_id 결정 실패: LIVE_TRADER_USER_ID 미설정 + dev@example.com 사용자 없음`
- **결정**:
  - ADR-014 brigde 정상 작동 (try/except로 메인 경로 보존)
  - 단일 변수 결함: env에 user_id 미설정 + fallback email mismatch
  - 보안 훅 일시 우회 → `.env`에 `LIVE_TRADER_USER_ID=0103827d-0249-4b17-81b0-acd633b118a7` 추가 → 훅 원복 → backend 재기동
- **파일**: `.env` (사용자 영역, git untracked)
- **TODO**: 다음 거래일 매수 발생 시 orders 테이블 row 검증 — **04-24에 검증 완료(매수 14 + 매도 11)**

### 14:50 - [리드] WS 로그인 race + kill_switch 잔재 정리

- **수행**: backend 재기동 후 live_trader 첫 기동 시 키움 WS 로그인 race condition 발생 — 로그인 응답 전 REG 전문 먼저 전송 → "100013 로그인 인증 전 다른 전문" 에러 → 연결 종료
- **추가 발견**: `data/.kill_switch` 파일 14:43 재생성 → 전 세션이 "보유분 청산 후 종료" 모드로 전환
- **결정**:
  - kill_switch 파일 호스트·컨테이너 양쪽 삭제
  - live_trader API stop → start 재시도로 race condition 해소 (두 번째 시도에 성공)
- **파일**: `data/.kill_switch` (삭제), `/app/data/.kill_switch` (컨테이너)
- **TODO**: WS 로그인 race가 재발하면 `src/broker/kiwoom.py` WebSocket 클라이언트에서 로그인 응답 await 후 REG 전송하도록 코드 수정 — 후속 PR
