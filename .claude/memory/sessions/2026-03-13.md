# 세션 로그 2026-03-13

### 00:50 - [backend] 키움 REST API 문서 기준 테스트 및 버그 수정

- **수행**: docs/kiwoom-rest-api/*.md 공식 문서와 src/broker/ 코드 비교 검증, 발견된 버그 4개 수정
- **파일**:
  - `src/broker/kiwoom.py` — 버그 수정
  - `src/broker/constants.py` — ORDER_TYPE_CODES 추가, deposit→account_eval API ID 변경
  - `tests/broker/test_kiwoom.py` — 수정된 동작에 맞게 테스트 업데이트
- **결정**:
  - trde_tp 값: "buy"/"sell" → "00"(지정가)/"03"(시장가) 2바이트 코드로 수정
  - 주문가능금액 API: kt00001(예수금) → kt00004(계좌평가현황), 필드: ord_alow_amt → ord_alowa
  - kt00018 qry_tp: "2"(개별) → "1"(합산)
  - _parse_expires_dt: KST → UTC 변환 추가
- **커밋**: `b0ee932` fix(broker): 키움 REST API 문서 기준 4개 버그 수정
- **TODO**: QA 통합 테스트 확인 (Task #5)

### 발견된 버그 목록 (Task #3 결과)

| # | 심각도 | 위치 | 문제 | 수정 |
|---|--------|------|------|------|
| 1 | HIGH | kiwoom.py:489 | trde_tp "buy"/"sell" (2바이트 초과) | "00"/"03" |
| 2 | CRITICAL | kiwoom.py:662 | kt00001 응답에 ord_alow_amt 없음 → 항상 0 반환 | kt00004 + ord_alowa |
| 3 | MEDIUM | kiwoom.py:645 | kt00018 qry_tp "2"(개별)로 합산 데이터 오조회 | "1"(합산) |
| 4 | MEDIUM | kiwoom.py:88-90 | expires_dt KST → UTC 9시간 오차 | astimezone(UTC) |

### 02:XX - [backend] 남은 이슈 3개 수정

- **수행**: Lock 메모리 누수 / WebSocket 토큰 자동갱신 / 주문체결 DB 연동 수정
- **파일**:
  - `src/broker/token_store.py` — `dict` → `weakref.WeakValueDictionary` (Lock GC)
  - `src/broker/kiwoom.py` — `_ensure_token` → `ensure_token` (public 노출)
  - `src/api/v1/realtime.py` — `_get_token`: `authenticate()` → `ensure_token()`, `on_order_exec` 콜백 추가
  - `tests/broker/test_token_store.py` — Lock GC 테스트 2개 추가
  - `tests/api/test_realtime.py` — on_order_exec 설정 확인, ensure_token 사용 확인 테스트 추가
- **결정**:
  - `_locks`를 `WeakValueDictionary`로 교체 → Lock 사용 종료 후 자동 GC
  - WebSocket 토큰 갱신: `_ensure_token()` 호출로 캐시 재사용 + 만료 시 자동 갱신
  - 주문체결 상태 "913" 값: "체결" → FILLED, "부분체결" → PARTIAL_FILL, "접수" → 무시
- **커밋**: `b653c1b` (소스), `eb51470` (테스트)
- **결과**: 693 tests passed, 93.61% coverage

### 01:12 - [lead] QA 검증 + main 배포 완료

- **수행**: 전체 QA 검증 후 기능단위 main 배포 실행
- **QA 결과**:
  - pytest 689 passed, coverage 94.01% (94.07%)
  - ruff check: All checks passed
  - pre-commit run --all-files: 전체 통과
- **배포 흐름**:
  1. PR #113 (문서 정합성 수정 + 백엔드 4개 버그) → dev squash merge ✅
  2. PR #114 (dev → main) → merge commit ✅
  3. PR #115 (주문체결 DB 연동 + 토큰 갱신 + Lock 누수) → dev squash merge ✅
  4. PR #116 (dev → main) → merge commit ✅
  5. claude 브랜치 main 싱크 ✅
- **팀 해체**: doc-code-sync 팀 전체 작업 완료

### 01:30 - [lead] 핫픽스: qry_tp 누락 + 모의투자 API 호환 + OrderSide 대소문자 통일

- **수행**: 사용자 실사용 중 발견된 3건 긴급 수정
- **파일**:
  - `src/broker/constants.py` — kt00004→kt00005→kt00001 최종 확정
  - `src/broker/kiwoom.py` — 예수금 API kt00001 + qry_tp:"2" + entr 필드
  - `src/broker/schemas.py` — OrderSideEnum "BUY"→"buy", BeforeValidator 대소문자 정규화
  - `src/api/v1/orders.py` — CreateOrderRequest side field_validator 추가
  - `src/trading/kill_switch.py` — check_drawdown side 비교 소문자 통일
  - `src/ai/signal/order_builder.py` — side "BUY"→"buy"
  - `tests/` — 관련 테스트 12개 파일 대소문자 수정
- **버그 상세**:
  1. kt00004 호출 시 qry_tp 필수 파라미터 누락 → 키움 API 1511 에러
  2. kt00005(체결잔고) 모의투자 미지원 → RC9000 에러
  3. OrderSideEnum "BUY"/"SELL" vs OrderSide "buy"/"sell" 불일치 → validation 에러
- **결정**:
  - 주문가능현금: kt00001(예수금상세현황) + entr 필드 (모의투자 호환)
  - OrderSideEnum 소문자 통일 + BeforeValidator로 대소문자 무관 수용
- **PR**: #117 → dev, #118 → main ✅

### 04:20 - [lead] 핫픽스: SQLAlchemy Enum DB 불일치 + 프론트 대소문자

- **수행**: 주문 실행 시 internal server error 수정
- **파일**:
  - `src/models/order.py` — Enum() values_callable 추가 (name→value 매핑)
- **버그**: SQLAlchemy Enum(OrderStatus)이 name("FAILED") 전송, DB enum은 소문자("failed") → InvalidTextRepresentationError
- **결정**: `values_callable=lambda e: [m.value for m in e]`로 소문자 value 사용
- **결과**: 689 tests passed, coverage 93.47%
- **PR**: #119 → dev, #120 → main ✅

### 04:31 - [lead] 피드백 반영: API 에러 상세 로깅 강화

- **수행**: 사용자 피드백 — "에러 raise만 하고 상세 로깅 없어서 원인 못 찾음" 반영
- **파일**:
  - `src/broker/kiwoom.py` — _request() 요청 body + 에러 응답 전문 로깅
  - `src/api/v1/orders.py` — 인바운드 요청 로깅 + 제출 실패 시 상세 컨텍스트
  - `src/api/v1/account.py` — 잔고 조회 실패 시 에러 로깅 추가 (기존 없었음)
- **배포**: PR #119→dev, #120→main, claude 싱크 완료

### 오늘 배포 전체 요약

| PR | 내용 | 상태 |
|---|---|---|
| #113 | 문서 정합성 수정 + 키움 API 4개 버그 | ✅ main |
| #114 | dev→main 릴리즈 | ✅ main |
| #115 | 주문체결 DB 연동 + 토큰 갱신 + Lock 누수 | ✅ main |
| #116 | dev→main 릴리즈 | ✅ main |
| #117 | 모의투자 API 호환 + OrderSide 소문자 통일 | ✅ main |
| #118 | dev→main 릴리즈 | ✅ main |
| #119 | Enum DB 매핑 + 로깅 강화 + 대소문자 호환 | ✅ main |
| #120 | dev→main 릴리즈 | ✅ main |
| #121 | 단타/스윙 이중 전략 + 그리드 서치 | ✅ main |
| #122 | dev→main 릴리즈 | ✅ main |

---

### 06:30 - [lead] 단타 전략 전면 개선 + 이중 전략 시스템

- **수행**: 전략 R:R 비율 개선, 이중 전략 자동 분류, 그리드 서치, 프론트엔드 오류 수정
- **파일**:
  - `src/backtest/strategy.py` — R:R 3:1 (SL -0.5%/TP +1.5%), 트레일링 스톱, 진입시간 필터(09:05-13:00), 양봉 필터, RSI/ATR 옵션
  - `src/backtest/engine.py` — `_simulate()` 통합 리팩토링, peak_price 추적
  - `src/backtest/grid_search.py` — (신규) ATR% 기반 단타/스윙 자동 분류, 9차원 파라미터 그리드
  - `scripts/run_grid_search.py` — (신규) 그리드 서치 실행 스크립트
  - `src/api/v1/results.py` — glob 패턴 `backtest_*.json`으로 변경 (grid_search 제외)
  - `frontend/src/app/(authenticated)/results/page.tsx` — `Array.isArray()` 가드 추가
  - `tests/` — 전략/엔진/그리드/API/live_trader 테스트 수정 (기본값 변경 반영)
- **결정**:
  - 기본 volume_ratio: 1.5 → 0.5 (5분봉 누적거래량 기준 현실적 값)
  - high_52w_threshold: 0.80 → 0.0 (단타에서 52주 필터 비활성)
  - 변동성 분류: ATR% > 2% → 단타, ≤ 2% → 스윙
  - 단타 그리드: SL [-0.3~-1%], TP [1~3%], Vol [0.3~0.7], trailing [None/-0.3%/-0.5%]
  - 스윙 그리드: SL [-2~-5%], TP [3~8%], Vol [0.3~0.5], trailing [None/-1%/-1.5%]
- **백테스트 결과** (5일):
  - HPSP(403870): PF 4.12, 승률 75%, +1.04% avg
  - 032830: PF ∞, 승률 100%, +0.55% avg
  - 삼성전자: PF 0.63, 승률 40% (단타 부적합)
- **PR**: #121 → dev (squash), #122 → main (merge) ✅
- **CI 수정 3회**: ruff SIM102/SIM103, test_results 파일명, test_base volume_ratio, test_live_trader 52w threshold

### 07:15 - [lead] 유니버스 확장 + 스크리닝 + 자동매매 실행

- **수행**: 거래량 활발한 종목 추가, 스크리닝 실행, 자동매매 백그라운드 실행
- **파일**:
  - `scripts/screen_symbols.py` — 유니버스 30 → 50개 확장 (KOSPI 거래량 활발주 15개 추가)
  - `scripts/live_trader.py` — CLI 기본값 업데이트 (vol_ratio=0.5, TP=1.5%, 52w=0.0)
  - `scripts/start_trading.sh` — (신규) 스크리닝+자동매매 원클릭 실행 스크립트
- **스크리닝 결과**: 15개 종목 선별 (price_ratio 상위)
  - HD현대중공업(92.6%), 포스코인터(92.5%), HPSP(88.6%), 한화오션(88.3%), 현대건설(88.0%)
  - 하이브(87.5%), HD현대(87.2%), KB금융(86.5%), LG(86.1%), HD한국조선해양(86.0%)
  - 신한지주(85.7%), KT&G(85.7%), 펄어비스(85.1%), SK하이닉스(84.6%), 삼성SDI(84.5%)
- **자동매매**: PID 45301, WebSocket 모드, 모멘텀 전략, 장 시작(09:00) 대기 중
- **TODO**: 장 종료(15:35) 후 결과 확인
