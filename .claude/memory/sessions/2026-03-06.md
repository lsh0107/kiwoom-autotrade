# 2026-03-06 세션 로그

### 00:00 - [리드] 문서 용어 통일 PR 배포 완료 (main 머지)
- **에이전트**: 리드
- **수행**:
  1. 세션 파일 충돌 해결 (stashed changes 채택)
  2. 9개 파일 커밋: `docs(rules): Agent Teams 용어 통일 + 문서 전면 재검토`
  3. PR #31 (docs/agent-teams-terminology → dev): 전체 Actions 통과, 머지
  4. PR #32 (dev → main): Bandit, CodeQL, Secret Detection, TruffleHog, Dependency Audit, Python SAST 전체 통과, 머지
  5. claude 브랜치 main과 싱크, feature 브랜치 삭제
- **PR**: https://github.com/lsh0107/kiwoom-autotrade/pull/31, https://github.com/lsh0107/kiwoom-autotrade/pull/32

### 00:09 - [리드] 개발환경 이슈 4건 해결
- **에이전트**: 리드
- **수행**:
  1. **ShadCN MCP**: `.mcp.json` 생성 (npx shadcn@latest mcp)
  2. **Hooks 구버전 API 수정**: security-scan.sh, activity-log.sh 두 파일 모두 새 Hook API로 전환
     - `CLAUDE_TOOL_NAME` env → JSON `.tool_name` 필드
     - `d.get('file_path')` → `d.get('tool_input', {}).get('file_path')`
     - `{"decision":"block"}` + exit 2 → stderr 메시지 + exit 2
  3. **서버 이슈 확인**: 서버 정상 작동. `GET /` 404는 정상, `/docs`는 `DEBUG=true` 필요
  4. **pre-commit install**: `.git/hooks/pre-commit` 설치 완료
- **생성 파일**: .mcp.json
- **수정 파일**: .claude/hooks/security-scan.sh, .claude/hooks/activity-log.sh
- **결정**: Swagger UI는 DEBUG=true일 때만 노출 (기존 보안 설정 유지)
- **TODO**:
  - Phase 1 프론트엔드 착수 (Next.js + ShadCN UI)
  - B12 Sandbox 설정 검토
  - 커밋 + PR
