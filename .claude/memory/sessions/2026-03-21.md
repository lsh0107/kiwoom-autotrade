### 00:07 - [data-eng] Airflow stock_master_sync DAG 신규 생성 + rebalance.py pool 버그 수정

- **수행**:
  1. `airflow/dags/periodic/stock_master_sync.py` 신규 생성 — 매월 1일 UTC 01:00 실행, pykrx로 KOSPI/KOSDAQ 전 종목 upsert 후 가격 상관관계(r≥0.7) 계산해 stock_relations 저장
  2. `airflow/dags/monthly/rebalance.py` 버그 수정 — `WHERE pool = 'A'` → `WHERE pool = 'pool_a'` (004_stock_universe.py seed 데이터 실제 값과 일치)
- **파일**:
  - `airflow/dags/periodic/stock_master_sync.py` (신규)
  - `airflow/dags/monthly/rebalance.py` (수정)
- **결정**:
  - lazy import 패턴 준수 (pykrx, psycopg2, pandas 모두 @task 함수 내부에서 import)
  - _get_db_conn() 대신 인라인 conn_uri 처리 — rebalance.py 스타일 유지
  - postgresql+asyncpg:// 접두사 변환 추가 (storage.py 기준 완전 적용)
- **TODO**: Task #4 QA 검증 대기

### 00:09 - [backend] ORM 모델 생성 + Alembic 마이그레이션

- **수행**:
  1. `src/models/stock.py` 신규 생성 — `Stock`, `MarketCapTier`
  2. `src/models/stock_relation.py` 신규 생성 — `StockRelation`, `RelationType`
  3. `src/models/stock_universe.py` 수정 — `stock_id` FK 컬럼 + `stock` relationship 추가
  4. `src/models/news_article.py` 수정 — `stock_id` FK 컬럼 + `stock` relationship 추가
  5. `src/models/__init__.py` 수정 — `Stock`, `StockRelation`, `MarketCapTier`, `RelationType` 추가
  6. `alembic/versions/005_stocks_master.py` 신규 생성 — stocks 테이블 생성 + stock_universe/news_articles FK
  7. `alembic/versions/006_stock_relations.py` 신규 생성 — stock_relations 테이블 + sector_peer 시드
- **파일**:
  - `src/models/stock.py` (신규)
  - `src/models/stock_relation.py` (신규)
  - `src/models/stock_universe.py` (수정)
  - `src/models/news_article.py` (수정)
  - `src/models/__init__.py` (수정)
  - `alembic/versions/005_stocks_master.py` (신규)
  - `alembic/versions/006_stock_relations.py` (신규)
- **결정**:
  - TYPE_CHECKING + forward ref으로 순환 import 방지
  - UUIDMixin의 id 컬럼과 충돌 방지 — stock_universe/news_article의 기존 id 컬럼 그대로 유지
  - SQL 내 긴 줄은 줄바꿈으로 처리 (noqa 대신)
- **TODO**: Task #4 QA 검증 완료

### 00:12 - [qa] 종목 DB 정규화 QA 검증

- **수행**:
  1. pytest 969개 전체 통과, 커버리지 91.35% (기준 85% 초과)
  2. ruff check — All checks passed (신규 모델 7개 파일)
  3. mypy — No issues (src/models/stock.py, stock_relation.py, stock_universe.py, news_article.py)
  4. import 검증 — `from src.models import Stock, StockRelation, MarketCapTier, RelationType` OK
  5. 마이그레이션 체인 확인 — 004→005→006 정상
  6. SQL 검토 — DISTINCT ON(symbol) + ON CONFLICT DO NOTHING 적절, sector_peer 시드 JOIN 로직 정상
  7. rebalance.py pool — `WHERE pool = 'pool_a'` 정상 확인
  8. DAG import — structlog 25.5.0 ↔ Airflow SDK 버전 충돌로 초기화 실패 (코드 이슈 아님, 환경 이슈)
- **파일**: 수정 없음 (모든 검증 통과)
- **결정**:
  - DAG import 에러는 `.venv` 내 structlog 버전 문제 — Airflow 환경 재구성 필요 (별도 이슈)
  - DAG 코드 자체(lazy import, tags, catchup=False, default_args)는 규칙 준수 확인
- **TODO**: 없음
